# 互联网医疗中心平台 · 整体进度与下一步计划

> 文档版本：V1.0（汇总版）
> 编制日期：2026-07-26
> 依据基线：PRD V2.0（双主线、7 类用户）、技术架构设计方案 V1.2、《工程进度与计划.md》、《P0–P3 自检与对抗式审查报告.md》、实际代码树（`code/`）
> 适用范围：管理层 / 团队交付状态对齐与上线排期决策

---

## 一、总体状态（一句话结论）

**代码交付基本完成（P0–P3 平台治理与数据中台后端地基已就位，机制级验证 21/21 PASS），但"上线就绪度 = 未就绪"：缺等保三级测评、互联网医院/药品经营等执业许可（硬门槛）、真实第三方对接与运行时验证。** 当前交付物是"可内部研发验证的后端骨架"，而非"可对外运营的互联网医疗产品"。

| 维度 | 结论 |
| --- | --- |
| 计划完成度（P0–P3） | 代码交付基本完成，机制级验证 21/21 PASS，`tsc --noEmit` 0 错误 |
| PRD V2.0 功能覆盖 | 后端数据域 ≈70%、前台/工作台 ≈10%、平台监管 ≈20%；存在显著缺口 |
| 技术架构方案覆盖 | RBAC / RLS / plat / 可解释 / 数据分级已实现；第三方对接 / 等保测评部分或未做 |
| 上线就绪度 | **未就绪**：缺等保测评 + 执业许可（硬门槛）+ 真实第三方对接 + 运行时验证 |

---

## 二、平台概况与交付范围

- **定位**：双主线架构 —— ① 互联网医院（2C 在线复诊 + 电子处方 + 药品销售）；② 健康数据中台（B2B2C 连锁门店赋能，以"连续、结构化的治疗效果数据"为核心壁垒支撑第二阶段融资估值）。聚焦疼痛管理 / 调理领域。
- **目标用户（7 类）**：患者/亚健康人群（2C）、执业医师、医院管理员、连锁门店管理员、调理师/健康顾问、平台运营管理员、星耀产业资本/数据中台运营。
- **技术栈**：
  - 后端：Python FastAPI（主）+ Node.js NestJS（辅）+ Go Gin（兜底）；SQLAlchemy 2.0 AsyncSession；JWT（HS256）+ RBAC 六角色 + RLS 行级隔离；Alembic 迁移。
  - 数据：PostgreSQL(JSONB)+pgvector / MongoDB / Redis / ClickHouse(OLAP) / Elasticsearch / Iceberg 数据湖。
  - 前端：Taro(React) 小程序 / React+Ant Design Pro 后台 / ECharts+DataV 看板 / 若依低代码。
  - AI：pandas/PySpark + scikit-learn/XGBoost/PyTorch + MLflow + LangChain/LlamaIndex(RAG) + PaddleOCR。
  - 部署：Docker + K8s + 等保三级。

---

## 三、P0–P3 完成度与验证证据

### 3.1 总体阶段状态

| 阶段 | 目标 | 状态 | 关键证据 |
| --- | --- | --- | --- |
| **P0 主链路** | backend 核心业务路由真实落地并验证 | ✅ 已完成并验证 | 路由 >50；`verify_p3` 全绿；端到端 httpx 验证 PASS |
| **P1 前后端联调** | admin-web / mp-taro 真实接口对接 | 🟢 部分完成 | admin-web 7 路由真实接后端；mp-taro 仍空壳；tsc 0 错误 |
| **P2 AI 与数据资产闭环** | RAG 可解释 / 反馈闭环 / CH 经营宽表 | ✅ 已完成 | ai-service RAG + `plat_model_pred_log` + `store_metrics` + Grafana |
| **P3 平台治理与生产就绪** | plat 真实 CRUD / RLS / 种子 / 合理用药 / 合规清单 | ✅ 已完成（代码层） | `plat/ai_models`、`plat/data_assets`、`store_scope`、8 端点注入、`seed.py`、`rx_engine`、合规清单 md+docx |

### 3.2 backend（🟢 最成熟，已端到端验证）

- **基础设施**：`core`（config / response / errors / security JWT+防重放 / deps）、`middleware`（request_id / idempotency / audit 骨架）、`db` 客户端、`models`（完整 `ih_/mt_/plat_` ORM）、`schemas`、`alembic`（0001 建全表、0003 风险画像、0004 model_id 可空、0005 rx_check_json）、Dockerfile。
- **业务路由（真实 CRUD，P0）**：
  - `ih`：users（微信 code2session 登录签发 JWT）/ doctors / orders+支付（占位预支付）/ prescriptions（开方→药师审核→approve/reject）/ **consultations 在线复诊（会话+图文消息）**。
  - `mt`：customers+授权 / pain_assessment / care_plans（对接 ai-service）/ treatment_records / stores/therapists / **repurchase_predictions 复购预测回写** / **risk_profiles 风险画像回写**。
  - `plat`：audit-logs 真实查询 + RBAC；ai_models / data_assets 真实 CRUD（P3）。
- **鉴权强制 + 审计真实化（P3 修复）**：全部业务路由注入 `current_user`/`require_role`；`write_audit` 的 `actor_id/role` 取自 JWT 真实主体；新增 `POST /auth/dev-token`（仅 debug 开启，生产 404）。机制级验证 9/9 PASS。
- **P1 复购/风险回写**：调用 ai-service `repurchase-prediction` v7 / `risk-profile` v5 真实推理并落库，AI 不可用优雅降级。
- **P2 ClickHouse 经营宽表（已完成）**：`services/store_metrics.py`（PG 按日预聚合写入 CH，幂等；CH 不可用仅告警降级）+ `mt/store-metrics` 接口 + Grafana 看板。注：PG 无金额/订单字段，`deal_amount/deal_orders` 暂置 0，待订单系统对接补齐。
- **P3 平台治理（已完成）**：① plat ai_models/data_assets 由占位升级为真实 CRUD（RBAC + 审计）；② RLS 行级隔离（`store_scope` + JWT `store_id` 声明 + dev-token 可签发带门店令牌，8 个 mt 端点注入）；③ `app/db/seed.py` 幂等种子（3 门店 + 调理师 + 预置 3 模型/4 资产）；④ 合理用药引擎 `services/rx_engine`（抽象层 + Mock 校验，开方前置校验降级不阻断，预留 HttpRxEngine 真实对接点）；⑤ 等保三级/执业许可合规清单（md+docx）。机制级验证 21/21 PASS。
- **jwt_secret 加固（已完成）**：默认值改 32 字节强随机串，`.env`/`.env.example` 同步，`InsecureKeyLengthWarning` 已消除（生产须以 `JWT_SECRET` 环境变量覆盖）。
- **admin-web 登录入口（已完成）**：登录页（dev 下 dev-token 按角色签发 JWT）+ `AuthRoute` 守卫 + 401 清理跳转。

### 3.3 admin-web（🟢 运营/门店后台，已接真实接口）

- `services/mt.ts` + `services/request.ts`（axios 封装、JWT 注入、401 清理）= 真实接口对接；`BasicLayout` + `AuthRoute` 守卫。
- 真实页面：customer（列表+授权）、repurchase（发起+历史）、risk（发起+历史）、data（门店经营看板：卡片+ProTable 接 `store-metrics`）、plat（审计日志 + AI 模型目录 + 数据资产目录两个 ProTable 列表页）、login（dev 下取令牌）。
- `npx tsc --noEmit` 0 错误。

### 3.4 mp-taro（🔴 仅页面骨架）

- 5 个占位页（index / login / doctor-consult / order / prescription），`wx.login` 仅 `console.log`，无真实业务流、无与后端联调证据。

### 3.5 ai-service（✅ 最成熟，已端到端验证）

- `model_loader.py`：MLflow 加载可用（已修 `torch` DLL 崩溃、`version` int→str 两处 bug）。
- 三个接口 **真实推理通过**：`plan-recommend` v8 / `repurchase-prediction` v7 / `risk-profile` v5，均带 `isAssist:true`。
- `train_sample.py`：训练并注册 3 模型到 Production 正常。
- **RAG 可解释（已完成）**：`plan-recommend` 接入 `app/rag/retriever.py`（内置疼痛/调理知识语料关键词检索，可无缝替换为 pgvector），`rationale` 引用真实知识条目。
- **AI 反馈闭环（已完成）**：业务回写落 `plat_model_pred_log(adopted=pending)` + `plat/model-pred-logs` 采纳/驳回接口（RBAC + 审计）；`model_id` 可空。

### 3.6 infra（🟡 编排较完整）

- ✅ `docker-compose.yml`：PG(+pgvector) / Redis / Mongo / ClickHouse / MinIO / Jaeger / Prometheus / Grafana 全编排。
- ✅ `k8s/helm`：namespace + deployment 基础模板；`pre-commit`、README 在。
- ⚠️ `nginx` 配置、`docker/postgres` 与 `clickhouse` 的 init SQL 多为占位。

---

## 四、对照 PRD V2.0 功能覆盖矩阵

### Part A：互联网医院主线

| PRD | 内容 | 实现情况 | 判定 |
| --- | --- | --- | --- |
| 3.1 用户端小程序 | 首页/找医生/找产品/复诊开药/个人中心 | `mp-taro` 仅 5 个空壳页，无真实业务流、无联调证据 | ⚠️ Partial（脚手架） |
| 3.2 医生端小程序 | 工作台/问诊聊天室/开方 | 同上，仅 doctor-consult 页面壳 | ⚠️ Partial（脚手架） |
| 3.3 医院管理后台 | 科室医生管理/处方审核配置/合作药房 | `ih/` 有 doctors/orders/prescriptions/users/consultations；缺科室结构、药房管理、药师排班审核配置 | ⚠️ Partial |

### Part B：健康数据中台主线

| PRD | 内容 | 实现情况 | 判定 |
| --- | --- | --- | --- |
| 3.4.1 客户健康档案 | 档案/疼痛评估/治疗记录/效果追踪/复购预测 | `mt/` customers、pain_assessment、care_plans、treatment_records、effect、repurchase、risk 后端齐全；admin-web 有 customer/repurchase/risk/data | ✅ 后端 + 部分前端 |
| 3.4.2 门店经营看板 | 到店率/转化/客单价/复购率/NPS/预警 | `store_metrics` 预聚合 + Grafana 看板 | ✅ |
| 3.4.3 调理师调配 | 排班/客户分配/能力标签 | `stores`/`therapists` 管理存在；缺排班、分配、能力标签 | ⚠️ Partial |
| 3.5 调理师工作台 | 我的客户/方案执行/治疗录入/依从性 | 无独立模块；仅后端 treatment_records/care_plans | ❌ Not Done |
| 3.6.1 合规大脑 | 采集审核/使用监控/输出审核/风险事件库 | 仅有审计日志 `plat_audit_logs` + RLS；缺采集审核、使用监控、输出审核、风险事件库 | ⚠️ Partial |
| 3.6.2 治疗数据沉淀 | 标准化/血缘/质量治理/资产目录 | `plat_data_asset`（资产目录+分级+血缘 JSON）已建模 | ✅ 建模 |
| 3.6.3 AI 辅助决策 | 风险画像/方案推荐/依从性/模型管理 | `ai-service`（plan/repurchase/risk）+ `plat_ai_model` + `pred_logs`（反馈闭环+可解释） | ✅ 后端 |
| 3.6.4 数据资产管理 | 估值/对外输出/审计月报 | 仅资产目录；缺估值、对外输出、月报 | ❌ Not Done |
| 3.7 平台监管看板 | 业务总览/异常预警/产品销售监管/数据资产看板 | 仅 `audit_logs` 列表；缺监管总览、异常预警、产品销售监管 | ⚠️ Partial |

### 非功能性需求（第 4 章）

| 项 | 状态 |
| --- | --- |
| 等保三级认证 | 已出测评清单（md+docx），**未测评** |
| 防注入/XSS/CSRF | 参数化查询 + JWT；WAF/CSRF 依赖部署，未验证 |
| 密码/敏感加密存储 | 脱敏展示已做；**落库强加密（bcrypt/KMS）未做** |
| 数据资产全量审计 | ✅ 审计日志 |
| 性能/压测 | 未做 |

---

## 五、对照技术架构设计方案 V1.2

| 架构章节 | 要求 | 实现 | 判定 |
| --- | --- | --- | --- |
| 10.2 RBAC 六角色 | JWT + 角色鉴权 | `core/deps.require_role`、`core/security` | ✅ |
| 11.2 plat 资产/模型目录 | 模型与资产 CRUD | P3 真实化 | ✅ |
| 13 数据分级 L1–L4 / 隐私计算 | 分级建模、联邦学习 | `sensitivity_level` 建模；隐私计算仅规划 | ⚠️ 建模完成，计算未落地 |
| 14 第三方对接（支付/短信/合理用药） | 三方可替换 | 合理用药 = 抽象层 + Mock；微信支付/短信 未做 | ⚠️ Partial |
| 15.2 RLS 行级隔离 | 按门店隔离 | `store_scope` + 8 端点注入 + JWT store_id | ✅ 代码级（未运行时验证） |
| 15.4 AI 可解释/反馈闭环 | 依据 + 采纳回写 | `ai-service/rag` + `pred_logs.adopted` | ✅ |
| 15.9 等保三级 | 测评 + 资质 | 清单已出；测评/资质未启动 | ⚠️ Partial |

---

## 六、对抗式审查风险缺口（可被推翻的证据缺口）

> 方法：默认每项"已完成"声明可疑，逐一验证或证伪。以下为可被推翻的缺口（影响上线判定）。

1. **RLS 仅代码级，未运行时验证**：`store_scope` 逻辑经 `verify_p3` 用模拟 payload 单测通过；但 `customer_ids_for_store` 子查询、各端点实际 SQL 过滤**未经真实 PG 执行验证**。store/therapist 角色强制绑定 store_id 的前提是 JWT 真的带 `store_id`，而 dev-token 的 store_id 在 seed 前无法跑通。
2. **种子数据从未执行**：`app/db/seed.py` 存在且幂等，但**未运行**；`MtStore` 无数据则 RLS 联调、经营看板、门店管理均无数据可查，演示/验收时"空转"。
3. **合理用药引擎是玩具 Mock**：`MockRxEngine` 仅 4 条相互作用规则；且 `prescriptions.create` 调用时 `patient` 传空 `{}`，孕期/过敏/日剂量信息未传入，禁忌与剂量告警几乎不触发，不满足处方安全合规要求。
4. **前端覆盖极窄**：admin-web 仅 7 个目录；PRD 的 3.5 调理师工作台、3.3 医院管理、3.7 平台监管**无前端**；小程序 `mp-taro` 仅 5 个空壳页。
5. **互联网医院主线不完整**：`ih/` 缺 3.3 科室/药房/药师审核配置；3.7 监管看板缺失；支付/短信第三方对接缺失。
6. **等保三级未测评，安全控制未闭环**：双因子认证、落库敏感字段加密（KMS）、审计日志防篡改（WORM）均未做；WAF/CSRF 依赖部署侧，未验证。
7. **性能与容灾未验证**：无压测；ClickHouse 仅在聚合层占位，未验证；AI 推理响应时间未测。
8. **AI 推理真实性未验证**：`ai-service` 注册 3 模型，但 repurchase/risk 是否真实训练产出、`pred_log` 是否真实落库，未经运行时验证（本环境无 PG/CH 运行时）。
9. **合规硬门槛（组织流程）全部未启动**：等保三级测评、医疗机构执业许可证、互联网医院准入、药品经营许可证、ICP 证、医师多点执业备案等，均为上线前置硬条件，当前仅出清单。

---

## 七、第一性原理结论

"上线"的本质 = **合规资质（可合法运营）+ 可运行系统（业务流闭环）+ 风险可控（安全/审计/隔离）**。

- **合规资质**：当前为 0（仅有清单），是阻塞上线的硬门槛。
- **可运行系统**：后端数据域骨架可达"内部可用"，但前台业务流（小程序）、医院/门店/调理师工作台、真实第三方对接缺失，离"对外运营"差一个数量级。
- **风险可控**：RBAC + RLS + 审计 + 可解释已就位（代码级），但缺双因子、落库加密、等保测评、运行时验证。

**判定**：P0–P3 完成的是"**平台治理与数据中台后端地基**"，不是"**可上线的互联网医疗产品**"。距上线仍有组织资质 + 业务前台 + 真实对接 + 验证四道大关。

---

## 八、下一步优先级计划（P3-B / P4）

按"阻塞上线 → 业务价值 → 验证"排序，建议里程碑与排期：

| 优先级 | 计划项 | 关键交付物 | 阻塞/价值 |
| --- | --- | --- | --- |
| **P0（最高，阻塞）** | 合规硬门槛 | 等保三级定级备案与测评排期；申办医疗机构执业许可证 + 互联网医院准入 + 药品经营许可证 + ICP + 医师多点执业备案 | 阻塞上线 |
| **P1** | 运行时验证 | `alembic upgrade head` + 跑 `seed.py`；用带 store_id 的 dev-token 真实联调 RLS；验证 pred_log / CH 写入 | 消除第六章风险 1/2/8 |
| **P2** | 真实第三方对接 | HttpRxEngine 接真实合理用药供应商；微信支付 / 短信对接（第 14 章）；开方 patient 信息真实传入 | 消除风险 3/5 |
| **P3** | 业务前台补位 | 小程序 2C/医生端真实业务流；调理师工作台（3.5）、医院管理（3.3）、平台监管看板（3.7） | 消除风险 4/5 |
| **P4** | 安全闭环 | 双因子认证、落库敏感字段加密（KMS）、审计防篡改（WORM） | 消除风险 6 |
| **P5** | 性能与质量 | 压测、ClickHouse 验证、AI 推理真实化验证、WAF/CSRF 部署验证 | 消除风险 7 |

### 排期建议（相对顺序，非绝对日期）

1. **立即启动（并行）**：合规资质申办 + 本地运行时验证环境搭建（起 infra、alembic、seed）。
2. **第 1 个迭代**：运行时验证闭环（RLS 真实联调、pred_log/CH 落库验证）→ 把"代码级完成"升级为"运行时验证完成"。
3. **第 2 个迭代**：真实第三方对接（合理用药供应商、微信支付/短信）→ 处方与交易链路合规可用。
4. **第 3–4 个迭代**：业务前台补位（小程序业务流、调理师/医院/监管前端）→ 达到"可对外运营"形态。
5. **并行贯穿**：安全闭环（双因子/加密/WORM）+ 性能质量（压测）→ 满足等保测评与上线硬指标。

---

## 九、验证记录（本次可复现）

- `code/backend/verify_p3.py`：**21/21 PASS**（编译、路由注册、store_scope 逻辑、rx_engine Mock、配置项、seed 结构）。
- `code/admin-web` `npx tsc --noEmit`：**0 错误**。
- 合规清单：`合规清单与等保三级准备工作.md` + `.docx`（pandoc 生成）。

> 本汇总所有"已完成"声明以上述代码树与机制级验证为证据；"未做/未验证"项已在第六、七节明确，供后续排期与验收追踪。
