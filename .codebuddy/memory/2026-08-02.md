# 2026-08-02 工作日志

## 短期五项闭环（用户："7/8/9 补测试 + 19 幂等 + 10 种子"），已提交 0278e9f 并 push

### 改动清单
1. **【19】幂等中间件真去重**：`app/middleware/idempotency.py` 从"仅透传校验"升级为**真实 Redis 去重**——写方法+带 `Idempotency-Key` 时先查 `idem:{method}:{path}:{key}`；命中返回缓存响应（`Idempotency-Replay: true`）；未命中执行业务并缓存**业务成功**（body.code==0）响应 TTL 24h；4xx/5xx 与业务失败（统一错误模型下 HTTP 200+code≠0）不缓存，允许重试。订单 `POST /orders`、`POST /{id}/pay` 等写接口因中间件全局注册自动受益。
2. **【10】P3 种子**：`app/db/seed.py` 新增 `_seed_p3_modules`，幂等补 演示药品/药房/库存(500/100)/科室/投诉(resolved)。`seed_all` 增加 summary 计数与打印。
3. **【7】P3 模块测试** `tests/test_p3_modules.py`（uvicorn+httpx）：pharmacies/departments CRUD（注意 update 是 PATCH 非 PUT）、complaints 全流程、dashboards 聚合结构（嵌套 core/compliance/warning/rx_trend_30d）。
4. **【8】XFF 单测** `tests/test_trusted_proxy.py`：空 cidr 不信任 / localhost 永不可信 / 命中 CIDR 信任 / 非法 CIDR 跳过 / 畸形 IP 不信任。
5. **【9】safe_jsonable 单测** `tests/test_safe_jsonable.py`：bytes 收敛 / 嵌套 / 不可序列化回退 str / 深度截断。

### 测试基础设施修复
- **根因**：`tests/conftest.py` 顶层 `from app.main import app` → 导入即建全局 Redis 异步连接池，pytest 主进程退出时 event loop 已关 → `'NoneType' object has no attribute 'send'` / `Event loop is closed`。
- **修复**：conftest 改为延迟导入 + session 级 `_close_redis` 在结束时 `redis_client.aclose()`；`test_rate_limit.py` 的 `_run` 开头 `await redis_client.connection_pool.disconnect()` 重置到当前 loop（修复跨 asyncio.run 复用已关闭 loop）。
- **结果**：`pytest tests/` 全量 **41 passed, 0 failed**（51s）。

### 关键约定（固化）
- backend 测试必须用「真实 uvicorn 子进程 + httpx」模式（test_health.py 风格），**禁用 TestClient**。
- 本项目的**统一错误模型**：业务失败也是 HTTP 200 + body.code≠0，因此判断"成功"须看 `body.code==0`，不能只看 HTTP 状态码（幂等缓存、错误识别均受影响）。
- P3 router 的 update 用 **PATCH**（非 PUT），前端/测试须对齐。
- dashboards 返回结构是**嵌套**（core/compliance/warning/rx_trend_30d），非扁平字段。
- 本地 Redis 残留 idem:* 缓存会污染幂等测试 → 测试 Key 必须唯一（时间戳+id），或跑前清 `idem:*`。

### 提交汇总（2026-08-01 ~ 08-02）
- 77ec515 fix(ih): DrugStockOut.is_low 缺省
- 5a0a6f7 refactor(ih): 库存 adjust delta 增减语义
- ad89a45 fix&feat: XFF绕过闭环 + 库存前端入口 + errors序列化bug + delta测试
- 0278e9f test&feat: P3全模块测试 + XFF单测 + safe_jsonable单测 + 幂等真去重 + 种子

## P3 计划 verify 阶段补全（56b99a3，已 push）

### 执行发现（原计划 stage1~4 + verify 的早期提交实际已在 0278e9f 前完成，但 verify 状态虚标，存在未跑通的 bug）
1. **后端致命 bug：ErrorCode 别名缺失**。27 处调用用了 `ErrorCode.NOT_FOUND` / `ErrorCode.BAD_REQUEST`，但 `errors.py` 仅定义 `RESOURCE_NOT_FOUND`(3004) / `PARAM_INVALID`(1001)。运行时命中错误分支即 `AttributeError` → 接口 500（应用能启动但错误接口全崩）。
   - **修复**：在 `ErrorCode` 补两个向后兼容别名 `NOT_FOUND=3004`、`BAD_REQUEST=1001`（保留正式语义名），零风险一次性修复全部误用。
2. **前端类型错误收敛**（让 `tsc --noEmit` 从 10 错误 → 0）：
   - `hospital.tsx:159` P3 引入：`createDepartment` 的 `head`/`remark` 为 null → 用 `?? undefined` 收敛。
   - `hospital.tsx:63/72` 历史遗留：`approveDoctor(r.id)`/`rejectDoctor(r.id)` 缺 `reviewer_id`（后端 `doctor.approve/reject` 基于 JWT actor 审计、不校验 reviewer_id 存在性）→ 补 `1` 占位透传。
   - `rx-review/index.tsx`：`Prescription` interface 缺 `items_json`（后端 IhPrescription.items_json 存在）→ 补 `items_json?: Record<string,unknown>`；`onChange` 隐式 any → 标注 `React.ChangeEvent<HTMLTextAreaElement>`。
   - `schedules/index.tsx`：未 import `Tag` → 补 `Tag`。

### 验证结果（本次 verify 真实执行）
- 后端：`pytest tests/` **41 passed, 0 failed**；`python -c import app.api.v1.ih` → 54 路由注册成功。
- 前端：`npx tsc --noEmit` → **EXIT:0**（零类型错误）。
- 迁移：`alembic current` → head=0015（P3 四表已建）；`alembic upgrade head` 可跑通。
- DB：本地 PG `postgresql://ihm:ihm_dev_pwd@localhost:5432/ihm` 可达，ih_* 表齐全。

### 提交
- 56b99a3 fix&feat: verify P3 链路修复 ErrorCode.NOT_FOUND/BAD_REQUEST 别名误用 + 前端类型错误收敛（push 0278e9f→56b99a3）

## 状态自检与后续任务梳理（08-02，分析型任务，无代码改动）

### 交付物（新建/修订）
- 新建 `互联网医疗中心平台_状态自检与后续任务_2026-08-02.md`（主报告：A 第一性原理自检 / B 对抗式审查 / C 文档vs工程对账 / D 状态报告 / E 分级任务）
- 修订 `互联网医疗中心平台整体进度与下一步计划.md`（追加第十章 2026-08-02 自检更新）
- 修订 `互联网医疗中心平台工程进度.md`（追加第七章 2026-08-02 自检更新）

### 核心发现（事实偏差 + 对抗式硬伤）
- **文档严重低估工程**：双因子 TOTP、审计哈希链、密码 PBKDF2、字段级 KMS 加密、医生/药师登录、P3 四模块全链路——P0-P3自检报告(07-26) 把它们标"未做/风险"，实际均已落地（已代码/运行时验证）。
- **对抗式硬伤 H1-H6**（须并入 P0/P1）：
  - H1 RLS 缺口：`risk_profiles.py` 两端点未注入 scope（任意 store 读全量）；`effect_tracking.py` 注入未用于 WHERE（死参数）；`scheduling.py:40` 手工调用参数语义错；**无多租户越权集成测试**（注入≠生效）。
  - H2 DEV 建档后门：`app/api/v1/ih/users.py:65-70` 当 role=doctor 无关联档案时**无条件**自动建 `IhDoctor(license_no="DEV-DOC-{user.id}")` 并签 doctor JWT，**不受 APP_ENV 守卫** → 生产任意用户可获医师身份（合规红线）。
  - H3 支付未生产化：`orders.py` `pay_order` 仅 dev 沙箱模拟 prepay_id；`/wxpay/notify` 骨架完整（RSA 验签+解密+幂等）但无真实商户号/证书联调。
  - H4 合理用药 4 规则 Mock（`rx_engine/mock.py` 仅 4 条相互作用）；`HttpRxEngine` 预留未接。
  - H5 种子未实证（`seed.py` 存在未跑）。
  - H6 `IhDoctor.dept` 仍 String 未外键化。

### 后续任务分级（详见主报告 E 章）
- P0：关 DEV 后门(H2) + 补 RLS 缺口+越权测试(H1) + 合规资质申办 + 支付生产化(H3)
- P1：种子实证(H5) + 密钥生产化(云KMS) + 合理用药真引擎(H4) + dept 外键化(H6)
- P2：CH 验证 + 审计脱敏对齐
- P3：小程序 S2-S8 闭环（药师审核真调 audit、支付真调起、mt 域接入）

### 关键代码证据位置
- users.py:65-70（DEV-DOC 后门）、orders.py:91-149(pay_order 沙箱)/160+(notify 验签)
- mt/risk_profiles.py:59-68（无 scope）、mt/effect_tracking.py:26-94（scope 死参数）、mt/scheduling.py:40（参数语义错）
- rx_engine/mock.py:18-26（4 条规则）

## P0 硬伤 H1/H2 修复完成（51b9603，已 push 56b99a3→51b9603）

### H2 关闭 DEV 建档后门（合规红线）
- `core/config.py` 新增 `allow_dev_auto_profile: bool = False`（参照 `wxpay_dev_sandbox` 风格，注释标注生产严禁置 True）。
- `ih/users.py` `_resolve_subject`：doctor / pharmacist 两个分支的自动建档均改为受该开关守卫，关闭时抛 `BusinessError(ErrorCode.FORBIDDEN, "账号无执业医师/药师档案，请先完成资质认证")`。保留 dev 联调便利，生产默认拒绝。

### H1 补齐 mt 域 RLS（注入 → 真正生效）
- `mt/risk_profiles.py`：两端点补 `scope: int | None = Depends(store_scope)`；list 用 `customer_ids_for_store(scope)` 过滤；POST 前查 `MtCustomer.source_store_id == scope` 校验归属，不符 FORBIDDEN。
- `mt/effect_tracking.py`：原 scope 为死参数，现补相同的读过滤 + 写侧归属校验；补 `customer_ids_for_store` / `MtCustomer` / `BusinessError,ErrorCode` import。
- `mt/scheduling.py`：`_load_therapist(db, therapist_id, user)` 内部误调 `store_scope(user, db)`（第二实参 AsyncSession 被当 scope_store_id → platform 角色恒误 403）。重构签名为 `(db, therapist_id, scope)`，由路由层 `Depends(store_scope)` 注入，**7 处**调用点同步修正。
- 顺带修复：`risk_profiles.py` 调用 `create_pred_log` 却漏 import（POST 必 NameError→500，此前从未被测到）；`repurchase_predictions.py` 清理 customer_id 条件重复 append。

### 新增 tests/test_rls_isolation.py（5 用例全过）
- 4 个无依赖必跑静态断言：11 个端点签名含 scope；`_load_therapist` 签名为 (db,therapist_id,scope) 且体内无 `store_scope(`；`allow_dev_auto_profile` 默认 False；`_resolve_subject` 两分支均受守卫。
- 1 个端到端双门店越权用例（种子 994010/994011 两店 + 994001/994002 两客户 + 994020/994021 两调理师）：跨店写被拒、跨店读为空、**显式传他店 customer_id 也不能绕过 scope**、跨店调理师排班被拒、本店操作不被误伤、platform 不下钻见全量 / 下钻后仅见该店。

### 验证
- `pytest` 全量 **46 passed**（41 原有 + 5 新增），e2e 真实执行未 skip（-v 确认）。
- `alembic current` = **0015 (head)**，无表结构变更。
- 全后端 read_lints 0 错误。

### 过程教训（重要）
- **replace_in_file 在重复代码块上会静默匹配到错误位置**：本次 `scheduling.py` 7 个路由的 `user: dict = Depends(require_role(...))\n    db: ... \n):\n    await _load_therapist(db, therapist_id, user)` 片段高度雷同，多次编辑落到了非目标函数上；`effect_tracking.py` 的写侧校验也曾整段未生效。**必须用 grep 复核每处改动落点**，不能假定 edit 成功。
- 新增测试的价值即时兑现：首轮 e2e 就直接暴露 effect_tracking 写侧校验漏改（A 店成功为 B 店客户写入），否则会误判修复完成。

## P1 工程收口：H4 合理用药真引擎 / H5 种子实证 / H6 dept 外键化（08a9a87，已 push 56b99a3→08a9a87）

### H4 合理用药真引擎（替代仅 4 条的 mock）
- 新增 `app/services/rx_engine/local.py`：`LocalRxEngine` + 内置临床知识库 `DEFAULT_KB`（相互作用 10 对 / 禁忌 7 条 / 重复用药 2 / 特殊人群 7 / 剂量上限 3 药），覆盖孕期、肝功能不全、肾功能不全、老年、儿童、哺乳期；支持外部 JSON（rx_engine_kb_path）覆盖；返回 `RxResult` 带 `level/rule_source=local_kb/suggestions`。
- `base.py` 的 `RxResult` 扩展 `duplicate_warnings / special_population / suggestions / rule_source / level`（保留旧字段，向后兼容调用方 `model_dump`）。
- `__init__.py` 工厂：`http > local > mock`；`config.py` `rx_engine_provider` 默认改 **local**（原 mock），新增可选 `rx_engine_kb_path`。
- `mock.py` 标注为降级基准（保留）；`http.py` 契约不变。`.env.example` 配置说明更新。
- 新增 `tests/test_rx_engine.py`（11 用例）：相互作用/禁忌/重复/特殊人群/剂量/降级/clean，纯逻辑必跑。

### H5 种子实证
- 新增 `app/db/verify_seed.py`（只读核对）：11 张关键表非空 + platform_admin 双因子（two_factor_enabled + totp_secret + password_hash）；支持 `--json` / `--strict`；GBK 终端改用 ASCII 标记。
- 新增 `tests/test_seed_smoke.py`：端到端（真实 PG 可达才跑，不可达 skipif）。**关键修复**：`seed_all()` 与 `verify()` 必须合并在**单次 `asyncio.run`** 内，否则 asyncpg 共享 engine 跨事件循环报 `'NoneType' has no attribute 'send'`（被 asyncpg 噪音掩盖真实 traceback）；且测试内幂等重跑 `seed_all()`，与 `test_data_asset` 的 `drop_all` 解耦。
- 实测：本地 PG 可达，种子已落地，platform_admin 双因子完整。

### H6 医师科室外键化
- `app/models/ih_models.py`：`IhDoctor` 加 `dept_id: FK(ih_department.id) nullable` + 索引，保留 `dept` 字符串作冗余过渡。
- 新增迁移 `alembic/versions/0016_doctor_dept_fk.py`：`add_column` + 按 `dept` 名回填 `dept_id`（未匹配置 NULL，不强制约束）+ `create_index` + `create_foreign_key`；幂等（列存在则跳过）。**迁移实测**：0015→0016 成功；因种子医生 `dept` 全为 NULL，回填 0 条（符合预期安全行为）。
- `schemas/ih.py`：`DoctorCreate` 加 `dept_id`；`DoctorOut` 加 `dept_id` + `dept_name`。
- `doctors.py`：列表支持 `dept_id` 过滤 + `_attach_dept_names` 批量联表填 `dept_name`；创建时传 `dept_id` 则回写 `dept` 字符串（取科室名）。5 处返回点统一填充 `dept_name`。
- `departments.py`：软删科室前校验是否仍被医师引用（`ref_count>0` 则拒绝），避免脏数据。
- 前端 `ih.ts`：`Doctor` 加 `dept_id?/dept_name?`，`listDoctors` 支持 `dept_id`；`hospital.tsx` DoctorTab 科室列改 `dept_name` 展示 + 加科室筛选下拉（ProFormSelect 从 `listDepartments` 加载）。`npx tsc --noEmit` EXIT:0。

### 验证结果（本次真实执行）
- 后端：`pytest` 全量 **58 passed**（原 46 + H4 11 + H5 1），无回归。
- 迁移：`alembic upgrade head` → head=**0016**（dept_id 列 + FK + 索引已建）。
- 前端：`npx tsc --noEmit` → 0 错误。
- read_lints：后端 0 错误。

### 提交
- 08a9a87 P1工程收口: H4合理用药本地真引擎 / H5种子实证 / H6医师科室外键化（16 文件，+1000/-29），已 push origin/main。

### 临时文件清理说明
- 本次调试在 backend/ 下生成的 _out.txt/_tail.txt 等临时输出文件已删除（仅本地日志，不影响数据与代码）。
- 用户拒绝删除 `code/admin-web/tsc_out.txt`、`report_orig.xlsx`、`verify_report.txt`、`push_out.txt`（历史/疑似用户文件），故保留。
- 后续若用户同意，可清理这些 root 级临时文件。

### 遗留 / 未动（非本次范围）
- H3 支付生产化、P2（CH 验证 / 审计脱敏）、P3 小程序 S2-S8 闭环仍未做。
- 种子医生 dept 为 NULL（种子未填科室），新创建医师才会带 dept_id；如需历史关联可后续在 seed 里补科室名。

## P2/P3 全量收口（57eccf8，已 push 08a9a87→57eccf8）

### 本次范围（用户确认：全量收口 + 落库前脱敏）：P2-CH验证 / P2-审计脱敏 / H3支付生产化 / S8 mt小程序闭环

### P2-CH 验证（端到端打通）
- `app/db/clickhouse_client.py` 新增 `ensure_store_metrics_table()`：建表 DDL + 列对齐补齐（历史旧表缺 store_name 等列，CREATE IF NOT EXISTS 跳过 → 改为 DESCRIBE 后 ALTER ADD COLUMN 逐列补齐）。新增 `ch_available()` 探测。
- 临时脚本实测：建表+列补齐成功 → 直写测试行 → 查询读回 11 列正确 → 清理。本地 docker `ihm-clickhouse` 长期 healthy。
- 新增 `tests/test_clickhouse_store_metrics.py`（CH 可达时跑、不可达 skip；测试内自清理）。**注意**：测试只验证 CH 写入/查询链路（ensure_table + 直写 + 按列名读回），不调 `aggregate_store_metrics`（它依赖共享 PG async engine，asyncio.run 跨事件循环会触发 asyncpg 'NoneType' has no attribute 'send' 陷阱）；PG 聚合链路由 store_metrics 单测覆盖。

### P2-审计脱敏（落库前脱敏，等保三级）
- 新增 `app/utils/mask.py`：`mask_json(obj)` 递归遍历 dict/list，对敏感键（id_card/phone/real_name/name/email/address/bank_card 及中文变体）及值兜底（身份证/手机/邮箱正则）掩码；规则：身份证前6后4、手机前3后4、姓名首字+*、邮箱首字+***@域名。
- `app/services/audit.py` 的 `write_audit`：落库前对 before/after 调 `mask_json`，哈希链基于脱敏后内容计算 → `verify_audit_chain` 仍自洽。91+ 调用点签名不变（仅内部增强）。
- 新增 `tests/test_audit_mask.py`：脱敏正确 + 哈希链校验通过 + 原对象不被修改。

### H3 支付生产化（真实下单 + 缺凭证安全降级）
- `app/api/v1/ih/orders.py` 的 `pay_order` 已通过 `create_prepay()` 按 `settings.wxpay_dev_sandbox` 路由沙箱/生产（`_production_prepay` 已实现 RSA-SHA256 JSAPI 签名）。本次补：生产分支失败（缺商户凭证/网关异常）时 `RuntimeError` → 捕获为 `BusinessError(PARAM_INVALID)`，**不崩主链路**（避免 500），返回统一错误模型。补 `import logging`。
- `/wxpay/notify` 验签路径（wxpay_verify.py）此前已就绪，本次确认无遗留。
- 新增 `tests/test_wxpay_production.py`：沙箱 prepay_id 前缀 = `sbx_prepay_`（非 WXPREPAY_SANDBOX_）；生产+缺凭证抛 RuntimeError；pay_order 降级为 BusinessError。**注意**：`create_prepay` 是 `@lru_cache` 装饰、有 4 个 keyword-only 参数（out_trade_no/amount/description/openid），返回 `PrepayResult`（属性访问非下标）。

### S8 mt 小程序闭环（用 code-explorer 子代理核对契约差异）
- 子代理输出两处 P0 阻断 + 多项合规缺口。本次聚焦 S8 已注册 7 页闭环 + 阻断级合规：
  - **§A 合规强规则1**：`care_plans.py` 创建时校验 `doctor_advice_id>0`，否则 `BusinessError(PARAM_INVALID)`（禁止以 0 冒充医师建议落库）。前端 `plans/index.tsx` 改弹窗提示（移除原 `doctor_advice_id:0` 伪值）。
  - **§B 守卫**：`plans/index.tsx` / `pain/index.tsx` / `records/index.tsx` 的 `submit` 对 `customerId` 无效时 toast 返回（原静默吞错传 NaN）。
  - **§C 真实详情**：`mt.ts` 补 `getCustomer`；`customers/detail.tsx` 改为真实拉取（移除伪造 `{id, auth_status:'authorized'}` 掩盖未授权）。
  - **§D RLS 归属**：`mt.ts` 补 `listStores`；`customers/index.tsx` 建客户传 `source_store_id`（默认取列表首个门店，否则 NULL 致 RLS 全失效）。`api.ts` 补 `stores`/`storeMetrics` 常量。
  - **§F 合规强规则2（更正留痕）**：`mt.ts` 补 `reviseTreatmentRecord`；`records/index.tsx` 增历史列表 + 更正弹窗（记录不可删，仅可更正留痕）。
- mp-taro `npx tsc --noEmit` → EXIT:0（修复 records storeId number→string 类型）。

### 验证结果（本次真实执行）
- 后端：`pytest` 全量 **64 passed, 2 skipped**（CH/沙箱相关 skip 因环境），无回归。
- 前端 mp-taro：`npx tsc --noEmit` → 0 错误。
- read_lints 后端 0 错误。

### 提交
- 57eccf8 P2/P3 全量收口：CH建表验证、审计落库前脱敏、H3支付生产化降级、mt小程序S8闭环（15 文件，+590/-44），已 push origin/main。

### 用户偏好更新（2026-08-02）
- **临时文件清理**：用户明确「验证后不需要的临时文件可直接删除，不用每次授权」。故本次调试生成的 _ch_verify_tmp.py / pytest_*.txt / tsc_mt*.txt 跑完即删，不再询问。

### 遗留（非本次，后续阶段）
- 微信支付真实商户凭证（mch_id/私钥/api_v3_key/平台证书）缺失时仅安全降级，未发真实请求（需运营配置后再集成联调）。
- 历史种子医生 dept 仍为 NULL。

## 收口 effect/scheduling 小程序前端入口（已提交并 push）

### 根因
- 后端 `effect_tracking`(`/mt/effect-tracking`)、`scheduling.schedule_router`(`/mt/therapists/{id}/schedules`)、`scheduling.tag_router`(`/mt/therapist-tags` + `/mt/therapists/{id}/tags`) 均已注册且路由正确，但**小程序 mp-taro 完全没有这两组的页面入口**：app.config.ts 未注册、services/mt.ts 缺 scheduling/tag 封装（effect 封装字段与真实 schema 不符）、constants/api.ts 缺路径、客户详情页无跳转按钮。
- admin-web 后台已含 scheduling 与 tag 的真实入口（src/routes/store + services/mt 的 schedule/tag），无需补。

### 改动（code/mp-taro）
- `constants/api.ts`：新增 `therapistSchedules:'/mt/therapists'`、`therapistTags:'/mt/therapist-tags'`（对齐后端真实前缀；废弃原误设的 `/mt/schedules`、`/mt/tags`）。
- `services/mt.ts`：重写/新增与后端真实契约一致的方法——`listEffectTracking`/`createEffectTracking`（customer_id + plan_id/baseline_pain/latest_pain/nps/repurchase_count/effect_level）；`listSchedules`/`createSchedule`(therapist维度，body 含 work_date/am_pm/start_time/end_time/capacity)；`listTags`/`createTag`(仅目录，platform 角色)；`assignTag`/`unassignTag`(分配给/解绑调理师，DELETE 非删目录)；`Therapist` 补 `name?` 字段。
- `pages/mt/effect/index.tsx`（新建）：效果四档列表+创建页，customer_id 守卫。
- `pages/mt/scheduling/index.tsx`（新建）：选门店→选调理师→建排班 + 标签目录/分配给当前调理师/解绑。
- `app.config.ts`：注册 `pages/mt/effect/index`、`pages/mt/scheduling/index`。
- `pages/mt/customers/detail.tsx`：客户详情增加「效果四档」「排班与标签」两个导航按钮。

### 验证
- mp-taro `npx tsc --noEmit` → EXIT:0（0 类型错误）。
- 后端路由核对：`mt/__init__.py` 已 include effect_tracking.router / scheduling.schedule_router / scheduling.tag_router，契约一致。

### 提交
- 本次 commit（本地 + push origin/main）：feat(mp-taro): 补齐 effect/scheduling 小程序前端入口。

### 注意（真实契约要点，避免再次错配）
- 排班是**按调理师**维度（`/mt/therapists/{id}/schedules`），无扁平 `/mt/schedules`；创建必传 `therapist_id/work_date/am_pm/start_time/end_time`。
- 标签目录与分配分离：创建目录 `POST /mt/therapist-tags`（仅 platform/xingyao）；分配是 `POST /mt/therapists/{id}/tags {tag_id}`；解绑是 `DELETE /mt/therapists/{id}/tags/{tag_id}`（**不删目录**）。
- effect 真实字段是 `plan_id + baseline_pain/latest_pain/nps/repurchase_count + effect_level(significant|effective|ineffective|worsened)`，非 pain_before/after。
