# 2026-07-26

## P6 前端接通完成（前端 P6 标注缺口切换为真实后端接口）
在 P0 等保测评+执业许可（组织流程）完成后，将前端原 P6 标注缺口切换为真实后端 P6 接口。

### 已交付
- **小程序医生排班** `mp-taro/src/pages/doctor-schedule/index.tsx`：localStorage → `GET/POST/DELETE /ih/schedules`。周视图网格(周一..周日 × 上午/下午/晚上) 经"生成未来 4 周具体日期记录 / 加载时按 weekday+am_pm 聚合"接通。`services/ih.ts` 新增 IhDrug/listDrugs/getDrug 与 DoctorSchedule 系列函数；`constants/api.ts` 加 `doctorSchedules`/`drugs`。
- **小程序药品目录页** `mp-taro/src/pages/drug-catalog/index.tsx`（新建）+ 在处方页加"药品目录"入口；`app.config.ts` 注册路由。
- **后台门店页** `admin-web/src/routes/store/index.tsx`：localStorage → `/mt/therapists/{id}/schedules` 与 `/mt/therapists/{id}/tags`；"客户分配"保留本地。`services/mt.ts` 新增排班/标签函数；`constants/api.ts` 加 `therapistTags`。
- **合规采集审核（新建模块）** `admin-web/src/routes/plat/compliance-review/index.tsx` + `services/plat.ts` 新增 submit/list/approve/reject；`App.tsx`/`BasicLayout.tsx` 注册路由与菜单。
- **药品目录管理页（新建，platform）** `admin-web/src/routes/ih/drug-catalog/index.tsx` + `services/ih.ts` 新增 listDrugs/createDrug/updateDrug/deleteDrug；注册路由菜单。

### 关键避坑（重要）
- ⚠️ **误覆盖风险**：`admin-web/src/services/ih.ts` 是**未跟踪(untracked)存量文件**（含 listDoctors/approveDoctor/rejectDoctor/listPrescriptions/listConsultations/listOrders/auditPrescription 等），被我最初用 write_to_file 覆盖，导致 hospital/operations/ih-supervision/sales-supervision 四个页面编译报 TS2305。已据上游页面与后端路由**重建**该文件（保留原函数+追加药品目录函数）。**教训：写新 service/文件前务必先 read 确认是否已存在，勿整文件覆盖已有模块。**
- 后端 `am_pm` 取值为 `morning/afternoon/evening`（非中文），前端需映射。
- 排班类后端按**具体 work_date** 存储，前端周视图需生成未来日期记录再聚合渲染。
- 角色门控：`localStorage.getItem('role')` 读取（platform/xingyao 可审核；platform 管药品）。

### 验证
- admin-web：`npx tsc --noEmit` 与 `npm run build` 均通过（exit 0）。
- mp-taro：`npx tsc --noEmit` 通过；`taro build`（weapp/h5）本环境因缺 `@tarojs/webpack5-runner` 等打包插件无法跑（环境问题，非代码问题）。

## 交付《互联网医疗中心平台工程进度》报告（面向负责人）
- 文件：`互联网医疗中心平台工程进度.md` + `.docx`（pandoc --toc 生成，exit 0），位于工作区根目录。
- 结构：总体状态 → 已完成（含本次 P6 接通小结）→ 未完成（P0–P5 优先级表）→ 下一步里程碑 M0–M4 → 难点与需配合解决事项（5 项，重点：合规资质阻塞、运行时环境、第三方供应商、前台人力、安全闭环）。
- 核心结论：研发底座已成形（21/21 PASS + P6 前端接通），但上线就绪度未就绪，最大阻塞是合规资质（等保测评+执业许可）组织流程，需负责人/公司层面推动；P6 接口尚未运行时联调（缺 PG/Redis 运行时）。

## 代码提交与推送 GitHub
- 用户要求提交所有更新文件。实际状态：269 个 P6/文档文件已在既有提交 `bf4f086`（266 files changed）入库；本次新增提交 `ddd5c2a`（2 files: `generate_report.py` + `互联网医疗中心平台_项目状态报告.xlsx`）。
- `git push origin main` 成功：`0e4ffb1..ddd5c2a  main -> main`（SSH 远程 `git@github.com:ai-junshu/InternetHospital.git`）。
- 坑：git status 中 `互联网医疗中心平台工程进度.xlsx` 是 Excel 临时锁文件 `~$...xlsx` 关联的**残留引用**，磁盘上不存在（Test-Path=False），`git add` 该名会报 pathspec 不匹配；真实文件名为 `互联网医疗中心平台_项目状态报告.xlsx`。`~$` 锁文件未提交。
- 教训：提交前用 `git status --short` + 实际 `Get-ChildItem`/Test-Path 核对文件名，避免 pathspec 失败导致整条 `git add` 中止。

## 新增仓库根目录 README
- 仓库原无根 README（各子模块 backend/ai-service/infra 已有独立 README）。新建 `README.md`：项目定位（双主线 IH/MT/PLAT）、技术栈、仓库结构、快速开始（infra→backend→ai-service→前端）、项目状态、合规说明、分支模型。
- README 只出 `.md`（GitHub 直接渲染），不出 `.docx`。
- 提交 `dc71e97`，已 push：`ddd5c2a..dc71e97 main -> main`。
