# 2026-07-31 工作记录

## 本周实现冲刺（基于 07-26 计划 + 用户"立即开始"指令）
约束：合规资质已就绪；微信支付（商户号待开）、第三方供应商（下周确认）暂未就绪 → 前端/后台开发不依赖二者，本次只做不依赖外部的部分。

### 已完成
1. **契约断裂修复（client/backend 对齐）**
   - B1 后端补 `POST /ih/doctors/{id}/approve|reject`（新增 `DoctorAuditIn` schema，auth=platform）
   - B2 前端 `submitCompliance` 路径改 `/plat/compliance/submit`
   - B3 处方状态枚举 `pending` → `pending_audit`（operations.tsx / ih-supervision.tsx 颜色映射+筛选+判断）
   - R5 审核 `reviewer_id` 写死 1 → 取 `localStorage.uid`（登录时存），operations/ih-supervision 两处

2. **admin-web 新增 5 页**（接真实端点）
   - routes/ih/patients（GET /ih/users）、orders（GET /ih/orders + mock 支付）、consultations（GET /ih/consultations）、schedules（GET /ih/schedules）、rx-review（待审列表+详情抽屉+rx_check_json 告警）
   - 补 service：listPatients/getPatient/payOrder/listSchedules/getConsultation/endConsultation；api.ts 加 ihUsers/ihSchedules
   - App.tsx 路由 + BasicLayout 菜单注册

3. **plat-crud**：ai-models 上下线/删除、data-assets 新增/编辑/删除（ModalForm）；service 补 setModelOnline/Offline/deleteModel/createDataAsset/updateDataAsset/deleteDataAsset

4. **mp-taro 主链路**
   - 新建 doctor-rx-create（POST /ih/prescriptions）、prescription-detail（GET /ih/prescriptions/{id}）
   - order 页"申请开药"→ 凭处方下单（无 prescription_id 弹窗引导开方）
   - doctor-consult 加"结束问诊并开方"入口；app.config.ts 注册两新页

5. **R4 后端（处方药凭方合规）**：`OrderCreate` 加 `prescription_id`（rx 必传）、`IhOrder` 加列、`create_order` 校验；小程序 createOrder 支持该字段

6. **运行时 M1**
   - 移出 `code/backend/.env` 出版本库（.gitignore 加 `code/backend/.env`）+ 建 `.env.example`
   - 新建 `infra/scripts/bootstrap.ps1`（docker compose → 等PG健康 → alembic upgrade → seed → uvicorn 后台）
   - `docker/prometheus/prometheus.yml` + compose 挂载

### 校验结果
- 后端 py_compile 通过
- admin-web `tsc --noEmit` 0 错
- mp-taro `tsc --noEmit` 0 错（修 3 个真实类型错误：prescription-detail 缺 Button 导入 + unknown 类型）
- ⚠️ mp-taro `taro build` 在 Node v24 下崩溃：`@tarojs/helper/src/npm.ts` 内部 `require(undefined)`（Taro3.x 与 Node24 不兼容，非代码错误）。建议在 Node 18/20 环境打包。

### 未做（留给后续/外部就绪后）
- 微信支付真实接入（商户号待开）、第三方供应商（合理用药/短信，下周确认）
- R6 药师角色后端 RBAC 大改（本次未强制，建议排期）
- B4/B5 医院科室/药房目前 frontend 用本地 DEMO，后端无端点（跨端缺口，待后端补）
- 真联调（M1 环境起后需实际操作验证，本次仅静态校验通过）
