# 2026-08-03 工作日志

## xlsx 项目状态报告升级 V1.1 → V1.2
- 用户确认：将 08-02 全天收口成果同步进 `互联网医疗中心平台_项目状态报告.xlsx`，bump 版本号。
- 用 openpyxl 一次性定点修订（保留样式/合并区域），未破坏格式。
- 修订点：
  - 项目概况仪表板：B3 日期/版本→2026-08-03/V1.2；E21/F21 95%→100%；C29 85%→92%（补 effect/scheduling 入口）；D37 调理师工作台补"后端API已具备"；A13 总体结论追加 08-02 收口段。
  - 本周完成事项：周期改 07-27~08-02；insert_rows(31,9) 追加 #26-#34 收口项；合计行改 34 项。
  - 下周计划：G9/G10/G11（运行时验证）→已完成；G12 HttpRx→本地引擎已落地/真实供应商待接；G13 支付→降级已落地/真实商户待接；G19/G20/G21（双因子/KMS/审计）→部分完成(已落地)。
  - 风险登记簿：F6 RLS→已验证；F7 种子→已验证；F8 合理用药→已缓解(local真引擎)；F9 前端→部分缓解(mp-taro 92%)；F11 安全合规→部分完成(双因子/KMS/审计已落地；测评/资质待启动)。
  - 技术架构对照：C7 合理用药 Mock→LocalRxEngine本地真引擎；C10 等保补双因子/KMS/审计已落地。
- 临时脚本 _patch_xlsx.py 与 6 个 _xlsx_*.txt 探查文件均已删除。
- 仍未做（保留未启动）：P0 合规资质(等保测评/执业许可/药品经营/ICP/多点执业)、真实第三方(微信支付商户/HttpRx供应商/短信/patient真实信息)、调理师工作台/医院管理/监管看板前端、云 KMS 生产化、P5 压测/CH性能/AI响应/WAF-CSRF 验证。

## Git 提交与推送
- commit `4bc1d53`（main）：21 files changed, 1738 insertions(+), 554 deletions(-)，已 push 至 `origin/main`（github.com:ai-junshu/InternetHospital.git，5f4600a..4bc1d53）。
- 提交内容：xlsx V1.2 升级、08-02 状态自检报告 md 新增、工程进度与整体进度计划 md 同步、.codebuddy plans（11 份）与 memory 日志（08-01/02/03）归档、删除一次性脚本 `generate_report.py`。

## 调理师工作台前端（mp-taro，新增）
- 新增 `code/mp-taro/src/pages/mt/workbench/index.tsx`：调理师工作台聚合首页。
  - 身份选择/切换（Storage key `therapistId`，门店→调理师两级 Picker，复用 doctor-workbench 范式）。
  - 今日排班（listSchedules 按 work_date==today 前端过滤）、我的客户（listCustomers 全量，卡片进 detail）、快捷录入宫格（8 入口，URL 复用 detail 既有模式）。
- `app.config.ts` 路由：确认 9 个 mt 页面原本已登记（customers/index、customers/detail、pain、plans、records、repurchase、risk、effect、scheduling）；新增登记 `pages/mt/workbench/index`。
- 复用 `services/mt.ts` 现有导出（listStores/listTherapists/listSchedules/listCustomers），未新增 API。
- 验证：`tsc --noEmit` exit 0，无 lint 错误；跳转 URL 与 detail/customers 既有模式一致。
- 未提交（工作区改动，待用户决定是否 commit）。

## 用户偏好补充
- 计划模式（plan mode）下用户会先确认 plan 再让我执行；本次"接受计划并立即开始"后我直接改文件。
- xlsx 是面向管理层/对外的仪表板，应反映最新真实状态（与 md 自检报告对齐），避免"严重低估/失准"。
