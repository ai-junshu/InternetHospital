# 2026-08-01 工作日志

## M1 真联调验证（curl + dev-token 实测，已授权）

### 联调发现的真实 bug（已修复并 commit ae30db9）
1. **ih_order.prescription_id 列缺失（致命）**：sprint 在 `IhOrder` 模型加了 `prescription_id` 列，但 alembic 0010 迁移只建了 P6 新表、漏了该列。导致 R4「处方药带处方下单」`POST /api/v1/ih/orders` 返回 5000 系统异常（ORM 写不存在的列）。
   - 修复：恢复 0010 为纯 P6 建表；新建 **0011_ih_order_prescription_id.py** 迁移补列（幂等 exists 检查）；库已 `alembic upgrade head` 到 0011。
   - 教训：模型层加列必须同步迁移，且已应用过的迁移 upgrade 不会再跑，补列应新建迁移而非改旧迁移。
2. **OrderOut 未回显 prescription_id**：前端/小程序拿不到关联处方 id。给 `app/schemas/ih.py` 的 `OrderOut` 加 `prescription_id`；mp-taro `Order` 接口同步。

### 7 条核心链路实测全通过（以 localhost:8000/openapi.json 为事实源）
- GET /ih/users(total 8)、/ih/consultations(2)、/ih/schedules(1) ✓
- POST /ih/doctors/{id}/approve → status=approved（B1）✓
- POST /plat/compliance/submit → 需 category/subject_type/title（B2）✓
- 开方 POST /ih/prescriptions → pending_audit；GET ?status=pending_audit 可拉；PATCH /{id}/audit → approved，审计表 actor_id=当前 JWT sub（非写死1，R5 正确）（B3）✓
- POST /ih/orders type=rx 无 prescription_id → 业务码1001拦截；带 → 200 且回显 prescription_id=1（R4）✓

### 环境注意
- PowerShell 传中文 JSON 给 curl 会 GBK 乱码报 JSON 解析错误；改用 `--data-binary "@file.json"`（文件 utf-8）可避坑。
- 后端 uvicorn 在 127.0.0.1:8000 运行，DEBUG=true；改动后若 --reload 不生效，需手动停 python 进程重启。
- `alembic revision` 命令在本机 PowerShell 下报错（疑似编码），直接用 write_to_file 手写迁移文件更稳。

### 提交
- ae30db9：9 文件 +81/-23，已 push 到 origin/main。

## 工作区整理 + 小程序进度盘点与下一步安排（commit f714bd1）

### 整理动作
- 删除历史诊断 `code/backend/alembic_log.txt`（修复前失败日志）。
- `.gitignore` 补充 Office 锁文件：`~$*.docx` / `~$*.xlsx` / `~$*.pptx`（避免再误跟踪）。
- 修正 `code/infra/scripts/smoke_test.ps1`：R4 断言由"期望 HTTP 422"改为"业务码 1001"（后端全局异常处理器统一返回 HTTP 200，判据是 body.code）；自包含开方→审核→下单闭环数据；加 `/health` 前置探活、失败计数与 `exit $fail`。可作 M1 回归基线。

### 小程序真实进度（code-explorer 盘点，13 页）
- 13 页全接真实后端接口，无任何 mock 数据；`services/ih.ts` 封装 20 函数对应后端 7 个 ih router。
- 已闭环：医生端主链路(~75% 状态机驱动)、患者端处方→下单→支付骨架(~60%)、只读查询(~80%)。
- **关键断点**：①医生身份体系缺失（最大风险，login_wx 仅签 patient，医生页 403/401）②支付未真调起（后端 pay_order 占位）③药师审核无小程序入口（后端 audit 已就绪但无页/未封装）④药品目录与开方未联动⑤处方列表未按患者过滤（越权）⑥mt 中台域未接。
- 结论：已过"接口全通"，处"业务半闭环"，距可上线差医生/患者身份分离、支付对接、审核工作台三块。

### 下一步排期（写入文档八-B节，S1-S8）
- 迭代A：S1 双身份登录 + S2 药师审核工作台 + S3 处方按患者过滤（打通最小可演示闭环）
- 迭代B（并行P2）：S4 药品联动+合规入参 + S5 微信支付真调起
- 迭代C：S6 问诊轮询/UI + S7 首页真实数据/tabBar
- 迭代D：S8 mt 中台域接入
- P0 合规资质（等保三级/执业许可/药品经营许可）并行启动，阻塞上线。

### 文档更新
- `互联网医疗中心平台整体进度与下一步计划.md`：3.4 mp-taro 由"5空壳页"纠正为"13页接真实接口"；P1 状态→已完成(M1)；PRD 3.1/3.2 判定校准；风险4校准；P3 小程序任务细化；新增八-B节小程序专项。

### 提交
- f714bd1：7 文件 +603/-7，已 push 到 origin/main（含 .gitignore/文档/smoke/memory/plans）。
- 未处理（非本次范围）：`项目状态报告.xlsx`（二进制，按约定不碰）；`~$...docx` 锁文件已取消跟踪+gitignore 忽略。

## 迭代 A · S1 双身份登录 + S2 药师审核工作台（commit 8ce50fd）

### 完成内容（14 文件 +528/-30，已 push 到 origin/main）
- **后端模型**：新增 `IhPharmacist` 实体（user_id/license_no/title/pharmacy_id/status，与 IhDoctor 同构）；新建迁移 `0012_ih_pharmacist.py`（幂等建表，已 `alembic upgrade head`）；`app/db/seed.py` 补 2 条药师种子。
- **鉴权 S1**：`deps.ROLES` 加 pharmacist（六→七角色）；`auth.py` MT_ROLES 补 doctor/pharmacist（修复 dev_token 选 doctor 登录 400 的既有 bug）；`users.login_wx` 支持 `role` 参数（patient/doctor/pharmacist），按身份关联 IhDoctor/IhPharmacist 签发 JWT（sub 取档案 id），返回 `user.role`；`schemas.UserOut` 加 role、`UserLoginWxIn` 加 role/doctor_id/pharmacist_id。
- **审核 S2**：`prescriptions.audit_prescription` 权限由 doctor/platform 改为 **pharmacist/platform**；`reviewer_id` 改取 `actor_of(_user)`（JWT 主体，审计链责任到人，防伪造）；`PrescriptionAuditIn.reviewer_id` 降级为可选（前端不再强传）。
- **前端**：`services/ih.ts` 加 role/pharmacist_id/auditPrescription；登录页三态身份选择并 `reLaunch` 按 role 路由（患者→index / 医师→doctor-workbench / 药师→pharmacist-review）；新增药师审核工作台页 `pages/pharmacist-review/index.tsx`（待审列表/详情/通过/驳回，复用处方卡片样式）+ 注册路由。

### 联调验证（统一用 Python urllib 驱动，避 curl 编码坑；7 条全 PASS）
- dev-token pharmacist 白名单 ✓；login_wx role=pharmacist→role=pharmacist ✓；默认→patient ✓
- 药师列 pending_audit ✓；药师审核→pharmacist_id=JWT sub(1) ✓；医生越权审核→2003 角色无权 ✓
- 前端 `tsc --noEmit` 通过。

### 重要经验
- **curl 报 5000 的假象**：`errors.py` 的 `_val` 把 `RequestValidationError.errors()`（含 bytes 的 input 字段）直接 `model_dump()` 序列化 → 触发 5000。当 curl 未显式 `Content-Type: application/json` 时请求被当 form 解析触发校验错误，进而暴露此既有 bug。**真联调统一用 Python urllib 传 JSON 最稳**（dev-token 用 urllib 正常，用 curl `--data-binary @file` 无 Content-Type 时 5000）。该 errors.py 序列化 bug 属既有技术债，本次未修（超出 S1/S2 范围），后续可单独处理。
- `--reload` 对 schemas 等文件有时不生效，改 schema 后务必手动重启 uvicorn 再验证。

## 迭代 A · S3 处方越权修复 + S4 药品联动 + S5 微信支付骨架（commit 1d47255）

### 完成内容（11 文件 +457/-28，已 push 到 origin/main）
- **S3 越权修复**（`app/api/v1/ih/prescriptions.py`）：`list_prescriptions`/`get_prescription` 按 role 归属过滤——patient 仅 `patient_id==sub`、doctor 仅 `doctor_id==sub`、pharmacist/platform 全量（`patient_id` query 仅 platform 可用）；越权返回 2003 FORBIDDEN。
- **S4 药品联动**：后端 `create_prescription` 按 `items[].drug_id` 反查 `IhDrug` 标准化 items_json（name/spec/otc_type/unit/price，覆盖前端手填）；前端开方页新增「从药品目录选药」入口（`drug-catalog?mode=picker` 经 Storage 回传回填 drug_id/spec/otc_type/price）；`PrescriptionItem` schema 加 otc_type/unit/price 可选字段。
- **S5 微信支付骨架**：`config.py` 加 wxpay_mch_id/v3key/cert_serial/notify_base_url/appid/dev_sandbox（dev 占位、生产走 env）；新增 `app/services/wxpay.py`（统一下单+JSAPI 签名，dev 沙箱返回模拟 prepay_id）、`app/core/wxpay_verify.py`（回调验签+AES-GCM 解密骨架，dev 沙箱跳过真验签）；`orders.pay_order` 改预支付状态机（trade_state=pending 返回 prepay 参数，不再直落 paid）；新增 `/orders/wxpay/notify`（微信回调，幂等更新）与 `/orders/{id}/pay/mock-success`（dev 模拟支付闭环）；`OrderPayIn` 加 description/openid；前端 order 页接 `Taro.requestPayment` 调起，amount 由处方药价汇总。

### 联调验证（Python urllib 驱动，6 项全 PASS）
- S4：listDrugs 有数据；开方 drug_id=6 反查标准化成功（otc_type/price 存在）。
- S3：patient A 列表仅见 patient_id={1}；patient B 看 A 处方详情 → 2003 越权拦截。
- S5：创建 rx 订单 amount=2000（药品价汇总）；pay_order 返回 sbx_prepay_... 且 pay_status=pending；mock-success → paid + trade_state=SUCCESS。
- 前端 `tsc --noEmit` 通过。

### 重要经验 / 技术债
- **errors.py 序列化 bug 仍存**（S1/S2 已记）：curl 无 Content-Type 触发 RequestValidationError → `_val` 序列化 bytes 失败 → 假 5000。联调一律用 Python urllib 传 JSON。
- **S5 仅交付沙箱+骨架**：真实微信支付需商户凭证（mch_id/APIv3 key/平台证书），未接真实 HTTP 调用与 RSA/AES-GCM 实现（NotImplementedError 占位）。生产前需补 `wxpay.py._production_prepay` 与 `wxpay_verify` 真验签，并禁用 dev_sandbox。
- **Taro.requestPayment** 类型与小程序标准字段略有差异，order 页传参用 `as any` 容错（沙箱无商户必然走 catch→mock-success）。
- dev 环境遗留联调造的处方/订单数据（id 9+ 等），与 M1/S1/S2 一致。

## 迭代 C · S6 问诊链路 + S7 首页整合（前端为主，含 3 个后端真实 bug 修复）

### 计划范围
- S6：患者侧问诊闭环（发起/查看/双向消息/实时轮询/结束）+ 医师端 doctor-chat 加轮询。
- S7：首页接真实医生列表+我的问诊入口+公告banner占位+加载态；profile 接 getMe 真实角色；tabBar(首页/复诊开药/我的)+全局登录守卫。

### 前端改动（mp-taro/src，tsc --noEmit 0 错误）
- `services/ih.ts`：新增 `listPatientConsultations(GET /ih/consultations?patient_id=)`、`getMe(GET /ih/users/me)`；`constants/api.ts` 补 `usersMe`。
- 新建 `pages/patient-consult-list/index.tsx`（我的问诊列表+发起复诊选医生弹层）与 `pages/patient-chat/index.tsx`（气泡UI+轮询+结束态）。
- `doctor-chat/index.tsx`：加 `setInterval` 4s 轮询 listMessages，onUnload 清定时器（复用 getConsultation 判断 ended 停轮询）。
- `index/index.tsx`：接 getDoctors 渲染推荐医生；服务入口加"我的问诊"指向 patient-consult-list；banner 本地静态占位（后端无公告域，预留 getAnnouncements 接口位）；骨架态。
- `profile/index.tsx`：onShow 调 getMe 取真实 role 展示身份标识，本地 user 兜底。
- `app.config.ts`：注册 patient-consult-list/patient-chat；加 tabBar 三 tab（纯文字）。`app.tsx`：useLaunch 校验 token 缺失则 reLaunch 登录页（全局守卫）。

### 后端真实 bug 修复（app/api/v1/ih/consultations.py，未单独 commit）
1. **end_consultation 契约冲突**：原强制 `doctor_id` 查询参数，患者端无法结束会话。改为 `doctor_id | patient_id` 均可选，归属任一匹配即可结束（双端可结束）。
2. **get_consultation 缺越权防护（真实安全漏洞）**：单条查询未做 role 归属校验，任意登录用户凭 id 可查他人会话。补 patient/doctor 归属校验，越权 → 2003 FORBIDDEN。同时 list_consultations 对 patient/doctor 角色强制归属（忽略外部传入的越权 id）。
3. **错误码误用 PERMISSION_DENIED**：该枚举不存在（AttributeError → 全局 5000），改为 `ErrorCode.FORBIDDEN`(2003)。这是越权"未生效"假象的根因。

### 联调验证（Python urllib 驱动，全 PASS）
- S7：GET /ih/doctors 有数据(首页推荐) ✓；GET /ih/users/me → {user_id, role:patient} ✓
- S6：患者 login→createConsultation(open) ✓；listPatientConsultations 含该会话 ✓；医师 start→双向 sendMessage ✓；listMessages 轮询拉到 2 条 ✓；PATCH /end(patient_id=) → status=ended ✓
- 越权：患者B GET 患者A会话 → **code 2003 非本人问诊会话** ✓（修复真实漏洞）

### 经验 / 注意
- 后端 consultations 的 start/end 是 **PATCH** 方法且 doctor_id/patient_id 走 **query 参数**（非 body），前端封装已是 PATCH+query，联调务必一致。
- errors.py 序列化 bug 仍在（技术债）：`PERMISSION_DENIED` 无效枚举触发 5000 暴露了此问题；本次已规避用 FORBIDDEN，但 errors.py 本身的 bytes 序列化 bug 仍未修，后续可单列处理。
- 改后端后需手动停 uvicorn 进程重启（--reload 对部分改动不生效）。
- 未提交 commit（用户未要求），改动留在工作区。

## 迭代C P0 收尾（本次）· 已修复并本地改动（未提交）

### 范围确认
- 迭代C 收尾 = **P0 必做**；健康数据中台 = **存量盘点 + 缺口清单（不写新业务代码）**。
- 此前迭代A/C/报告均已 push（1d47255 / ecd49fb / c398ebc）→ 本次在已 push 代码上做 P0 收尾。

### 1. 修复 end_consultation 越权绕过（security 关键）
- `code/backend/app/api/v1/ih/consultations.py`：原 end_consultation 用查询参数 `doctor_id`/`patient_id` 做归属校验，不传参时两 if 均短路 → **任意登录用户可结束任意会话**。改为取 JWT 主体 `actor_of(_auth)` + role 判定（patient/doctor/platform），与 get_consultation 越权写法一致。
- 顺带修一处既有缩进 bug：`if obj.status != "ongoing"` 把后续 status 设置块吞成子句导致永不执行（患者 end 返回 code 0 但 status 仍 ongoing），已拆出独立执行。
- write_audit 误用旧变量 `_user` → 改为 `_auth`（原 _user 在本函数已不存在，会 NameError）。

### 2. 前端传参修复
- `mp-taro/src/services/ih.ts`：endConsultation 去掉 doctorId 参数，URL 不再拼 `?doctor_id=`。
- `mp-taro/src/pages/patient-chat/index.tsx`：调用改为 `endConsultation(id)`（原误传 patientId 当 doctor_id → 患者结束恒 2003）。
- `mp-taro/src/pages/doctor-chat/index.tsx`：同步改为 `endConsultation(id)`（避免 TS 签名不匹配）。

### 3. 新增测试 tests/test_consultations.py
- 无依赖必跑：路由注册检查 + end_consultation 不再暴露 doctor_id/patient_id 参数检查。
- 端到端（DB 可达时跑，不可达自动 skip）：覆盖 归属过滤 / 2003 越权读取 / 双端结束（本人可、他人 2003、结束后不可再结束）。复用 test_p6 的 ASGI+token 模式 + DB skip。
- 本地 pytest 全绿（EXIT=0）。

### 4. 健康数据中台存量盘点（产出文档）
- 文档：`互联网医疗中心平台_数据中台盘点与缺口清单.md`
- 核心结论：mt 主线"骨架已立、血肉不足"。9 路由 + 12 表 + RLS + admin-web 壳已落地，但 PRD V2.0 **三条合规强规则全未完整落库**：
  - 规则1 方案关联医师ID：`care_plans.py:34` 把 `doctor_advice_id=created_by`（调理师自身）当医师建议ID，**语义错位**且字段可空无 FK 无强制。
  - 规则2 治疗记录留痕更正：`treatment_records.py` 无 DELETE 端点（侥幸）+ 无 PATCH 更正 + write_audit 未传 before，留痕更正缺失。
  - 规则3 效果四档判定：仅 `mt_effect_tracking` 空表，无路由/service/算法。
- AI 特征工程 `ai-service/app/ml/feature_store.py` 全占位（build_features 返回 {}）；Iceberg/Milvus/Spark/Flink 在 docker-compose 未编排（仅 pgvector/ClickHouse 已落地）。数据资产无血缘编排。
- 优先级：P0 三条合规强规则 → P1 数据资产闭环（挂钩融资估值）→ P2 AI 能力 → P3 基础设施编排。

### 待办
- 上述后端+前端+测试改动**尚未 commit/push**（用户指示"接受计划并立即开始"但本次会话未要求提交，按既往"可推送"默认，需确认是否提交）。
- 报告中迭代C 状态（mp-taro 85% / 已上线）未因本次 P0 收尾更新，如提交可考虑同步报告或下期统一更新。

## 提交与 P0 合规强规则实施（追加）
- 已提交并推送 `c3853be`：迭代C P0 收尾（end_consultation 越权 + 前端传参 + 测试）+ 数据中台盘点文档。
- 用户："可以，先提交，提交后，按照优先级建议，逐步完成。" → 实施 P0 三条合规强规则。

### P0 三条已落地并推送 `f52f0e9`（c3853be..f52f0e9）
- 规则1 方案关联医师ID：`schemas/mt.py` CarePlanCreate.doctor_advice_id 改必填；`mt_models.py` MtCarePlan.doctor_advice_id 改非空；`care_plans.py` 移除 `=created_by` 语义错位。
- 规则2 治疗记录留痕更正：新增 MtTreatmentRecordRevision 修订表；`treatment_records.py` 新增 PATCH /{record_id} 更正接口（白名单 + 写修订表 + write_audit 补 before）；无 DELETE（显式不可删）。
- 规则3 效果四档判定：`services/effect_service.py` 已实现 judge_effect_level + build_effect_tracking（阈值：显效Δ≥4/NPS≥8/复购≥2；有效Δ=2-3或NPS=7；无效Δ=0-1或NPS≤5；恶化Δ<0或NPS≤3）；新增 `effect_tracking.py` 路由（POST 判定落库 + 无效/恶化触发 next_action=recommend_upgrade_or_consult，GET 列表）并注册至 `mt/__init__.py`。
- 测试：`tests/test_mt_compliance.py` 覆盖三条规则，pytest 全绿。
- 文档三条规则标 ✅ 已落地，总体结论 P0 划除。
- 未纳入提交：报告 xlsx 本地改动、plan 文件、临时调试文件（用户既往偏好保留）。

### 下一步（剩余优先级）
- P1 数据资产闭环（Iceberg 资产表 + 血缘采集，挂钩融资估值）—— 最大缺口。
- P2 AI 特征工程 feature_store.py 空占位 → 复购/风险/方案推荐。
- P3 基础设施：Milvus/Spark/Flink 编排。

## P1/P2/P3 全部实施完成（追加，已推送 b1b9648）
- 用户："继续，按照剩余优先级全部执行" → 接受计划、实施 P1→P2→P3。
- 提交 `b1b9648`（f52f0e9..b1b9648，17 文件 +891/-15），已 push origin/main。

### P1 数据资产闭环
- `plat_models.py`：PlatDataAsset 增 lifecycle_status/data_volume/valuation_json；新增 PlatDataLineage 血缘表。
- `data_assets.py` 增补 5 端点：GET/POST /{id}/lineage、GET /{id}/valuation（质量分×数据量×敏感度系数，L1=1/L2=1.5/L3=2.5/L4=4）、POST /{id}/lifecycle（状态机 collected→cleaned→stored→analyzed→output→archived→destroyed + 审计）、GET /export（脱敏，不含 lineage/valuation 明细）、POST /{id}/collect（运行时聚合回写）。
- 注意：`GET /export` 必须定义在 `/{asset_id}` 之前，否则被模板路由抢匹配（FastAPI 顺序匹配）。
- `services/data_asset_collector.py`：按资产名匹配采集器（"治疗效果数据集"→聚合客户/记录/显效率），seed 静态→运行时动态。
- 测试 `tests/test_data_asset.py` 全绿（DB 不可达自动 skip；测试内 drop_all+create_all 重建以应用新列）。

### P2 AI 特征工程
- `ai-service/app/ml/feature_store.py`：`build_features(customer_id, raw)` 真实聚合（治疗频次/依从率/NPS/效果四档/画像标签 + risk_flags/risk_score，空数据信号门控→risk_score=0）。
- 三个推理入口（plan_recommend/risk_profile/repurchase_prediction）改为调用 build_features 取标准化特征驱动模型（原始不出域、联邦取数语义）。
- PlanRecommendReq 新增 nps/effect_level/treatment_count/adherent_count 字段。
- 测试 `ai-service/tests/test_feature_store.py` 全绿。

### P3 基础设施编排（骨架）
- `docker-compose.yml` 增补 iceberg-rest（REST Catalog + 复用 minio）+ milvus standalone（etcd + storage + milvus），含 healthcheck；Spark/Flink 注释模板。
- `config.py` 增补 iceberg_rest_uri/milvus_uri 等；新增惰性容错客户端 `app/db/iceberg_client.py`/`milvus_client.py`（依赖未装时 import 容错返回 None，不影响启动）；`pyproject.toml` 增补 optional 依赖 datalake(pyiceberg)/vector(pymilvus)。
- 注意：客户端用标准 logging（app/core/logging.py 不存在），勿引不存在模块。

### 关键经验
- pytest 在 PowerShell 无 tail/sort；用 `> file.txt 2>&1; echo EXIT=$LASTEXITCODE` 捕获，再 read_file 看结果。
- 测试 DB 改动模型列后需 drop_all 重建（create_all 不改已有表）。
- 计划文件散落在 .codebuddy/plans/ 且文件名含中文/括号，git 用通配易失配，建议按文件路径直加或排除。

### 未纳入提交（用户既往偏好保留）
- 报告 xlsx 本地改动、.codebuddy/plans/*.md、根目录临时调试文件（build_report.py/verify*.txt/等）。

## 深化三任务：采集器配置化 + P3 真实建表 + 模型接入 MLflow registry（本次，待 commit）

用户指令："采集器配置化、P3 真实建表/建集合、模型接入 MLflow registry"。三项均已完成实施，测试全绿，待提交推送。

### A. 采集器配置化
- 新增 `services/collector_config.py`：声明式采集器注册表 `COLLECTOR_REGISTRY`（treatment_outcome/customer_profile/plan_library/store_metrics 四型 + 聚合口径）+ `DEFAULT_RULES` 名称关键字推断 + `resolve_collector_type`/`list_collectors`。新增资产只需声明 `collector_type` 或加一条关键字规则，**无需改采集器源码**。
- 改造 `services/data_asset_collector.py`：`collect_asset_metrics(db, asset)` 接收资产对象，按 `collector_type` 路由到通用聚合器（`_aggregate` 按类型算 data_volume/quality_score，表缺失容忍），回写后返回 `{collector_type, aggregation, data_volume, quality_score}`。
- `data_assets.py`：`POST /{id}/collect` 改传资产对象并直接返回 metrics（含采集器类型）；新增 `GET /collectors` 清单端点（类型/口径/各资产命中类型，透明化运营）。**注意 `/collectors` 须定义在 `/{asset_id}` 之前**（否则被模板路由抢匹配，与 P1 的 `/export` 同坑）。顺带修了原 list_collectors 路由内 `from ... import list_collectors` 遮蔽自身导致的递归 bug（重命名为 `list_collector_specs`）。

### B. P3 真实建表/建集合
- `db/iceberg_client.py`：`ensure_asset_table()` 用 pyiceberg 真实建 `ihm.plat_data_asset` 表（12 列 schema，与估值字段对齐），幂等；`get_status()` 探测可达性。
- `db/milvus_client.py`：`ensure_health_vector_collection()` 真实建 768 维（健康画像 embedding，与 ai-service 对齐）IVF_FLAT/COSINE 集合，幂等；`get_status()` 探测。
- 二者仍为惰性（pyiceberg/pymilvus 缺失时安全降级 available=False，**不影响启动**）。
- `config.py` 增补 `iceberg_namespace="ihm"`/`milvus_token`/`milvus_dimension=768`。
- 新增 `api/v1/plat/lake_init.py`：`GET /data-lake/status`（探测）+ `POST /data-lake/init`（真实建表+建集合，仅 platform/xingyao，审计）；注册至 `plat/__init__.py`。

### C. 模型接入 MLflow registry
- 新增 `ai-service/app/ml/feature_contract.py`：**集中定义三类模型特征列契约**（RISK/PLAN/REPURCHASE 各列名+顺序）+ `build_frame(model_name, records)`（缺失列补0、多余列丢弃，保证与训练一致）。这是修复"registry 模型加载列错位"的关键。
- `ai-service/app/ml/feature_store.py`：新增 `to_model_features(customer_id, raw, model_name)` 把 build_features 输出映射到契约列。
- 三推理入口（risk_profile/plan_recommend/repurchase_prediction）统一改用 `to_model_features`+`build_frame`，列与训练严格一致。
- `model_loader.py`：`load_model` 用 `search_model_versions` 取最新版本（替代已废弃 `get_latest_versions(stages=)`）；新增 `list_registry()`（元数据枚举，不触发实际加载）。
- `app/core/config.py`：`mlflow_tracking_uri` 默认改 `sqlite:///tracking.db`（与训练脚本同源，开箱即用 registry 闭环，无需 server；生产用 env 覆盖）。
- 新增 `api/model_registry.py`：`GET /models/registry`（版本/可达性）+ `GET /models/health`（探活）；注册至 main.py。
- `scripts/train_sample.py`：重写为用 `feature_contract.build_frame` 训练三模型并注册（列与推理一致），`search_model_versions` 取最新 transition 到 Production；修复 `np.clip(...).values` bug。
- 新增 `tests/test_registry.py`：验证契约列对齐 + 路由注册。

### 测试与验证
- backend `pytest tests/test_data_asset.py` → 2 passed（路由检查 + 采集器配置化 e2e）。
- ai-service `pytest tests/` → 9 passed（含原失败 repurchase 测试因重训对齐列而修复）。
- 关键修复：registry 中存在旧列训练的模型导致推理列不匹配 → 重训脚本用新契约列重新注册覆盖 → 推理一致。

### 关键经验（本次新增）
- MLflow registry 加载 sklearn 模型对列名+顺序严格校验，推理 DataFrame 必须与训练时**逐一对应**（用 feature_contract 单点契约约束两端）。
- `get_latest_versions(stages=)` 在 mlflow 2.9+ 废弃，改用 `search_model_versions(f"name='{m}'")` 取最新版本。
- FastAPI 静态字面路由（`/collectors`）必须放在路径参数路由（`/{id}`）之前，否则被抢匹配。
- 路由函数内 `from x import list_collectors` 若与函数同名会遮蔽自身→递归 RecursionError，需用别名。

## 运行时真实验证（第一步收尾，消除进度文档第六章风险 1/2/8）

用户："按照建议顺序执行" → 第一步（内部可立即做：运行时验证）。计划 `8db2e4f9...`，已 finished。

### 关键校正
- 此前清单"补 0008 加密迁移"是**误判**：经核查 `0009_encrypt_secret_and_health_tags.py` 早已落地，DB `alembic_version=0012`（最新 head），`totp_secret`/`health_tags` 加密改造已生效。无需补迁移。

### 执行结果（5 项全 PASS）
1. **seed 幂等注入**：`python -m app.db.seed` 成功 → 3 门店 / 5 调理师 / 3 模型 / 4 资产 / 4 账号（platform_admin 双因子，otpauth_uri 已输出）/ 2 药师。库已是 v0012，直接跑安全。
2. **backend 启动真实验证**：`uvicorn app.main:app --host 127.0.0.1 --port 8000` 起服务，`/health` 返回 redis=ok + postgres=ok（本机 Redis 也在跑，非自动放行路径）。
3. **应用层 RLS 跨门店隔离**：构造 store_id=1 JWT 调 `GET /api/v1/mt/customers` → 仅见本门店客户（越权行=[]）；store1 访问 store2 客户 → **业务码 2003 FORBIDDEN**（data=null）。
4. **落库加密**：直连 PG 核验 `plat_account.totp_secret` 与 `mt_customer.health_tags` 均为 KMS 信封密文（`eyJ2IjogMSwg...` base64 JSON 含 v/kn/wd/dn/ct），非明文 base32 / `{"pain":...}`。
5. **清理**：停 backend 进程、删临时脚本、回滚验证造的测试客户（清空 mt_customer 表，仅测试数据，seed 正式数据完好保留）。

### 重要架构事实（验证中确证）
- **后端统一用业务码表达错误**：全局异常处理器返回 **HTTP 200 + body.code ≠ 0**（如 FORBIDDEN=2003、越权/角色无权=2003），而非 HTTP 4xx。验证脚本据此断言 code==2003，而非 HTTP 403。
- **RLS 是应用层 store_id 过滤**（非 DB 原生 CREATE POLICY）：`deps.store_scope` 从 JWT 取 store_id 注入查询条件（customers.py / treatment_records.py / stores.py）。
- **KMS 信封加密**：`app/core/kms.py` encrypt 返回 base64(JSON{v,kn,wd,dn,ct})；`encrypted_field.py` 的 EncryptedString/EncryptedJSON 在 ORM 层透明加解密。

### 后续阶段（未执行，用户建议顺序的后续）
- P0 资质：等保三级/执业许可/药品经营许可/ICP/医师多点执业备案（需用户组织，非代码）。
- S8 小程序 mt 中台域接入（13 页未接中台主线）；3.3 医院管理后台 / 3.6 监管看板 / 调理师工作台（PRD 缺口）。
- 微信支付生产化（wxpay.py 真验签，当前沙箱占位）；合理用药真引擎（HttpRxEngine，当前 MockRxEngine）。

## Phase1 收尾 + Phase2 微信支付生产骨架（本次，待 commit）

### Phase1 收尾（用户授权批量停止+删除）
- 停 backend(8000, PID 24520) 与 Mock 供应商(9099, PID 17740)；删临时脚本 `_dbg.py`/`_verify_rx.py`/`scripts/mock_rx_provider.py`。
- `.env` 的 `RX_ENGINE_PROVIDER` 由 http(指向已删9099) 复位为 `mock`（联调已完成，避免残留指向停服）。

### Phase2 微信支付生产骨架（已实施，未端到端验证——无商户凭证）
- `config.py`：新增 `wxpay_private_key_path`(商户私钥pem路径) 与 `wxpay_cert_cache_dir`(默认 `{backend_root}/.cache/wxpay_certs`)。
- `app/services/wxpay.py`：`_production_prepay` 由 NotImplementedError 补全为真实实现——
  - `_rsa_sign`：商户私钥对消息做 RSA-SHA256 + Base64；
  - `_auth_header`：按 `METHOD\nURL\nTS\nNONCE\nBODY\n` 构造 `Authorization: WECHATPAY2-SHA256-RSA2048`；
  - 真实 `httpx.post /v3/pay/transactions/jsapi`，解析 prepay_id；
  - 按 JSAPI 规则用私钥对 `appId/timeStamp/nonceStr/package` 做 paySign。
  - 缺凭证时抛 RuntimeError（不实际发请求），沙箱路径不变。
- `app/core/wxpay_verify.py`：补全 `decrypt_resource`(cryptography AES-256-GCM) + `verify_notify`(生产用 `PlatformCertStore` 按 serial 取平台证书公钥 RSA 验签) + `PlatformCertStore`(GET /v3/certificates 下载、APIv3 key 解密、按 serial 文件+内存缓存) + `cert_decrypt_to_pem`。
- `.env.example`：补 WXPAY_MCH_ID/API_V3_KEY/CERT_SERIAL/APPID/PRIVATE_KEY_PATH/CERT_CACHE_DIR/NOTIFY_BASE_URL/DEV_SANDBOX + 合理用药引擎说明；`wxpay_dev_sandbox` 默认 True 不变，生产置 False。
- 验证（静态）：沙箱 prepay/verify_notify 正常；生产缺 appid 正确抛 RuntimeError；`tsc` 不适用（纯后端）；`cryptography 46.0.6` 已装。
- 注意：`PlatformCertStore._download_certs` 用商户私钥对 GET /v3/certificates 签名（复用 wxpay._auth_header），证书轮换自动按 serial 获取。

### Phase1/2 之后待办（计划剩余）
- S8 小程序 mt 中台域前端（services/mt.ts + pages/mt/*）；
- 医院管理后台 ih/* 页面核查补全（code-explorer 定位 gaps）。

## Phase3 S8 小程序中台域前端（本次，待 commit）

- 新建 `mp-taro/src/services/mt.ts`：封装 customers/painAssessments/carePlans/treatmentRecords/effectTracking/repurchasePredictions/riskProfiles 的列表/详情/创建/预测接口，对齐 ih.ts 风格（request+API+PageResult）。
- 补 `constants/api.ts` 的 mt 完整路径（含 effect/repurchase/risk）。
- 新建 6 类页面（app.config.ts 已注册路由）：
  - `pages/mt/customers/index.tsx`（客户列表+新增+去授权，调 authorizeCustomer）
  - `pages/mt/customers/detail.tsx`（统计入口：评估/计划/记录）
  - `pages/mt/pain/index.tsx`（NRS 疼痛评估录入）
  - `pages/mt/plans/index.tsx`（照护计划列表+新建）
  - `pages/mt/records/index.tsx`（治疗记录录入，含 NPS）
  - `pages/mt/repurchase/index.tsx`（触发 AI 复购预测+列表）
  - `pages/mt/risk/index.tsx`（触发 AI 风险画像+列表）
- `app.scss` 追加 mt 共享样式（mt-page/card/btn/form/tag/stat 等）；`tsc --noEmit` 无 mt 相关类型错误。
- 后端 mt 模块齐全（customers/pain_assessment/care_plans/treatment_records/effect_tracking/repurchase_predictions/risk_profiles），前端首次打通中台域主线。

## Phase4 医院管理后台 ih 核查与前端补全（本次，待 commit）

- code-explorer 核查：后端 ih 实际 7 模块 = users/doctors/prescriptions/orders/consultations/schedules/drugs（与计划原命名不符，已按实际）。
- 已修复的纯前端可修缺口（P0/P1/P2，未动后端）：
  1. `services/ih.ts`：`IhPatient` 字段改为后端 `UserOut` 真实脱敏命名（real_name_mask/phone_mask/id_card_mask/role/user_type）；补 `createSchedule/updateSchedule/deleteSchedule/getSchedule`；`Order` 补 `prescription_id`。
  2. `rx-review/index.tsx`：按后端真实 `rx_check_json` 结构 `{conflicts,contraindications,dosage_warnings}` 判定告警（原判 .hit/.warnings/.risk_level 永为通过=合规风险）；展示 `items_json` 处方明细；审核 `note` 真实提交（驳回必填，移除硬编码'不合规'）。
  3. `patients/index.tsx`：字段映射对齐脱敏命名（原 nickname/phone/gender/age 全空）。
  4. `schedules/index.tsx`：接入真实 createSchedule/deleteSchedule（原假提示'待联调'/'需后端 DELETE 支持'），补 capacity/status 列。
  5. `orders/index.tsx`：补 `prescription_id` 处方药追溯列（PRD 3.3 核心）。
  6. `BasicLayout.tsx`：补 `/ih/drug-catalog` 菜单入口（原页面在但导航无入口）。
- 未修（P3，前后端双缺，超出本计划范围，待单独决策）：departments（科室结构，现 hospital.tsx 用 DEMO_DEPTS 硬编码）、pharmacies+库存（IhDrug 无 stock 字段）、complaints（投诉售后，零代码）、dashboards（监管看板，现 operations.tsx 用 4×page_size=1 拼 total 临时方案）。这些需建表迁移+模型+router+页面。

## 计划四 Phase 当前状态
- Phase1 合理用药真引擎：已完成并收尾（停进程+删脚本+RX_ENGINE复位mock）✅
- Phase2 微信支付生产骨架：已完成（未端到端验证，无凭证）✅
- Phase3 S8 小程序 mt 域前端：已完成 ✅
- Phase4 医院后台前端核查补全：已完成（P0-P2 修复；P3 双缺模块未建，待决策）✅
- 全部改动尚未 commit（用户未要求提交）。
- P5 性能容灾；修 errors.py `_val` 对 RequestValidationError 含 bytes input 序列化的 5000 技术债（历次联调已多次触发，统一用 Python urllib 传 JSON 规避）。

## 提交里程碑
- `f4925a0`（推送 origin/main）：前四项工作——合理用药真引擎/微信支付生产骨架/S8中台域前端/医院后台前端核查补全。
- `efa4997`（推送 origin/main）：P3 四个双缺模块（药房库存/投诉售后/监管看板/科室结构）。用户要求"先提交，提交后继续"，已分两次提交。

## P3 双缺模块建设（本次，已 commit efa4997）

用户澄清：四个模块**全部建设**，按**业务闭环优先**排序（药房库存 > 投诉售后 > 监管看板 > 科室结构）。

### 后端（code/backend）
- 迁移 0013/0014/0015（幂等，alembic upgrade head 跑通）：
  - `IhPharmacy`(name/region/license_no/contact/status) + `IhDrugStock`(drug_id/pharmacy_id/stock/safety_stock，联合唯一)
  - `IhComplaint`(order_id/user_id/type/content/status/reply，状态机 pending→processing→resolved/closed)
  - `IhDepartment`(name/head/remark)
- 新 router：pharmacies.py（全CRUD，写操作 require_role('platform')）、drug_stocks.py（查询+出入库，require_role('platform','pharmacist')）、complaints.py（列表/详情/创建/处理回复，创建不强制角色）、dashboards.py（GET 聚合：core/compliance/warning/rx_trend_30d，复用既有表）、departments.py（全CRUD，require_role('platform')）。
- dashboards 趋势查询踩坑：`func.to_char` 须与 GROUP BY/ORDER BY 用同一表达式（否则 asyncpg GroupingError）。已修。
- 所有写操作接 write_audit；列表接口公开只读（无 JWT 要求），写接口缺 JWT 返回 code:2001（已实测验证）。

### 前端（code/admin-web）
- services/ih.ts：补 pharmacies/drug-stocks/complaints/dashboards/departments 封装。
- hospital.tsx：DeptTab / PharmacyTab 删除 DEMO_DEPTS/DEMO_PHARMACIES 硬编码，接入真实接口（platform 角色可增删）；canEditPharmacy/canEditDept = role==='platform'。
- 新建 routes/ih/complaints/index.tsx（列表+处理抽屉，PII 仅脱敏 user_id）。
- operations.tsx：替换为 getDashboards，补审方通过率/投诉/低库存卡片（原 4×page_size=1 临时拼装已弃）。
- BasicLayout.tsx：补 /ih/complaints 菜单入口；此前已补 /ih/drug-catalog。

### 遗留/注意
- IhDoctor.dept 仍保留 String 命名（未外键化，避免波及医师链路）——计划中已约定。
- P3 后端接口写操作均需 platform JWT，前端当前用 localStorage role 判断可见性，但实际写请求需后端 dev-token（联调时需平台角色 token）。
- mp-taro 未改动（P3 为管理后台范畴）。

## P5 性能容灾核查 + 根目录无用文件清理（本次）

### P5 现状（结论：已实际完成，无待补）
- 经逐个打开核查代码仓库，P5 计划全部能力早已落地（计划 todo 全 completed）：
  - `app/core/cache.py`（Redis cache-aside + `cached` 装饰器）、`app/middleware/rate_limit.py`（双档 per-ip/per-user，429+Retry-After）、`db/session.py` pool_pre_ping + main.py lifespan 探活/优雅关闭 + `/health` 升级依赖状态。
  - `tests/load/locustfile.py` + pyproject `dev=["locust>=2.0"]`；`tests/test_cache.py`/`test_rate_limit.py`/`test_health.py`。
  - 灾备：`infra/scripts/backup_db.sh` + `restore_db.sh` + `infra/README.md` RPO/RTO 小节。
- 用户问"继续做 P5"——实际 P5 已做完，无需再写代码。**可选项**：跑一次真实验证（docker 起 + pytest + locust 冒烟）确认限流/缓存生效，属于验证而非建设。

### 根目录无用文件清理（已执行删除 18 个）
- 删除（零代码引用，历史生成/调试残留）：build_report.py / fix_report.py / generate_report.py / update_report.py / verify_final.py（5 个 openpyxl 报告脚本）；cells_dump.txt / cells2.txt / cur_cells.txt / merges.txt / orig_dash.txt / report_dump.txt / verify_final.txt / verify2..6.txt（13 个 dump/verify 文本）；backend_err.log / backend_run.log（2 个 uvicorn 日志）。
- 保留：所有 .md/.docx 文档、`项目状态报告.xlsx`、`README.md`、`code/`、`infra/`、`mlruns/`。
- 仍存疑（未删，待用户定）：`report_orig.xlsx`（报告脚本原始输入模板，重生成会用到，建议保留）、`verify_report.txt`（verify 输出，价值极低，可删）。
- 注意：根目录那批"诊断/调试/报告"文件属各阶段临时产物，约定既往保留；本次用户明确"哪些无用可删"才清理，今后如用户未要求不再主动删。

## P5 真实验证（用户："保留，并启动 P5 真实验证"）

### 结论：P5 四项能力真实验证全部通过
- **pytest（5 passed, 9.2s）**：test_cache（Redis 命中/删除）、test_rate_limit（计数）、test_health（依赖状态）全绿，直连 docker PG/Redis。
- **/health 探活**：curl 返回 `code:0, status:ok, dependencies:{redis:ok, postgres:ok}`，lifespan 探活 + 双依赖真实可达。
- **locust 30s 脉冲（单用户，无 think-time）**：
  - `POST /api/v1/auth/login`（非白名单，受 RateLimitMiddleware 管控）：1463 请求中 **1345 个返回 429**（Too Many Requests），单 IP 60s/120 上限真实触发；限流前 ~118 个返回 401（登录失败，属正常）。
  - `GET /health`（白名单）：490 请求 **0 失败**，平均 18ms / p99 51ms，正确放行。
  - 聚合吞吐 ~67 req/s；429 返回 p50=8ms，限流中间件开销极低、不拖垮服务。
  - locust exit code 1 仅因 100% 请求被标 failure（限流生效的预期表现），非 backend 错误。
- **灾备**：backup_db.sh / restore_db.sh / infra/README RPO-RTO 小节已就绪（未实跑，属脚本可用性，非运行态）。

### 验证后清理
- 杀掉临时 backend（uvicorn PID 16320）、删除临时 locustfile 与 4 个日志（p5_*.log/.err）。
- 按用户"保留"指示，根目录 `report_orig.xlsx` 和 `verify_report.txt` **保留未删**。

### 环境备注（验证用）
- docker 全家桶（PG/Redis/Mongo/ClickHouse）长期运行 healthy；backend `.env` 连接串指向 localhost:5432/6379（即 docker 实例）。
- Python 3.14.2（系统全局）；无 .venv，依赖装于全局；locust 2.46.2 可用。
- PowerShell `Get-Content` 因编码策略被拦截，改用 read_file / Invoke-RestMethod 替代。

## P4 安全闭环渗透自测 + P3 前后端联调（用户："先P4渗透自测，再做P3联调"）

### P4 渗透自测结果
- **自动化基线**：`verify_auth.py`（9/9 PASS，报告落 backend/verify_auth_result.txt） + `test_rate_limit.py`（1 passed 真实 Redis）。
- **手验关键风险点**（debug 态 backend 8000 + dev-token 取 platform/patient JWT）：
  - 缺 JWT → `code:2001`（统一错误模型，非 401）✅
  - 越权 patient→store-only 写接口 → `code:2003` ✅（require_role 生效）
  - dev-token 仅 debug=True 时可用，debug=False 404 关闭 ✅（生产默认安全）
  - 限流 per_ip 固定窗口 120/60s **精确触发 429**（第121次起），返回统一错误体+Retry-After+X-RateLimit-Limit ✅
  - 白名单 `/health` 不受限 ✅
  - **⚠️ 风险点（已知设计取舍）**：`X-Forwarded-For` 首段被信任为客户端 IP，攻击者可每请求换 XFF 绕过 per-ip 档（标注"经反代时"）。直连无反代场景是弱点 → 建议生产前置反代强制覆盖 XFF 或仅受信任代理后启用。
  - 2FA/加密/KMS/审计/幂等/防重放：已在 P4 阶段落地（verify_auth 覆盖鉴权面，加密/审计为写操作固有调用），本次未逐接口重测，属已验收能力。

### P3 前后端联调结果
- 用 dev-token(platform) 跑通四模块全链路：
  - 药房：create/list/patch ✅
  - 投诉：create/list/detail/**handle(PATCH /{id})**/状态回写(status→resolved, reply 回写) ✅ —— 注意 handle 是 PATCH 非 POST，前端 services/ih.ts 用 PATCH 与后端一致 ✅
  - 看板：core/compliance/warning/rx_trend_30d 全有值 ✅
  - 科室：create/list/patch/delete ✅
  - 库存：upsert→adjust(PATCH)→list→低库存 is_low 计算 ✅（stock=10<safety=20 触发 is_low=True，only_low 命中）
  - 越权：patient 写药房 → 2003 ✅
- **🔴 发现并修复 1 个真实后端 Bug**：`DrugStockOut.is_low` 无 default，upsert/adjust 调 write_audit 时 `DrugStockOut.model_validate(orm)` 因 is_low 缺失抛 ValidationError → 返回 code:5000（审计写操作必挂）。已修（is_low: bool = False），提交 `77ec515`，重联调全绿。
- **⚠️ 契约语义隐患（非阻断）**：库存 adjust 两端均为"直接设置 stock 值"语义，但 UI 文案为"入库/出库"（应为 delta 增减）。当前联调不挂（PATCH 成功），建议后续改为 delta 累加与 UI 一致。不在本次修复范围。
- **测试数据注意**：IhDrug 表无种子数据，首次联调取不到 drug_id；需用 POST /ih/drugs 造一条才能验库存写。联调脚本已处理（造 drug→验库存→删 drug）。

### 临时文件清理
- 验证用临时脚本（_p3_*.py / _p4_*.py / p4_rl_*.py）已删除；backend 进程已停。
- 根目录保留：report_orig.xlsx、verify_report.txt（用户指示保留）。backend/verify_auth_result.txt 为历史产物未动。

### 关键路径速查（供后续会话）
- 限流中间件：`app/middleware/rate_limit.py`（per_ip 120/60s，白名单 /health /docs /openapi.json /metrics）
- 越权防护：require_role / actor_of / store_scope 在 `app/core/deps.py`
- P3 router：`app/api/v1/ih/{pharmacies,drug_stocks,complaints,dashboards,departments}.py`
- 联调拿 token：`POST /api/v1/auth/dev-token {"role":"platform","sub":"1"}`（需 settings.debug=True 启动 backend）
- 注意 PowerShell 跑 Python 脚本：脚本文件放 backend 目录（否则 app 包 import 失败，因 Python 把脚本所在目录而非 cwd 加入 sys.path）

## 库存 adjust 语义改造：设值 → delta 增减（本次，已实施待 commit）

用户："继续把库存 adjust 改为 delta 增减语义"。

### 改动内容
- `app/schemas/ih.py`：删除 `DrugStockUpdate`（stock/safety_stock 设值语义），新增 `DrugStockAdjust`：`delta_stock: int`（必传，正=入库/负=出库）、`reason: Optional[str]`（审计事由）、`safety_stock: Optional[int]`（仍为设值语义，可选同步调阈值）。
- `app/api/v1/ih/drug_stocks.py`：`adjust_drug_stock` 改为按 `s.stock + body.delta_stock` 累加；扣减后库存<0 抛 `ErrorCode.PARAM_INVALID`（修正旧误用不存在的 `ErrorCode.BAD_REQUEST`/`NOT_FOUND`）；`safety_stock` 负数同样 PARAM_INVALID；审计 `after` 快照并入 `adjust_detail{delta_stock,reason,direction}`（direction=in/out/none，不新增表字段，保持哈希链完整）。
- `code/admin-web/src/services/ih.ts`：`adjustDrugStock(id, {delta_stock, reason?, safety_stock?})` 类型对齐。

### 验证（Python urllib 驱动 uvicorn 子进程，全 PASS）
- 初始 stock=100 → +30=130（in）→ -50=80（out）→ 同步 safety_stock=30 → 超额 -200 被拒并提示"当前库存"✅
- 修复了旧 `DrugStockUpdate` 时代便存在的 `ErrorCode.BAD_REQUEST`（不存在枚举）AttributeError 隐患。

### 注意
- `PlatAuditLog` 无 detail 字段，adjust 明细走 `after.adjust_detail`；`errors.py` 的 `error()` 响应键是 `message`（非 `msg`）。
- 改 schema 后需手动重启 uvicorn（--reload 对 schema 不总生效）。
