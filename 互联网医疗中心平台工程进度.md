# 互联网医疗中心平台工程进度

> 文档名称：互联网医疗中心平台工程进度
> 编制日期：2026-07-26
> 适用范围：项目负责人 / 管理层 — 交付状态对齐、上线排期决策、外部协同事项推进
> 依据基线：PRD V2.0（双主线、7 类用户）、技术架构设计方案 V1.2、实际代码树（`code/`）、合规/等保材料清单，以及本次 P6 前端接通的交付结果
> 说明：本文件为面向负责人的"总览 + 行动"版；详细 PRD/架构覆盖矩阵与对抗式风险缺口见《互联网医疗中心平台整体进度与下一步计划.md》《P0–P3 自检与对抗式审查报告.md》

---

## 一、总体状态（一句话结论）

**研发侧平台底座已成形（P0–P3 后端地基 + ai-service 已落地并机制级验证 21/21 PASS；本次进一步将前端 P6 标注缺口全部接通后端真实接口），但项目"上线就绪度 = 未就绪"：缺等保三级正式测评 + 互联网医院/药品经营等执业许可（硬门槛、需组织流程与外部审批），以及真实第三方对接与运行时端到端验证。**

当前交付物是"**可内部研发验证、接口闭环的系统骨架**"，距离"可合法对外运营的互联网医疗产品"仍有四道大关：**合规资质 → 运行时验证 → 真实业务前台 → 安全/性能闭环**。

| 维度 | 结论 |
| --- | --- |
| 计划完成度（P0–P3 + 本次 P6 前端） | 后端地基完成；前端 P6 缺口已接通（admin-web 全量 build 通过，mp-taro 类型检查通过） |
| PRD V2.0 功能覆盖 | 后端数据域 ≈75%、前台/工作台 ≈30%、平台监管 ≈25%（较上一版提升） |
| 技术架构方案覆盖 | RBAC/RLS/plat/可解释/数据分级/双因子/审计哈希链已实现；第三方对接/等保测评部分或未做 |
| 上线就绪度 | **未就绪**：缺合规测评 + 执业许可（硬门槛）+ 运行时验证 + 真实第三方对接 |

---

## 二、已完成情况

### 2.1 方案与合规文档（✅）
| 文档 | 状态 |
| --- | --- |
| PRD V1.0 / V2.0（双主线、7 类用户） | 完成 |
| 技术架构设计方案 V1.2（15 章） | 完成 |
| 合规清单与等保三级准备工作（md + docx） | 完成 |
| 等保三级测评与互联网医院执业许可 — 材料清单（交付版） | 完成（可直接转申报台账） |
| P0–P3 自检与对抗式审查报告 | 完成 |
| 整体进度与下一步计划 | 完成 |

### 2.2 后端 backend（🟢 P0–P3 业务链路已落地并验证）
- **基础设施**：`core`（config / 统一响应 / 错误处理 / security JWT+防重放 / deps）、`middleware`（request_id / idempotency / audit）、`db` 客户端、`models`（完整 `ih_/mt_/plat_` ORM）、`schemas`、`alembic` 迁移、Dockerfile。
- **业务路由（真实 CRUD，P0）**：`ih`（登录签发 JWT / doctors / orders+支付 / prescriptions 开方与药师审核 / consultations 在线复诊）、`mt`（customers+授权 / pain_assessment / care_plans / treatment_records / stores / therapists / 复购与风险画像回写）、`plat`（audit-logs + AI 模型 / 数据资产真实 CRUD）。
- **鉴权强制 + 审计真实化（P3）**：全部业务路由注入 `current_user`/`require_role`，`write_audit` 取 JWT 真实主体；`verify_p3` 机制级验证 9/9 PASS。
- **P1 复购/风险回写**：调用 ai-service 真实推理并落库，AI 不可用优雅降级。
- **P2 ClickHouse 经营宽表**：PG 预聚合写 CH（幂等），`store-metrics` 接口 + Grafana 看板（金额字段暂置 0，待订单系统对接）。
- **P3 平台治理**：plat 真实 CRUD、RLS 行级隔离（`store_scope` + 8 个 mt 端点注入）、幂等种子 `seed.py`、合理用药引擎抽象层 + Mock、合规清单文档。机制级验证 **21/21 PASS**。
- **安全控制（部分已真落地）**：双因子 TOTP 已真实现（密钥落库、登录真校验）；RBAC 六角色 + 门店级 RLS 已落地；审计日志 SHA-256 链式防篡改，附 `verify_audit_chain.py` 可出具完整性证明。
- **admin-web 登录入口**：登录页（dev 下 dev-token 按角色签发 JWT）+ `AuthRoute` 守卫 + 401 清理。

### 2.3 AI 服务 ai-service（✅ 最成熟，已端到端验证）
- `model_loader.py`：MLflow 加载可用（已修 `torch` DLL 崩溃、`version` int→str 两处 bug）。
- 三接口真实推理通过：`plan-recommend` v8 / `repurchase-prediction` v7 / `risk-profile` v5，均带 `isAssist:true`。
- RAG 可解释：`plan-recommend` 接入知识检索，`rationale` 引用真实知识条目。
- AI 反馈闭环：业务回写 `plat_model_pred_log` + 采纳/驳回接口（RBAC + 审计）。

### 2.4 前端 admin-web（🟢 已接真实接口，本次新增 2 页）
- 真实页面（共 9 个目录）：customer（列表+授权）、repurchase（发起+历史）、risk（发起+历史）、data（门店经营看板）、plat（审计日志 + AI 模型目录 + 数据资产目录）、login。
- **本次新增/改写**：
  - `store` 门店页：原 localStorage 保存改写为调用 `/mt/therapists/{id}/schedules` 与 `/mt/therapists/{id}/tags`（排班 + 能力标签分配，保留"客户分配"本地字段）。
  - **新建 `compliance-review` 合规采集审核页**：提交工单 + 列表（状态筛选）+ 通过/驳回（reject 必填意见），审核按钮仅 platform/xingyao 可见。
  - **新建 `drug-catalog` 药品目录管理页**（platform 角色）：ProTable 列表 + 新建/编辑 ModalForm + 下架。
  - `services/mt.ts`、`services/plat.ts`、`services/ih.ts` 增补排班/标签/合规/药品目录函数；路由与菜单已在 `App.tsx`、`BasicLayout.tsx` 注册。
- 验证：`npx tsc --noEmit` 0 错误；`npm run build` 全量构建通过（exit 0）。

### 2.5 前端 mp-taro 小程序（🟡 由空壳升级为部分可用，本次大幅补强）
- **本次新增/改写（P6 缺口接通）**：
  - `doctor-schedule` 医生排班页：原 localStorage 改写为调用 `GET/POST/DELETE /ih/schedules`（周视图网格按"生成未来 4 周具体日期 / 加载聚合"接通）。
  - `services/ih.ts`：新增 `listDrugs/getDrug`（药品目录只读）与 `list/create/delete DoctorSchedule`。
  - **新建 `drug-catalog` 药品目录浏览页**（开方选药），并在处方页加入口。
- 验证：`npx tsc --noEmit` 通过；`taro build` 因本环境缺 `@tarojs/webpack5-runner` 等打包插件无法运行（环境限制，非代码问题）。

### 2.6 P6 标注缺口接通（本次交付小结）
> 原前端 4 处标记为"P6 缺口"的接口对接，已全部切换为真实后端调用：

| P6 后端域 | 前端改造 | 状态 |
| --- | --- | --- |
| 医生排班 `/ih/schedules` | mp-taro 医生端页 localStorage → 真实接口 | ✅ 代码完成 |
| 调理师排班/标签 `/mt/therapists` | admin-web 门店页 localStorage → 真实接口 | ✅ 代码完成 |
| 药品目录 `/ih/drugs` | service + 小程序开方选药 + 后台管理页 | ✅ 代码完成 |
| 合规采集审核 `/plat/compliance` | **新建 service + 审核模块**（原无前端） | ✅ 代码完成 |

### 2.7 基础设施 infra（🟡 编排较完整）
- ✅ `docker-compose.yml`：PG(+pgvector) / Redis / Mongo / ClickHouse / MinIO / Jaeger / Prometheus / Grafana 全编排。
- ✅ `k8s/helm`：namespace + deployment 基础模板；`pre-commit`、README 在。
- ⚠️ `nginx` 配置、pg/CH 的 init SQL 多为占位。

---

## 三、未完成情况（按优先级与风险）

| 优先级 | 事项 | 现状 | 影响 |
| --- | --- | --- | --- |
| **P0（阻塞）** | 合规硬门槛：等保三级正式测评 + 执业许可 | 仅出材料清单；双因子/审计链等技术控制已落地，但**定级备案、第三方测评、卫健委审批、许可证申办均未启动** | 不解决则无法合法上线（硬门槛） |
| **P1** | 运行时端到端验证 | `alembic upgrade head` 与 `seed.py` **从未执行**；RLS 仅代码级单测；pred_log / CH 写入未真实库验证；本次 P6 前端接的后端接口**未做运行时联调** | 联调/验收时门店维度功能"空转"，P6 接口可用性无证据 |
| **P2** | 真实第三方对接 | 合理用药 = 抽象层 + 玩具 Mock（仅 4 条规则，开方 patient 信息未传入）；微信支付 / 短信未做 | 处方安全合规不达标；交易链路不可运营 |
| **P3** | 业务前台补位 | 小程序 2C/医生端完整业务流缺失；PRD 3.5 调理师工作台、3.3 医院管理（科室/药房/药师审核配置）、3.7 平台监管看板无前端 | 离"可对外运营"差一个数量级 |
| **P4** | 安全闭环 | 落库敏感字段强加密（KMS/信封加密）、审计防篡改 WORM 存储、WAF/CSRF 部署验证未做（双因子已落地） | 等保测评关键项缺口 |
| **P5** | 性能与质量 | 无压测；ClickHouse 仅聚合层占位未验证；AI 推理时延未测 | 上线后稳定性风险 |

> 注：以上 9 项对抗式审查缺口（详见《P0–P3 自检与对抗式审查报告.md》第四节）中，**RAG/可解释、AI 反馈闭环、双因子、审计哈希链、P6 前端接通** 已在本阶段补强，其余仍开放。

---

## 四、下一步计划（里程碑与排期建议）

按"阻塞上线 → 业务价值 → 验证"排序，建议如下里程碑（排期为相对顺序，非绝对日期）：

| 阶段 | 计划项 | 关键交付物 | 负责方 | 阻塞/价值 |
| --- | --- | --- | --- | --- |
| **M0（最高，阻塞）** | 合规资质申办 | 等保三级定级备案；对接第三方测评机构；申办医疗机构执业许可证 + 互联网医院准入 + 药品经营许可证 + ICP + 医师多点执业备案 | **公司/负责人牵头，研发配合** | 阻塞上线 |
| **M1** | 运行时验证闭环 | 起 infra + `alembic upgrade head` + 跑 `seed.py`；带 store_id dev-token 真联调 RLS；验证 pred_log / CH 写入；**对本次 P6 前后端做真实联调** | 研发 + 运维 | 消除 P1 风险，使 P6 接口可用 |
| **M2** | 真实第三方对接 | HttpRxEngine 接真实合理用药供应商；微信支付 / 短信对接；开方真实传入 patient 信息 | 研发 + 商务 | 消除 P2 风险 |
| **M3–M4** | 业务前台补位 | 小程序 2C/医生端完整业务流；调理师工作台（3.5）、医院管理（3.3）、平台监管看板（3.7） | 前端研发 | 消除 P3 风险 |
| **M贯穿** | 安全与性能闭环 | 落库加密（KMS）、审计 WORM、WAF/CSRF；压测、CH 验证、AI 推理真实化验证 | 研发 + 运维 | 消除 P4/P5 风险 |

**建议节奏**：立即并行启动 M0（合规申办）与 M1（本地运行环境）；M1 完成后滚动推进 M2–M4；安全与性能贯穿全程，为等保测评与上线硬指标留足缓冲。

---

## 五、难点与需配合解决事项（请负责人决策/协调）

以下问题非研发单方可闭环，需要项目负责人或公司层面推动，请重点协调：

1. **合规资质是最大阻塞（M0，需组织流程与外部审批）**
   - 等保三级测评需对接有资质的第三方测评机构并启动定级备案；互联网医院/医疗机构执业许可需**实体医疗机构依托 + 人员资质 + 制度与信息系统安全证明**，向省级卫生健康行政部门申报。
   - 药品经营许可证、ICP 证、医师多点执业备案同期推进。
   - **需要**：明确申报主体与责任人、落实实体医疗机构合作/依托、预算与排期。研发可配合提供技术证据（已具备双因子、RBAC/RLS、审计哈希链等），但审批流必须由公司层面发起。

2. **真实运行环境缺失（M1）**
   - 当前无 PG/Redis/ClickHouse 运行时，种子数据未落库，P6 前端接通的接口尚未做真实联调，验收时门店维度功能会"空转"。
   - **需要**：运维/云资源到位，搭建可运行环境并执行迁移与种子，支撑端到端验证。

3. **真实第三方供应商对接（M2）**
   - 合理用药知识库/引擎、微信支付、短信网关均需商务签约与密钥，研发侧已有可替换抽象层（HttpRxEngine 骨架在），但供应商未定。
   - **需要**：商务侧确定供应商并开通对接资质，研发方可在一周内完成接入。

4. **业务前台工作量与资源（M3–M4）**
   - 小程序完整 2C/医生业务流、调理师工作台、医院管理、平台监管看板体量较大，是当前前端覆盖的主要缺口。
   - **需要**：确认优先级与人力投入，避免阻塞"可对外运营"形态。

5. **安全闭环遗留项（M贯穿）**
   - 落库敏感字段强加密（KMS/信封加密）、审计 WORM 存储、WAF/CSRF 部署验证为等保测评关键缺口；双因子已落地。
   - **需要**：明确加密方案（自研 KMS 或云厂商 KMS）与部署侧安全组件采购。

---

## 六、验证记录（本次可复现）

- `code/backend/verify_p3.py`：**21/21 PASS**（编译、路由注册、store_scope 逻辑、rx_engine Mock、配置项、seed 结构）。
- `code/admin-web` `npx tsc --noEmit`：**0 错误**；`npm run build`：**通过（exit 0）**。
- `code/mp-taro` `npx tsc --noEmit`：**通过**（打包插件本环境缺失，非代码问题）。
- 合规材料清单：等保三级测评与执业许可材料清单（md + docx）已就绪，可直接转申报台账。

> 本报告中"已完成"声明以上述代码树与机制级验证为证据；"未完成/需配合"项已在第三、五节明确，供排期、验收与跨部门协同追踪。

---

## 七、2026-08-02 状态自检更新（第一性原理 + 对抗式审查）

> 依据：`互联网医疗中心平台_状态自检与后续任务_2026-08-02.md`。本次以真实代码为事实源，补充本报告（07-26 版）未覆盖的偏差与对抗式硬伤。

### 7.1 事实纠正与补充

| 项 | 本报告原状（07-26） | 工程真实（08-02 代码实证） | 纠正 |
| --- | --- | --- | --- |
| 双因子/审计链/落库加密 | 2.2.45 已纠正为"已真落地" | 确认（RFC6238 / SHA-256 链 / PBKDF2 / KMS 信封） | 一致，维持"已落地" |
| RLS 隔离 | 2.2.44 "8 端点注入" | 实测 mt 10 路由：7 真实隔离；`effect_tracking.py` 注入未用于 WHERE（死参数）；`risk_profiles.py` 完全未注入（任意 store 读全量）；`scheduling.py:40` 手工调用参数语义不匹配；**无多租户越权集成测试** | **存在 2 处隔离缺口**（H1） |
| 医生身份 | 2.5/3.4 隐含"缺失" | `users.py` `WX_LOGIN_ROLES={patient,doctor,pharmacist}` 已支持医生/药师签发 | 已支持，但含后门（H2） |
| 支付 | 2.2 / 三-P2 "微信支付未做" | `orders.py` `pay_order` 调 `create_prepay`（dev 沙箱模拟）+ `/wxpay/notify` 真实 RSA 验签骨架 | 骨架完成，缺真实商户号/证书联调（H3） |
| P3 四模块 | 三-P3 "医院管理（科室/药房/药师审核）无前端" | 08-02 科室/药房/投诉/看板全链路验证：pytest 41 passed、tsc 0 错、alembic head=0015 | **已闭环** |
| 种子 | 三-P1 "从未执行" | `seed.py` 存在幂等，但运行与否未实证 | 维持"未实证"（H5） |

### 7.2 对抗式审查新增硬伤（须并入 P0/P1）

- **H1 RLS 隔离缺口（🔴）**：`risk_profiles.py` 两端点未注入 `scope`，任意 store 可读全量；`effect_tracking.py` 注入未使用；无多租户越权测试。注入≠生效。
- **H2 DEV 建档后门（🔴 合规红线）**：`users.py:65-70` 当 `role=doctor` 无关联档案时**无条件**自动建 `IhDoctor(license_no="DEV-DOC-{user.id}")` 并签 doctor JWT，不受 `APP_ENV` 守卫。生产任意用户可获医师身份。
- **H3 支付未生产化（🟠）**：仅 dev 沙箱模拟，无真实商户号/证书/联调；`/pay/mock-success` 仅沙箱可用但逻辑在。
- **H4 合理用药 4 规则 Mock（🟠）**：`rx_engine/mock.py` 仅 4 条相互作用 + 4 条孕期禁用；`HttpRxEngine` 未接真实供应商。
- **H6 IhDoctor.dept 未外键化（🟡）**：仍为 String 命名，无约束关联 `IhDepartment`。

### 7.3 更新后的优先级排序（融合本文档第三章）

- **P0（阻塞）**：合规资质申办 + **关 DEV 建档后门（H2）** + **补 RLS 缺口 + 多租户越权测试（H1）** + 支付生产化（H3）。
- **P1**：运行时验证（alembic+seed 实证 H5）+ 密钥生产化（云 KMS）+ 合理用药真引擎（H4）+ `IhDoctor.dept` 外键化（H6）。
- **P2**：数据中台 CH 验证、审计脱敏对齐。
- **P3**：小程序 S2-S8 业务闭环（药师审核真调 audit、支付真调起、mt 域接入）。

> 说明：H1/H2 为本次对抗式新发现，原本文档第三章未列入，须作为 P0/P1 高优先级技术债跟踪，不可因"双因子/审计链已落地"而误判安全闭环已完成。
