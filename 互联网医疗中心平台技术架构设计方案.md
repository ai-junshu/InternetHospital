# 互联网医疗中心平台 · 技术架构设计方案

> 配套文档：《互联网医疗中心平台产品需求文档（PRD V2.0）》
> 本文为 PRD V2.0 的「第 6 章 技术架构建议」独立成文版，供技术评审、招采与研发落地参考。

## 文档版本历史

| 版本号 | 修订日期   | 修订人 | 修订说明                                                       |
| ------ | ---------- | ------ | -------------------------------------------------------------- |
| V1.0   | 2026-07-25 | AI     | 基于 PRD V2.0 双主线架构，输出技术选型、数据库与数据中台方案。 |
| V1.1   | 2026-07-25 | AI     | 增补第 14 章「第三方服务对接清单（微信支付/短信）」。 |
| V1.2   | 2026-07-25 | AI     | 增补第 15 章「优化补充设计」（处方双轨审核/行级隔离/隐私计算/AI反馈闭环/可观测性/缓存一致/CDC/双通道容灾/上线上前置许可）；并就医保不接入、联邦学习、不独立App 更新相关章节。 |

## 1. 引言

### 1.1 背景

PRD V2.0 确立了"**互联网医院 + 健康数据中台**"双主线架构，业务覆盖 2C 在线复诊、2B 连锁门店赋能、2G 平台与星耀产业资本运营三大角色群（共 7 类用户）。系统需同时满足：

- 微信小程序作为 C 端/医生端统一承接入口；
- 门店管理后台、数据中台管理后台、平台管理后台作为 Web 运营与决策入口；
- 以"客户健康档案 → 疼痛评估 → 调理方案 → 治疗记录 → 效果追踪 → 复购预测"为链路，持续沉淀**结构化治疗数据资产**；
- 基于数据资产构建 AI 辅助决策（风险画像、方案推荐、依从性管理），并支撑第二阶段融资估值。

### 1.2 设计目标

1. **快速开发**：业务接口与 AI 能力可并行交付，MVP 3–6 个月可上线核心闭环。
2. **AI 贴合**：训练数据来源、数据录入、分析、挖掘、诊断分析全链路在同一技术栈内闭环，避免业务/算法双语言割裂。
3. **数据资产化**：千万级数据下的多维分析 <5s，支持数据血缘、质量治理与对外合规输出。
4. **合规安全**：满足等保三级、数据加密、处方/健康档案留存 ≥15 年、AI 辅助不替代医师等硬性约束。

### 1.3 设计原则

- **前后端分离 + 微服务化**：业务服务与 AI 服务解耦，独立迭代与扩缩容。
- **分库分场景**：OLTP / 文档 / OLAP / 向量 / 搜索按场景独立选型，避免"一库打天下"。
- **开源自建优先**：核心组件全部开源可控，降低被绑定风险，便于等保合规改造。
- **云原生交付**：容器化部署，弹性应对问诊高峰与模型批量训练。

## 2. 总体架构

采用"接入层 → 应用层 → 数据层 → AI 层"四层架构：

```
接入层   微信小程序(2C用户端 / 2B医生端)  +  Web管理后台(门店 / 数据中台 / 平台)
        │
应用层   业务中台(问诊 / 处方 / 商城 / 档案) + 数据中台(采集→治理→资产) + AI决策服务
        │
数据层   业务库(OLTP) · 缓存 · 文档库 · OLAP看板 · 向量库(AI) · 搜索引擎 · 数据湖(资产)
        │
AI层     特征工程 · 模型训练 · MLOps · LLM诊断辅助 · RAG知识库
```

- **接入层**：微信小程序承接 2C/2B 轻量交互；Web 后台承接运营、看板与治理。
- **应用层**：业务逻辑与 AI 推理通过 API 网关串联，AI 仅输出辅助结果。
- **数据层**：多类型存储各司其职，统一通过数据中台对外提供资产服务。
- **AI 层**：离线训练 + 线上推理，模型版本与数据血缘全程可追溯。

## 3. 前端技术选型

| 场景 | 推荐方案 | 选型理由 |
| ---- | -------- | -------- |
| 微信小程序（2C 用户端 / 2B 医生端） | **原生小程序** 或 `Taro` / `uni-app`（一套代码多端） | PRD 明确以微信小程序为载体；Taro 复用 React 语法，组件可复用于后台 |
| 三套管理后台（门店 / 数据中台 / 平台） | **Vue3 + Element Plus** 或 **React + Ant Design Pro** | 后台 CRUD/表单密集，ProTable、表单校验开箱即用，开发最快 |
| 中台数据看板 | **ECharts / AntV (G2/G2Plot)**，大屏用 **DataV** | 覆盖到店率、转化率、客单价、复购率、NPS、效果趋势、风险画像；ECharts 图表最全 |
| 低代码后台骨架 | **若依 RuoYi** 或 **Amis** | RuoYi 自动生成权限 + CRUD 骨架，可省约 30% 后台工作量 |

> 建议：小程序用 `Taro`（React 技术栈），后台用 `React + Ant Design Pro`，前后端 TS 同源，团队协作效率最高。

## 4. 后端开发语言与框架

**主推：Python 为主、Node.js 为辅、Go 兜底高并发。**

- **Python (FastAPI)**：① AI/ML 生态唯一全栈霸主（PyTorch、scikit-learn、pandas、MLflow）；② FastAPI 异步性能优秀、自动生成接口文档，开发极快；③ 复购预测、风险画像、方案推荐、诊断分析均由 Python 实现，避免双语言割裂。
- **Node.js (NestJS)**：若团队前端为 JS 栈，业务接口统一用 NestJS，适合问诊 IM、订单、支付回调等 IO 密集场景。
- **Go (Gin)**：仅在高并发实时推送（IM 问诊、预警推送）引入，非必需。

> 结论：**单一 Python 全栈（FastAPI + 前后端分离）是"快速开发 + AI 贴合"的最优解**；若追求前后端同语言，采用 Node.js 业务 + Python AI 微服务。

## 5. 数据存储架构

按 PRD 的数据特征分库选型：

| 用途 | 选型 | 对应 PRD 需求 |
| ---- | ---- | ------------- |
| 业务主库 OLTP | **PostgreSQL**（首选）或 MySQL | 用户/医生/处方/订单；PG 原生 JSONB 存处方清单、健康标签，扩展性强 |
| 文档/半结构化 | **MongoDB** | 治疗记录 JSON、疼痛评估原始答案 JSON、方案组合（读写频繁、结构多变） |
| 缓存/会话/队列 | **Redis** | 登录态、处方状态、预警去重、会话列表 |
| OLAP 数据看板 | **ClickHouse**（首选）或 Apache Doris | "千万级多维分析 <5s"——门店经营宽表、效果分布、复购率滚动窗口 |
| 向量库（AI） | **Milvus** 或 **pgvector**（PG 插件） | 3.6.3 方案推荐"相似客户案例"检索、RAG 诊断知识库、风险画像向量化 |
| 搜索引擎 | **Elasticsearch** | 找医生/找药品全文检索、合规风险事件库检索 |
| 数据湖/资产 | 对象存储 + **Apache Iceberg/Hudi**（云上可用 OSS/S3 + MaxCompute/EMR） | 5.2 数据资产生命周期：原始治疗数据归档、血缘、对外输出 |

> 起步精简组合：**PostgreSQL + Redis + MongoDB + ClickHouse + pgvector**，覆盖约 95% 需求，全部开源可控。

## 6. AI 工具链与数据中台

贴合"训练数据来源 → 录入 → 分析 → 挖掘 → 诊断分析"全链路：

- **数据录入**：小程序/调理师工作台表单 → FastAPI 写入 Mongo/PG；图片经 OCR（PaddleOCR）提取处方/评估单。
- **数据处理/挖掘**：`pandas` / `PySpark`（千万级用 Spark on 数据湖）。
- **特征工程 & 训练**：`scikit-learn`（复购/风险/依从性用 XGBoost）、`PyTorch`（疼痛效果预测）；`MLflow` 管理模型版本（对应 3.6.3 模型管理：训练集/版本/效果指标/回滚）。
- **诊断/方案辅助**：`LangChain` / `LlamaIndex` + 大模型 API（如 GLM/通义/混元）做 RAG——将疼痛评估量表、临床指南、历史方案构建知识库，输出"可解释方案推荐 + 相似案例数"（满足 PRD 可解释性要求）。注意：仅辅助，不替代医师（合规红线）。
- **MLOps**：`DVC`（数据版本）+ `MLflow`（实验）+ 模型服务化（FastAPI / BentoML）。
- **隐私计算对外输出**：向星耀产业资本等外部主体采用联邦学习 / 多方安全计算（TEE），原始治疗数据不出域，仅输出模型参数与聚合指标（详见 15.3）。
- **看板数据源**：核心指标预计算写入 ClickHouse，前端 ECharts 直连查询服务。

**数据中台流水线**：

```
采集(小程序/工作台/POS) → 标准化(字段/取值/时序) → 清洗(去重/校验/隔离)
→ 结构化存储(疼痛类型×调理方案×产品组合 三维) → 质量治理(完整性/准确性巡检)
→ 资产目录(血缘/敏感等级/质量评分) → 分析(模型训练/洞察) → 输出(合规脱敏/协议管控)
```

## 7. 推荐技术组合

### 7.1 快速起步 MVP（3–6 个月）

> 小程序 `Taro` + 后台 `React + Ant Design Pro` + 后端 `Python FastAPI` + `PostgreSQL + Redis + MongoDB` + 看板 `ClickHouse + ECharts` + 向量 `pgvector` + AI `scikit-learn + MLflow`

优先跑通：在线复诊 + 电子处方 + 客户健康档案 + 门店经营看板 + 复购预测。

### 7.2 生产级（融资估值 / 等保三级）

> 前端同左；后端 `FastAPI(业务) + Python AI 微服务`；DB `PostgreSQL(主) + MongoDB(文档) + ClickHouse(OLAP) + Milvus(向量) + ES(搜索)`；数据中台 `Spark/Flink + 数据湖(Iceberg)`；云原生 `K8s + 等保三级安全组 + 加密存储`。

补充：实时预警用 Flink 流处理；大模型诊断辅助独立 GPU 推理节点；数据资产对外输出经合规大脑审核服务。

## 8. 与 PRD 关键约束的对应

| PRD 约束 | 技术方案对应 |
| -------- | ------------ |
| 数据留存 ≥15 年、加密 | PG/Mongo 开启 TDE 加密 + 冷数据归档数据湖，满足等保三级 |
| 千万级多维分析 <5s | ClickHouse 列式存储 + 预聚合宽表直击看板需求 |
| AI 预测 <2–3s 响应 | 模型离线训练、线上推理用 FastAPI + 向量索引缓存，毫秒级返回 |
| 合规大脑 / 数据血缘 | 数据湖 Iceberg 自带血缘/版本，MLflow 追模型溯源；对外输出走隐私计算/联邦学习，原始不出域，满足 5.2 全生命周期留痕 |
| AI 可解释性 | RAG 输出推荐依据 + 相似案例数；风险画像追溯至原始评估数据 |
| 微信支付 / 服务通知 | 后端对接微信支付 + 订阅消息 API |

## 9. 部署与运维建议

1. **云原生**：全部服务容器化（Docker）+ K8s 编排，CI/CD（GitLab CI / GitHub Actions）自动化构建与灰度发布。
2. **等保三级**：网络分区（DMZ/业务区/数据区）、最小权限、全量审计日志；敏感字段应用层 + 存储层双重加密。
3. **高可用**：PG 主从 + 读写分离，Redis 哨兵，ClickHouse 集群；关键服务多副本。
4. **监控**：Prometheus + Grafana 监控接口性能/模型衰减；AI 模型效果衰减自动告警（对应 3.6.3）。
5. **成本**：MVP 阶段可用单机 PostgreSQL + 托管 Redis；数据量与模型规模上升后再引入 ClickHouse/Milvus/Spark 集群。

## 10. 系统部署拓扑与接口规范

### 10.1 系统部署拓扑

采用 K8s 容器编排，按安全等级做命名空间隔离（ns-business / ns-ai / ns-data），网络分区满足等保三级（DMZ / 业务区 / 数据区）：

```
                          互联网 / 公网
                              │
                    ┌─────────┴─────────┐
                    │  CDN / WAF / DDoS  │
                    └─────────┬─────────┘
                              │ HTTPS / TLS 1.3
                    ┌─────────┴─────────┐
                    │  Nginx 入口网关     │
                    │ (SSL终止 / 限流 / 灰度)
                    └─────────┬─────────┘
                              │
                    ┌─────────┴─────────┐
                    │  API Gateway        │
                    │ (鉴权 / 路由 / 限流) │
                    └─────────┬─────────┘
                              │ 内网（业务区）
        ┌──────────────┬──────┴───────┬──────────────┐
        │              │              │              │
  业务微服务集群    AI 决策服务     数据中台服务     后台 Web 服务
  (FastAPI × N)  (推理 / GPU 节点) (采集/治理/资产) (React 静态)
        │              │              │
        └──────────────┴──────┬───────┴──────────────┘
                              │ 数据区（加密 / 隔离）
   ┌──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐
   │PostgreSQL│ MongoDB  │  Redis   │ClickHouse│ Milvus   │Elasticsea│
   │ (主从)   │ (副本集) │ (哨兵)   │ (集群)   │ (向量)   │rch(搜索) │
   └──────────┴──────────┴──────────┴──────────┴──────────┴──────────┘
                              │
              数据湖 (Iceberg / 对象存储) —— 资产归档 / 血缘
                              │
   ┌──────────────────────────┴──────────────────────────┐
   │ 安全与合规：等保三级 / 全量审计日志 / 合规大脑审核     │
   └─────────────────────────────────────────────────────┘
      外部对接：微信支付 / 短信 / 微信服务通知 / 星耀产业资本（数据输出）
```

- **接入与网关**：Nginx 做 SSL 终止与灰度；API Gateway 统一鉴权、路由、限流（令牌桶）、防重放。
- **业务区**：业务微服务、AI 决策服务、数据中台服务、后台静态资源分属于不同命名空间，Pod 多副本 + HPA 自动扩缩容。
- **数据区**：数据库独立网段，仅允许网关与服务网段访问；敏感库启用 TDE 加密与访问审计。
- **外部依赖**：微信支付 / 短信 / 服务通知经专线或 HTTPS 对接；向星耀产业资本的数据输出须经合规大脑审核服务。

### 10.2 接口规范

- **风格与版本**：RESTful + JSON；路径统一前缀 `/api/v1/{module}/{resource}`，模块含 `ih`（互联网医院）、`mt`（数据中台）、`plat`（平台）。
- **鉴权**：
  - 微信小程序：`wx.login` 获取 `code` → 后端 `code2session` 换 `openid` 并签发 JWT；
  - 后台账号：账号密码 + 图形验证码登录，签发 JWT；
  - 所有接口须带 `Authorization: Bearer <JWT>`；基于 RBAC 控制角色（患者/医师/门店/调理师/平台/星耀）数据权限。
- **统一响应格式**：

  ```json
  {
    "code": 0,
    "message": "success",
    "data": { },
    "timestamp": 1690000000,
    "requestId": "req-xxxx"
  }
  ```

  `code = 0` 表示成功；非 0 为错误码。
- **错误码分段**：`1xxx` 参数校验、`2xxx` 鉴权/权限、`3xxx` 业务规则（如处方待审核）、`4xxx` 合规拦截、`5xxx` 系统异常。
- **安全规则**：
  - 全站 HTTPS/TLS 1.3；敏感字段（身份证号、处方、健康档案）传输加密、存储脱敏（如 `张*`）；
  - 防重放：`timestamp + nonce + sign`（HMAC）校验，5 分钟窗口；
  - 限流：单用户/单 IP 令牌桶，核心交易接口更严格；
  - 幂等：支付、开方等写操作须传 `Idempotency-Key`，避免重复提交；
  - 审计：涉及处方、健康档案、数据资产输出等写操作全量留痕（操作人/时间/前后值）。
- **接口文档**：FastAPI 自动生成 OpenAPI（Swagger UI），并导出 `openapi.yaml` 供前端 Mock 与联调。

### 10.3 关键接口示例

| 接口 | Method / Path | 关键字段 | 说明 |
| ---- | ------------- | -------- | ---- |
| 开具处方 | `POST /api/v1/ih/prescriptions` | `patientId, doctorId, items[], diagnosis, signature` | 医生开方，状态置 `pending_audit` |
| 录入治疗记录 | `POST /api/v1/mt/treatment-records` | `customerId, storeId, therapistId, planId, products[], nps` | 调理师录入，不可删仅可更正留痕 |
| 复购预测 | `GET /api/v1/mt/repurchase-prediction?customerId=` | 返回 `nextVisitProb, repurchaseProb, riskLevel, modelVersion` | AI 服务返回，毫秒级 |
| AI 方案推荐 | `POST /api/v1/mt/plan-recommend` | `customerId, painType` → `top3Plans[], rationale, similarCases` | RAG 输出可解释推荐，仅辅助 |

> 注：所有 AI 类接口返回结果均标注 `isAssist = true`，前端须明示"仅供参考，不替代医师诊断"。

## 11. 数据库表结构设计

### 11.1 分层与命名规范

- **业务库（PostgreSQL）**：按模块前缀 `ih_`（互联网医院）、`mt_`（健康数据中台）、`plat_`（平台）分域；统一字段 `id`（bigint 自增 / uuid）、`created_at`、`updated_at`、`is_deleted`（逻辑删除）。
- **文档库（MongoDB）**：存储结构多变、JSON 密集的数据（治疗记录原始、疼痛评估原始答案）。
- **OLAP（ClickHouse）**：经营指标宽表，支撑看板多维分析。
- **向量库（Milvus）**：方案/案例向量集合，支撑相似检索与 RAG。
- 敏感字段（姓名、手机、身份证、处方）统一**存储脱敏 + 应用层按权限解密**；表级开启 TDE 加密。

### 11.2 核心表结构（PostgreSQL）

**互联网医院 `ih_*`**

| 表 | 关键字段 | 说明 |
| -- | -------- | ---- |
| `ih_user` | `id, openid(UK), real_name_mask, phone_mask, id_card_mask, user_type, created_at` | 患者/用户，实名信息脱敏存储 |
| `ih_doctor` | `id, user_id(FK), license_no(UK), title, hospital_id(FK), dept, good_at, consult_price, status` | 执业医师，执业证书编号唯一 |
| `ih_prescription` | `id, prescription_no(UK), patient_id, doctor_id, pharmacist_id, diagnose, items_json, status(enum), signature_url, created_at, audit_at` | 处方主表，状态 `pending_audit/approved/rejected` |
| `ih_prescription_item` | `id, prescription_id(FK), drug_id, name, spec, dosage, freq, qty` | 处方药品明细 |
| `ih_order` | `id, order_no(UK), user_id, type(enum rx/otc), amount, pay_status, created_at` | 订单，rx 不进购物车单独生成 |

**健康数据中台 `mt_*`**

| 表 | 关键字段 | 说明 |
| -- | -------- | ---- |
| `mt_customer` | `id(customer_id), name_mask, gender, birth_date, phone_mask, source_store_id, health_tags(jsonb), auth_status, auth_file_url, created_at` | 客户健康档案，须授权激活 |
| `mt_pain_assessment` | `id, customer_id(FK), assess_time, scale_type(enum VAS/NRS/McGill), answers_json(jsonb), score(float), pain_site, pain_nature, therapist_id` | 疼痛评估，结果归档画像 |
| `mt_care_plan` | `id(plan_id), customer_id, doctor_advice_id, pain_type, goal, cycle, items_json, product_combo_json, reeval_nodes, status` | 调理方案，须关联医师建议 |
| `mt_treatment_record` | `id(record_id), customer_id, store_id, therapist_id, plan_id, service_time, products_json, oper_sites_json, nps, images_json, remark, created_at` | 治疗记录，**不可删仅可更正留痕** |
| `mt_effect_tracking` | `id, customer_id, plan_id, assess_seq_json, effect_level(enum significant/effective/ineffective/worsened), generated_at` | 效果追踪 |
| `mt_repurchase_prediction` | `id, customer_id, predict_time, next_visit_prob, repurchase_prob, risk_level(enum high/medium/low), model_version` | 复购预测，模型版本可追溯 |
| `mt_store` | `id(store_id), name, region, city, type, status` | 连锁门店 |
| `mt_therapist` | `id(therapist_id), name, license_no, store_id, skill_tags(jsonb), status` | 调理师，持职业资格证 |

**平台 / 数据中台 `plat_*`**

| 表 | 关键字段 | 说明 |
| -- | -------- | ---- |
| `plat_data_asset` | `id(asset_id), name, owner, sensitivity_level(enum L1-L4), usage_scope, quality_score, update_freq, lineage_json` | 数据资产目录 |
| `plat_ai_model` | `id(model_id), name, version, train_dataset_id, algo_type, metrics_json, status, online_at, offline_at` | AI 模型，版本与效果指标 |
| `plat_model_pred_log` | `id, model_id, version, input_features_json, predict_result_json, predict_time, user_id` | 模型预测日志 |
| `plat_audit_log` | `id, actor_id, role, action, resource, before_json, after_json, ip, created_at` | 全量审计留痕 |

### 11.3 多存储协同示例

- **ClickHouse 经营宽表** `mt_store_metrics`：`date, store_id, region, appointment_cnt, arrival_cnt, deal_customers, deal_amount, deal_orders, repurchase_customers, nps_avg`（对应 3.4.2 看板指标，预聚合）。
- **MongoDB 文档**：`treatment_records`（完整 JSON，含操作图片地址、产品组合）、`pain_assessment_raw`（量表原始答案）。
- **Milvus 向量**：`plan_embeddings`（调理方案向量）、`case_embeddings`（相似客户案例向量），支撑方案推荐与 RAG。

> 完整字段定义见 PRD V2.0 附录 5.1 数据字典（17 字段）与 5.2 数据资产生命周期。

## 12. CI/CD 流水线

### 12.1 流水线图

```
开发者 push / Merge Request
        │
        ▼
┌─────────────────── CI 阶段 ───────────────────┐
│ • 代码 Lint (ruff / eslint)                      │
│ • 单元测试 (pytest / jest)                       │
│ • 安全扫描 (SAST + 依赖漏洞 + 密钥检测)          │
│ • 构建镜像 (docker build)                        │
│ • 推送镜像 (Harbor / ACR 私仓)                   │
└───────────────────┬──────────────────────────┘
                    │ 镜像就绪，触发 CD
        ┌───────────┼───────────────────────┐
        ▼           ▼                        ▼
    dev 环境      staging 环境            prod 环境
   自动部署      自动部署 + 自动化回归     人工审批
   (Helm/K8s)    + 等保预检 + 性能压测      │
                 │                        ▼
                 │                  灰度发布(Canary)
                 │                        │ 监控达标
                 │                        ▼
                 │                  全量滚动更新
                 └───────────────────┬──┘
                                     ▼
                          观测: Prometheus + Grafana
                          异常自动告警 → 回滚(Argo Rollouts)
```

### 12.2 环境与发布策略

- **分支模型**：`main`（生产）、`release/*`（预发）、`develop`（集成）、`feature/*`（开发）。
- **环境**：dev（自动）→ staging（自动 + 等保预检/压测）→ prod（审批 + 灰度）。
- **发布方式**：K8s + Helm；生产采用 Canary 灰度（先 5%→25%→100%），Argo Rollouts 按错误率/延迟指标自动回滚。
- **配置与密钥**：配置走 ConfigMap/Secret（KMS 加密），不入库；敏感配置经 Vault 注入。
- **制品追溯**：镜像 tag = `git-sha`，关联提交、流水线、SBOM（软件物料清单），满足等保三级溯源。

### 12.3 机器学习流水线（MLOps）

独立于应用 CI/CD，由数据/算法团队维护：

```
数据采集(门店/工作台) → 特征工程(Feature Store) → 训练(MLflow 实验)
→ 模型注册(版本/指标) → 合规评估(可解释性/偏差) → 审批上线
→ 线上推理服务(FastAPI, 向量缓存) → 效果监控(衰减告警/回滚)
```

- 训练数据、模型权重、评估指标全部入 `plat_ai_model` 与数据湖，版本与血缘可追溯（对应 3.6.3 / 5.2）。
- 模型上线须经合规大脑对"可解释性 + 不替代医师"的校验。

## 13. 安全合规检查表（等保三级逐项）

### 13.1 总体说明

- 依据 **GB/T 22239-2019《信息安全技术 网络安全等级保护基本要求》第三级**（等保 2.0 三级）。
- 覆盖 10 个控制类：安全物理环境、通信网络、区域边界、计算环境、管理中心、管理制度、管理机构、管理人员、建设管理、运维管理。
- 医疗健康系统叠加行业合规：《个人信息保护法》《数据安全法》《健康医疗大数据管理办法（试行）》《互联网医院管理办法（试行）》《处方管理办法》《生成式人工智能服务管理暂行办法》。
- 状态列：**已规划**=架构已设计、**建设中**=实施中、**待测评**=需第三方等保测评确认。

### 13.2 等保三级逐项检查表

| 控制类 | 关键控制点 | 等保三级核心要求 | 平台对应措施 | 状态 |
| ------ | ---------- | ---------------- | ------------ | ---- |
| 安全物理环境 | 物理访问控制/防灾害 | 机房分区出入管控、防火防水防雷、冗余供电 | 采用云厂商等保机房（物理环境由云服务商承担，需索取测评证明） | 已规划 |
| 安全通信网络 | 网络架构 | 划分关键网段、带宽冗余、关键设备冗余 | K8s 多可用区、DMZ/业务区/数据区逻辑隔离 | 已规划 |
| 安全通信网络 | 通信传输 | 采用密码技术保证通信保密性/完整性 | 全站 HTTPS/TLS 1.3；API Gateway 终止 SSL | 已规划 |
| 安全通信网络 | 可信验证 | 边界设备系统程序可信验证 | 云厂商裸金属/可信启动（依赖云侧） | 待测评 |
| 安全区域边界 | 边界防护/访问控制 | 跨边界访问受控、默认拒绝、白名单 | API Gateway + Nginx 统一出入站、WAF、防 SQL 注入/XSS/CSRF | 已规划 |
| 安全区域边界 | 入侵防范 | 检测/防御外部与内部攻击、恶意代码 | WAF + DDoS 防护 + HIDS；依赖漏洞扫描 | 建设中 |
| 安全计算环境 | 身份鉴别 | 双因子/多因子、登录失败处理、远程防窃听 | JWT + 微信 `code2session`；后台账号+验证码；失败锁定；TLS 传输 | 已规划 |
| 安全计算环境 | 访问控制 | 默认拒绝、权限分离、最小授权 | RBAC 六角色（患者/医师/门店/调理师/平台/星耀）；库表级权限 | 已规划 |
| 安全计算环境 | 安全审计 | 审计覆盖、记录保护、集中分析 | `plat_audit_log` 全量留痕；审计日志独立存储防篡改 | 已规划 |
| 安全计算环境 | 入侵防范 | 最小化安装、关闭无用端口/服务 | 容器最小化镜像、NetworkPolicy 仅开放必要端口 | 建设中 |
| 安全计算环境 | 数据完整性 | 传输/存储完整性校验 | TLS 完整性；PG 校验和 + 数据湖 Iceberg 版本校验 | 已规划 |
| 安全计算环境 | 数据保密性 | 传输/存储保密性 | TLS 传输加密；PG/Mongo **TDE 加密**；敏感字段**存储脱敏**（如 `张*`） | 已规划 |
| 安全计算环境 | 数据备份恢复 | 本地+异地备份、定期恢复验证 | PG 主从 + 定时备份；数据湖冷归档；季度恢复演练 | 建设中 |
| 安全计算环境 | 剩余信息保护 | 退出清除、存储释放清除 | 会话超时销毁；加密擦除废弃介质 | 已规划 |
| 安全计算环境 | 个人信息保护 | 采集最小必要、授权、去标识化 | 最小必要采集 + 授权书电子签名；训练/输出数据脱敏去标识（5.2） | 已规划 |
| 安全管理中心 | 集中管控 | 系统/审计/安全管理三权分立、集中审计告警 | 合规大脑统一管控；审计/告警/运维集中（Prometheus+Grafana） | 已规划 |
| 安全管理制度 | 策略/制度/规程 | 发布安全策略、管理制度、操作规程 | 制定《数据安全管理制度》《AI 伦理与合规准则》等 | 待测评 |
| 安全管理机构 | 岗位/审批/审核 | 设立专职安全岗、授权审批、定期审核 | 设立安全合规负责人 + 星耀资本合规对接 | 待测评 |
| 安全管理人员 | 录用/培训/离岗 | 背景审查、定期培训、离岗权限回收 | 入职背调、年度安全培训、离职自动销权 | 待测评 |
| 安全建设管理 | 定级备案/测评 | 完成定级备案、上线前等保测评 | 上线前完成等保三级测评与渗透测试、代码审计 | 待测评 |
| 安全运维管理 | 漏洞/变更/应急 | 漏洞与补丁管理、变更审批、应急预案演练 | 依赖扫描+季度漏扫；变更评审；年度应急演练 | 建设中 |

### 13.3 医疗行业附加合规

| 法规/要求 | 关键条款 | 平台措施 |
| --------- | -------- | -------- |
| 《互联网医院管理办法》 | 仅限复诊、医师资质、处方审核 | 复诊合规确认弹窗（3.1.2）；医师执业证书校验；药师审核（3.1.4） |
| 《处方管理办法》 | 电子处方签名、可追溯 | 医生电子签名 `signature_url`；处方状态全程留痕 |
| 《个人信息保护法》 | 最小必要、单独同意、出境评估 | 授权书电子签名；跨境/对外输出走合规大脑+协议（5.2） |
| 《数据安全法》/《健康医疗大数据管理办法》 | 分类分级、重要数据管控 | 数据资产敏感等级 L1–L4；数据湖分级存储与血缘 |
| 《生成式人工智能服务管理暂行办法》 | 内容合规、可解释、不替代专业判断 | AI 输出标注 `isAssist=true`；RAG 可解释；上线经合规大脑校验 |

### 13.4 上线前合规自查计划

1. **定级备案**：向公安机关完成等保三级定级与备案。
2. **差距测评**：委托具备资质的测评机构开展等保三级测评，输出整改项清单。
3. **安全测试**：第三方渗透测试 + 代码安全审计（SAST/DAST）+ 依赖成分分析（SCA）。
4. **数据治理**：完成数据分类分级清单、脱敏规则、数据资产目录（对应 3.6.2 / 5.1）。
5. **应急演练**：数据安全事件应急预案 + 年度实战演练。
6. **持续合规**：安全运营中心（SOC）常态化监测，模型/数据季度合规复审。

## 14. 第三方服务对接清单（微信支付 / 短信）

### 14.1 对接总览

平台在交易、通知、身份三个维度依赖外部第三方服务，统一经「API Gateway → 第三方网关代理」对外调用，密钥/证书集中托管于 KMS/Vault，禁止硬编码与入库。

| 类别 | 服务 | 用途 | 推荐服务商 | 合规要点 |
| ---- | ---- | ---- | ---------- | -------- |
| 交易 | 微信支付（JSAPI） | 处方/OTC 商城收款、退款 | 微信支付商户平台 | 商户须持《医疗机构执业许可证》；处方药资质单独报备 |
| 通知 | 短信 | 验证码、订单/复诊通知、营销 | 阿里云/腾讯云短信 | 签名+模板备案；营销短信须带退订方式 |
| 通知 | 微信订阅消息 | 服务通知（复诊提醒、开方结果） | 微信公众平台 | 须用户主动订阅，尊重退订 |
| 身份 | 微信登录 | `code2session` 换 `openid` | 微信开放平台 | 仅取必要用户信息，授权最小化 |
| 存储 | 对象存储 | 处方/评估单图片、电子签名文件 | 阿里云 OSS / 腾讯云 COS | 私有桶 + 临时签名 URL |
| 可选 | 实名/地图/电子签 | 身份核验、门店定位、合同签署 | 权威实名接口 / 高德 / 法大大 | 仅用于必要核验，数据最小化 |

### 14.2 微信支付对接清单

**接入要素**

- **支付场景**：小程序内 JSAPI 支付（`trade_type=JSAPI`），需用户 `openid`；H5/PC 场景另用 MWEB/NATIVE。
- **商户主体**：互联网医院主体须具备《医疗机构执业许可证》，并以合规经营主体申请微信支付商户号（MCHID）；处方药销售类目需单独资质审核。
- **证书与密钥**：APIv3 密钥、商户 API 证书（apiclient_key.pem）、平台证书（自动刷新），统一托管 KMS。

**核心接口**

| 接口 | 方向 | 关键字段 | 说明 |
| ---- | ---- | -------- | ---- |
| 统一下单 | 平台→微信 | `appid, mchid, description, out_trade_no(UK), amount(total), payer(openid), notify_url` | 生成预支付 `prepay_id`，前端 `wx.requestPayment` 拉起 |
| 支付结果通知 | 微信→平台 | `out_trade_no, transaction_id, trade_state, success_time` | 异步回调，**须验签 + 幂等** |
| 订单查询 | 平台→微信 | `out_trade_no / transaction_id` | 补偿对账用 |
| 申请退款 | 平台→微信 | `out_trade_no, out_refund_no(UK), amount` | 退方/退单；退款原路返回 |
| 退款结果通知 | 微信→平台 | `refund_id, refund_status` | 异步回调，须验签 |
| 账单下载 | 平台→微信 | `bill_date, bill_type` | 每日对账 |

**关键设计**

- **幂等**：以 `out_trade_no`（= 平台 `order_no`）为主键，支付/退款回调经 `Idempotency-Key` 防重复入账；回调处理前先查库确认未支付。
- **回调安全**：验签采用平台证书 + APIv3 密钥解密报文；失败返回非成功，微信按策略重试（指数退避）。
- **对账**：每日定时拉取微信账单，与本地 `ih_payment` 流水逐笔比对，差异告警。
- **合规**：处方药订单支付须与处方审核状态（`ih_prescription.status=approved`）强关联；医保对接（如后续打通）须独立结算通道。

**关联表结构（PostgreSQL）**

- `ih_payment`：`id, order_no(FK), transaction_id, mch_id, amount, currency, pay_channel(enum wechat), trade_state, refund_status, prepay_id, paid_at, notify_raw_json, created_at`

> 注：本期**医保支付暂不接入**，处方药结算仅走微信支付；后续若打通医保须独立结算通道与医保电子凭证对接（详见 15.1）。

### 14.3 短信服务对接清单

**用途与分级**

| 场景 | 类型 | 触发 | 频率控制 |
| ---- | ---- | ---- | -------- |
| 登录/绑定手机 | 验证码 | 用户操作 | 单手机号 1 条/60s，日上限 5 条 |
| 订单通知 | 交易提醒 | 支付/发货 | 事件触发单次 |
| 复诊/回访提醒 | 服务通知 | 调度任务 | 按预约时间推送 |
| 营销活动 | 推广 | 运营投放 | 限时段（9:00–21:00），日上限 1 条 |

**接入要素**

- **服务商与资质**：阿里云/腾讯云短信，完成**短信签名 + 模板**双备案；医疗类营销内容须规避夸大疗效表述。
- **接口**：`SendSms`（发送）、`QuerySendDetails`（回执查询）、上行/状态报告回调（失败/退订）。

**关键设计**

- **防刷**：验证码发送前置图形/滑块验证码；同 IP/手机号限流（令牌桶）；验证码服务端存储 Redis（5 分钟过期），错误次数封顶。
- **退订合规**：营销短信正文含「回 T 退订」等退订指令，上行退订写入黑名单，禁止再次触达（依据《通信短信息服务管理规定》）。
- **内容合规**：不得发送诊疗结论、处方明细等敏感健康信息；通知类与营销类签名/模板严格分离。
- **可观测**：发送量、到达率、退订率指标入 Prometheus，异常下跌告警。

**关联表结构（PostgreSQL）**

- `ih_sms_log`：`id, phone_mask, scene(enum), template_code, content_mask, status(enum sending/success/failed), provider, send_at, receipt_at, error_msg`

### 14.4 其他第三方服务（微信服务通知 / 登录 / 存储）

- **微信订阅消息**：小程序 `subscribeMessage` 一次性订阅，发送复诊提醒、开方结果；模板 ID 在微信后台配置，须用户先订阅，退订即停。
- **微信登录**：`wx.login` → `code2session` 换 `openid`/`unionid`，仅用于账号绑定，不额外采集微信资料。
- **对象存储 OSS/COS**：处方图片、评估单、电子签名文件存私有桶，访问经后端签发临时 URL（STS/临时签名），过期失效，避免敏感文件直链泄露。

> 注：前端统一以**微信小程序（Taro）**承接，**不单独开发独立 App**；多端需求由 Taro 一套代码编译微信/可能的其他小程序端满足。

### 14.5 第三方对接统一规范

| 维度 | 规范 |
| ---- | ---- |
| 密钥管理 | 证书/密钥统一 KMS/Vault 托管，运行时注入 Secret，禁止入库与前端下发 |
| 超时重试 | 调用设超时（支付 3s/短信 2s），失败指数退避重试，避免雪崩 |
| 幂等 | 交易类以平台单号为主键 + `Idempotency-Key`，回调去重入账 |
| 熔断降级 | 第三方异常时熔断（如短信失败降级站内信），保证主链路（问诊/开方）可用 |
| 对账与审计 | 每日自动对账；第三方交互入 `plat_audit_log` 与业务流水，全量留痕 |
| 合规 | 外部传输加密（TLS）；医疗敏感数据不经第三方明文；对外数据输出走**隐私计算/联邦学习**，原始不出域（15.3）；营销触达尊重退订 |

## 15. 优化补充设计

### 15.1 处方审核：内部 + 外部合理用药引擎双轨

- **双轨审核**：① 平台内部药师人工审核（`ih_prescription.status`：`pending_audit → approved/rejected`）；② 调用**外部合理用药引擎**（如美康/逸曜）做配伍禁忌、剂量、适应症预审，结果作为药师辅助判据，不替代人工结论。
- **外部对接**：审核服务经 API Gateway 调用，输入诊断 + 药品明细，返回`相互作用 / 超量 / 禁忌`风险等级；外部不可用时**不阻塞主链路**，降级为仅内部审核 + 告警。
- **HIS 回传**：与协作实体医院 HIS 通过标准接口（HL7 FHIR / 视图 / API）同步就诊与审核结果，支撑复诊连续性（对应 3.1.2 复诊确认）。

### 15.2 门店行级数据隔离（RLS）

- 多门店 B2B2C 场景，在 RBAC 角色之外补充**行级数据权限**：门店管理员 / 调理师仅可见 `store_id` 命中的客户与记录。
- **实现**：PostgreSQL 行级安全策略（Row-Level Security）+ 会话注入 `current_store_id`；API 层数据权限中间件统一追加 `store_id` 过滤，杜绝越权查询。对应 `mt_customer` / `mt_treatment_record` / `mt_store_metrics` 均带 `store_id`。

### 15.3 隐私计算 / 联邦学习对外输出（原始不出域）

- 向星耀产业资本等外部主体的数据输出，不走原始数据搬运，采用**隐私计算**（联邦学习 / 多方安全计算 / 差分隐私）在可信执行环境（TEE）内联合建模，仅输出**模型参数 / 聚合指标**，原始治疗数据不出域。
- **实现**：合规大脑编排联邦任务，各参与方本地训练、仅交换加密梯度；对外交付物经数据脱敏 + 差分隐私加噪，满足《个保法》"最小必要 + 去标识化"。对应 5.2 数据资产对外输出。

### 15.4 AI 反馈闭环（human-in-the-loop）

- 医生对 AI 方案推荐 / 风险画像的"采纳 / 驳回 / 修正"回流至 `plat_model_pred_log`（新增 `adopted enum`、修正后标签），形成标注数据集。
- **闭环**：反馈 → 特征重标注 → 模型再训练（12.3 MLOps）→ 效果指标（采纳率 / 命中率）纳入看板；以数据资产壁垒驱动模型持续迭代。

### 15.5 可观测性补全（指标 / 链路 / 日志）

- **指标**：Prometheus + Grafana（已有）。
- **链路追踪**：OpenTelemetry 注入 `TraceID`，跨「小程序 → 网关 → 微服务 → AI 推理 → DB」全链路串联，Jaeger 存储。
- **日志集中**：Loki / ELK 收集结构化日志，与 `TraceID` 关联；支付 / 开方等关键链路全量留痕。

### 15.6 缓存一致性策略

- Redis 采用 **Cache-Aside（旁路缓存）**：读穿透、写时先更库再删缓存；处方 / 订单状态变更以"删除缓存 + 短 TTL + 延迟双删"防脏读。
- 缓存仅存非敏感派生数据（如看板预聚合），敏感字段以 PG 为唯一真相源。

### 15.7 搜索库 CDC 同步

- Elasticsearch 全文检索（找医生 / 找药品）与 PG 主库通过 **CDC（Debezium / Canal）** 捕获 binlog 准实时同步，避免双写不一致；同步链路异常降级为定时全量重建。

### 15.8 第三方双通道容灾

- **短信**：主（阿里云）+ 备（腾讯云）双通道，主通道失败自动切换；营销类限主通道、关键通知（验证码 / 支付）双通道保障可达。
- **支付**：微信支付为主，预留聚合支付（如后续接入银联 / 支付宝 H5）作为兜底；第三方异常触发熔断（14.5），主链路（问诊 / 开方）不降级。

### 15.9 上线前置合规许可清单

| 许可 / 备案 | 主管部门 | 说明 |
| ----------- | -------- | ---- |
| 《互联网医院执业许可证》 | 卫健部门 | 开展在线复诊 / 处方的前置资质 |
| 《ICP 备案》+《EDI 许可证》 | 通信管理部门 | 在线数据处理与交易处理业务 |
| 《药品经营许可证》 | 药监部门 | 若自营 OTC / 处方药零售 |
| 微信支付商户资质 + 处方药类目报备 | 微信支付 | 对应 14.2 |
| 等保三级定级备案与测评 | 公安 + 测评机构 | 对应 13.4 |
| 数据安全 / 个人信息保护影响评估（PIA） | 内部合规 | 上线前完成 |
