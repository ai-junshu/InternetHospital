**互联网医疗中心平台产品需求文档（PRD V2.0）**

## **文档版本历史**

| 版本号 | 修订日期   | 修订人 | 修订说明                                                                                                                                                                                                                                            |
| ------ | ---------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1.0   | 2023-10-27 | AI     | 初始版本创建，包含核心功能需求                                                                                                                                                                                                                      |
| V2.0   | 2026-07-24 | AI     | 整体重构：确立"互联网医院 + 健康数据中台"双主线架构；新增 B2B2C 连锁门店赋能业务；新增连锁门店管理员、调理师/健康顾问、星耀产业资本/数据中台运营三类角色；完整展开健康数据中台四大模块（客户健康档案管理、门店经营数据看板、治疗数据沉淀、AI辅助决策）；补充数据治理与数据资产生命周期管理。 |

## **1. 引言**

### **1.1 产品概述**

**产品名称**：互联网医疗中心平台

**产品定位**：以"**互联网医院 + 健康数据中台**"双轮驱动的合规互联网医疗与健康数据服务平台。

- **主线一 · 互联网医院**：以公立医院为核心主体，严格遵循国家法律法规，集在线复诊、健康管理、电子处方、药品销售于一体，通过微信小程序为医患提供便捷 2C 服务，并通过销售品牌药品及健康服务包实现盈利。
- **主线二 · 健康数据中台**：依托**星耀产业资本健康产业赋能服务投资平台**的"**合规大脑 + 医疗 AI 数据中台**"能力，为连锁门店（聚焦疼痛管理/调理领域）提供数字化基础设施，包括客户健康档案管理、门店经营数据看板、治疗数据沉淀、AI 辅助决策四大模块。

**战略价值**：数据中台的核心价值不仅是运营效率提升，更是第二阶段产品公司估值的"**数据资产**"——连续、结构化的治疗效果数据，是区别于普通消费品公司的核心壁垒。

### **1.2 目标用户**

**互联网医院主线**：
- **患者/亚健康人群 (2C)**：常见病、慢性病复诊患者，有健康管理需求的亚健康人群。
- **执业医师 (2B)**：合作公立医院的执业医师，愿意提供线上服务的医生。
- **医院管理员 (2B)**：合作医院内负责管理互联网医院业务的管理人员。

**健康数据中台主线**：
- **连锁门店管理员 (2B)**：连锁门店店长/督导，负责门店经营数据看板监控、客户健康档案管理、调理师调配。
- **调理师/健康顾问 (2B)**：门店内非医师服务人员，承担疼痛评估、调理方案落地执行、客户依从性管理。

**平台运营**：
- **平台运营管理员 (2G)**：平台方的运营、审核、监管人员。
- **星耀产业资本/数据中台运营 (2G)**：负责合规大脑运营、AI 模型管理、数据资产治理与对外输出（含融资估值数据支撑）。

### **1.3 参考资料**

- 《互联网诊疗管理办法（试行）》
- 《互联网医院管理办法（试行）》
- 《药品网络销售监督管理办法》
- 《处方管理办法》
- 《中华人民共和国个人信息保护法》
- 《中华人民共和国数据安全法》
- 《健康医疗大数据管理办法》
- 《生成式人工智能服务管理暂行办法》

## **2. 总体概述**

### **2.1 产品结构图**

```
互联网医疗中心平台
├── 互联网医院主线
│   ├── 用户端小程序 (2C)
│   │   ├── 首页 (找医生、找产品、直播、去开药)
│   │   ├── 健康管理 (健康医典、健康计划、慢病管理)
│   │   ├── 我的医生
│   │   ├── 消息中心
│   │   └── 个人中心 (含代理中心入口)
│   ├── 医生端小程序 (2B)
│   │   ├── 工作台
│   │   ├── 患者管理
│   │   ├── 医学圈
│   │   └── 我的
│   └── 互联网医院管理后台 (2B)
│       ├── 医院管理
│       ├── 业务模块
│       └── 运营数据
├── 健康数据中台主线
│   ├── 连锁门店管理后台 (2B)
│   │   ├── 客户健康档案管理
│   │   ├── 门店经营数据看板
│   │   ├── 调理师调配
│   │   └── 治疗记录查询
│   ├── 调理师/健康顾问工作台 (2B)
│   │   ├── 客户管理
│   │   ├── 调理方案制定与执行
│   │   ├── 治疗记录录入
│   │   └── 依从性管理
│   └── 健康数据中台管理后台 (2G)
│       ├── 合规大脑
│       ├── 治疗数据沉淀
│       ├── AI 辅助决策
│       └── 数据资产管理
└── 平台管理后台 (2G)
    ├── 互联网医院监管
    ├── 产品销售监管
    └── 数据资产看板
```

### **2.2 全局规则与限制**

1.  **合规性规则**：
    - 所有用户、医生、调理师必须完成**实名认证**；调理师需持相关职业资格证书。
    - 所有在线问诊服务必须在页面显著位置提示"**本服务仅适用于复诊患者**"，并要求用户确认。
    - 处方药必须凭电子处方销售，处方需经**执业药师审核**后方可生效。
    - 调理师**不得**进行诊断、开方、侵入性治疗等医疗行为；调理方案需基于执业医师出具的方案建议执行。
    - AI 辅助决策输出仅为**辅助参考**，不替代医师诊断与处方权。
2.  **数据安全**：
    - 敏感信息（病历、处方、身份证号、健康档案、治疗记录）在传输和存储时必须**加密**，显示时必须**脱敏**。
    - 所有问诊记录、电子处方及相关日志必须**保存不少于 15 年**。
    - 治疗数据、客户健康档案作为**数据资产**，须按数据分级分类管理，对外输出须经合规审核与脱敏处理。
3.  **数据治理规则**：
    - 数据采集遵循"**最小必要**"原则，明确告知用户数据用途并取得授权。
    - 数据资产实行**全生命周期管理**：采集→清洗→存储→分析→输出→归档/销毁，各环节留痕可审计。
    - AI 模型训练数据须可追溯，模型版本与训练数据集须归档管理。
    - 跨主体数据共享（如向星耀产业资本、融资评估机构输出）须签订数据合作协议，明确数据范围与用途。
4.  **交互规则**：
    - 所有支付流程通过**微信支付**完成。
    - 主要消息通知通过**微信服务通知**推送。

## **3. 功能需求详述**

### Part A：互联网医院主线

#### **3.1 用户端小程序 (2C)**

##### **3.1.1 首页**

**功能描述**：用户打开小程序后看到的第一个页面，集成核心服务入口和内容推荐。

**子功能点**：
1.  **顶部搜索栏**
    - 可搜索医生、药品名称、疾病症状。
2.  **ICON 导航区**
    - 【找医生】：跳转至医生列表页。
    - 【找产品】：跳转至药品商城页。
    - 【去开药】：跳转至申请开药流程。
    - 【直播中心】：跳转至直播列表页。
    - 【健康医典】：跳转至健康知识库。
3.  **推荐板块**
    - **医生推荐**：根据用户地理位置、历史浏览等因素推荐医生。
    - **药品推荐**：展示"爆品专区"、"品牌专区"的药品。
    - **内容推荐**：展示健康科普文章、视频。

##### **3.1.2 找医生**

**功能描述**：用户可按科室、疾病查找医生，并查看医生详情购买服务。

**子功能点**：
1.  **科室列表**
    - 展示与合作医院一致的科室结构（如：内科->心血管内科）。
    - 支持按科室筛选医生。
2.  **医生列表**
    - 显示医生头像、姓名、职称、医院、科室、擅长、好评率、服务价格。
    - 支持按"评分"、"接诊量"、"距离"排序。
3.  **医生详情页**
    - **基本信息区**：展示医生资质（含执业证书编号缩略图，点击可查看大图）、简介、擅长、服务满意度。
    - **服务项目区**：清晰列明服务类型与价格（如：图文问诊￥X，电话问诊￥Y，服务包￥Z）。
    - **服务须知**：折叠面板，内容包含"服务范围"、"注意事项"（仅限复诊）、预计回复时间。
    - **评价区**：展示用户评价列表。
    - **浮动按钮**：【图文问诊】，点击后弹出合规确认弹窗。
4.  **合规确认弹窗**
    - **文案**："根据国家规定，互联网诊疗仅适用于复诊患者。请您确认已在线下医院有过就诊记录。"
    - **选项**：【取消】、【确认并继续】。
    - **规则**：用户必须点击"确认并继续"方可支付问诊费并发起问诊。

##### **3.1.3 找产品（药品商城）**

**功能描述**：展示和销售药品、保健品的核心页面。

**子功能点**：
1.  **商品分类**
    - 按功效（感冒用药、肠胃用药）、按品牌、按专区（爆品、OTC、保健品）分类。
2.  **商品列表**
    - 商品卡片显示：商品图、名称、规格、价格、会员价、标签（"Rx处方药"、"OTC"）。
3.  **商品详情页**
    - **通用信息**：商品名称、批准文号、生产企业、规格、用法用量、不良反应、禁忌、注意事项。
    - **差异化购买逻辑**：
        - **处方药**：突出显示"处方药，凭处方购买"提示。**购买按钮为"申请开药"**，点击后跳转至复诊流程。
        - **OTC/保健品**：购买按钮为"立即购买/加入购物车"。保健品需展示"本品不能替代药物"提示。
4.  **购物车与订单**
    - OTC/保健品可加入购物车，统一结算。
    - 处方药**不能**加入购物车，必须通过"申请开药"流程单独生成订单。

##### **3.1.4 去开药（复诊开药流程）**

**功能描述**：复诊患者开具处方的专用流程。

**主流程**：
1.  用户从首页或处方药详情页点击"**申请开药**"。
2.  系统引导用户**选择历史就诊记录**或**描述病情、上传历史病历/处方照片**。
3.  系统**推荐科室医生**或用户自行选择医生。
4.  支付问诊费，进入**问诊聊天室**。
5.  医生问诊，点击"**开具处方**"。
6.  医生在处方页面选择药品、填写用法用量，系统提供**药品禁忌提示**。
7.  医生**电子签名**提交处方。处方状态变为"**待药师审核**"。
8.  合作药房的**执业药师**审核处方。
    - **审核通过**：用户收到通知，支付药费，药房发货。
    - **审核驳回**：药师填写驳回理由，通知医生和用户。医生可修改重开。
9.  用户可在"我的处方"中查看所有处方记录及状态。

**数据需求**：
- 处方数据表需包含字段：处方 ID、患者 ID、医生 ID、药师 ID、开具时间、审核时间、药品清单（JSON）、用法用量、诊断信息、处方状态、电子签名文件地址。

##### **3.1.5 个人中心/代理中心**

**功能描述**：管理用户个人信息、订单，并提供推广功能。

**子功能点**：
1.  **我的问诊/处方/订单**：查看所有历史记录和状态。
2.  **我的分享码**：
    - 用户可生成专属分享码。
    - 被推荐人通过此码注册并消费，推荐人可获得**积分或现金奖励**。
    - **规则**：奖励仅来源于**直接推荐用户的首单消费**，奖励金额有上限。
3.  **推广业绩**：
    - 展示"**我的推广**"数据，如：直接推荐人数、成功订单数、可获得奖励总额。
    - **严禁**展示"下级成员"、"多级团队"等涉传信息。

#### **3.2 医生端小程序 (2B)**

##### **3.2.1 工作台**

**功能描述**：医生处理问诊工作的主界面。

**子功能点**：
1.  **今日数据**：显示待处理问诊、今日接诊量、累计接诊量、今日收入。
2.  **问诊列表**：以会话列表形式展示所有进行中、待处理的问诊。
3.  **问诊聊天室**：
    - 可与患者进行图文、语音沟通。
    - 内置功能按钮：【病历模板】、【开具处方】、【结束问诊】。
    - **开具处方**：跳转至处方页面，药品从平台目录中选择，填写后电子签名提交。
4.  **我的排班**：设置自己可接受问诊的时间段。

#### **3.3 互联网医院管理后台 (2B)**

##### **3.3.1 医院管理**

**功能描述**：合作医院管理自身互联网医院业务。

**子功能点**：
1.  **科室与医生管理**：维护本院科室结构、医生上下线、医生服务定价。
2.  **处方审核配置**：配置本院执业药师排班与审核规则。
3.  **合作药房管理**：维护对接的实体药房信息、库存同步规则。

##### **3.3.2 运营数据**

**功能描述**：医院层面查看本院互联网医院运营情况。

**子功能点**：
1.  **业务总览**：本院在线医生数、今日问诊数、今日处方数、处方审核通过率。
2.  **异常预警**：处方异常、行为异常的院内处理流转。

### Part B：健康数据中台主线

#### **3.4 连锁门店管理后台 (2B)**

##### **3.4.1 客户健康档案管理**

**功能描述**：为连锁门店客户提供完整的健康档案管理，覆盖疼痛评估、治疗记录、效果追踪、复购预测全链路，是数据中台的源头数据采集入口。

**子功能点**：
1.  **客户档案建立**
    - 客户首次到店或线上注册时建立基础档案，包含：客户 ID、姓名（脱敏）、性别、年龄、联系方式（脱敏）、来源门店、注册时间、健康标签。
    - 支持扫码录入（身份证/微信授权）与手工录入两种方式。
    - **规则**：必须取得客户《健康档案授权书》电子签名后方可激活档案。
2.  **疼痛评估**
    - **评估量表**：内置标准化疼痛评估工具，包括 VAS 视觉模拟量表、NRS 数字评分量表、McGill 疼痛问卷、部位图标记。
    - **评估流程**：调理师引导客户完成量表→系统自动计算评分→生成疼痛画像（部位/性质/强度/频率/持续时间）→归档至客户档案。
    - **复评机制**：支持按调理方案周期自动触发复评提醒，记录每次评估变化曲线。
3.  **治疗记录**
    - 记录每次调理服务的完整信息：服务时间、门店、调理师、调理方案 ID、使用产品组合、操作部位、客户反馈（即时 NPS）。
    - 支持拍照上传（操作前后对比图，需客户授权）。
    - **规则**：治疗记录一经提交不可删除，仅可补充更正并留痕。
4.  **效果追踪**
    - 基于多次疼痛评估与治疗记录，自动生成**效果趋势图**（疼痛评分随时间变化、调理次数累计效果）。
    - 支持按客户/门店/调理方案维度查看效果分布。
    - **效果等级**：系统自动判定"显效/有效/无效/恶化"四档，触发不同后续动作（如无效则推荐升级方案或建议就医）。
5.  **复购预测**
    - 基于客户档案、治疗记录、效果数据、消费行为的机器学习模型，预测客户**下次到店概率**与**复购概率**。
    - 输出**复购风险名单**：高/中/低风险客户分层，推送至调理师工作台进行针对性跟进。
    - **规则**：预测模型版本与训练数据集须归档可追溯。

**数据需求**：
- 客户档案表：客户 ID、姓名脱敏、性别、出生年月、联系方式脱敏、来源门店 ID、注册时间、健康标签 JSON、授权状态、授权文件地址。
- 疼痛评估表：评估 ID、客户 ID、评估时间、评估量表类型、量表原始答案 JSON、综合评分、疼痛部位、疼痛性质、评估师 ID。
- 治疗记录表：记录 ID、客户 ID、服务时间、门店 ID、调理师 ID、调理方案 ID、产品组合 JSON、操作部位 JSON、客户 NPS、操作图片地址 JSON、备注。
- 效果追踪表：追踪 ID、客户 ID、调理方案 ID、评估序列 JSON、效果等级、生成时间。
- 复购预测表：预测 ID、客户 ID、预测时间、下次到店概率、复购概率、风险等级、模型版本。

##### **3.4.2 门店经营数据看板**

**功能描述**：连锁门店管理员实时监控门店经营核心指标，支撑运营决策与门店横向对比。

**子功能点**：
1.  **核心指标实时监控**
    - **到店率**：预约到店数 / 预约总数，按日/周/月维度展示趋势。
    - **转化率**：成交客户数 / 到店客户数，区分首单转化与复购转化。
    - **客单价**：成交总额 / 成交单数，支持按调理方案/产品组合拆分。
    - **复购率**：复购客户数 / 成交客户数，按 30/60/90 天滚动窗口统计。
    - **NPS**：净推荐值，基于每次服务后即时调研数据计算，按门店/调理师维度排名。
2.  **门店横向对比**
    - 多门店经营指标横向排名，支持按区域/城市/门店类型筛选。
    - 异常门店自动标红（如某门店到店率连续 7 天低于均值 20%）。
3.  **调理师绩效看板**
    - 调理师服务量、客户满意度、复购贡献、效果达成率排名。
    - 支持按门店/时间段筛选，绩效数据可导出用于薪酬核算。
4.  **经营预警**
    - 客流异常下滑、转化率骤降、NPS 低分门店、复购率持续走低等预警自动推送至门店管理员。
    - 预警支持配置阈值与推送渠道（站内信、企业微信）。

**数据需求**：
- 经营指标宽表：日期、门店 ID、区域、预约数、到店数、成交客户数、成交总额、成交单数、复购客户数、NPS 均值、调理师数。
- 预警事件表：预警 ID、门店 ID、预警类型、预警阈值、实际值、触发时间、处理状态、处理人。

##### **3.4.3 调理师调配**

**功能描述**：门店管理员对店内调理师进行排班、客户分配、能力调配。

**子功能点**：
1.  **排班管理**：调理师排班表，支持跨门店借调。
2.  **客户分配**：将客户分配至专属调理师，支持客户偏好（性别/资历）匹配。
3.  **能力标签**：调理师能力标签维护（擅长疼痛类型、调理手法、服务年限），支撑智能分配。

#### **3.5 调理师/健康顾问工作台 (2B)**

##### **3.5.1 客户管理**

**功能描述**：调理师管理自己负责的客户列表与档案。

**子功能点**：
1.  **我的客户**：按"今日到店/复购风险/效果追踪中"等视图分类展示。
2.  **客户详情**：查看客户健康档案、疼痛评估历史、治疗记录、效果趋势、复购预测结果。
3.  **客户跟进**：复评提醒、复购跟进任务、依从性提醒，任务支持企业微信外呼。

##### **3.5.2 调理方案制定与执行**

**功能描述**：调理师基于医师方案建议与客户画像，制定并执行个性化调理方案。

**子功能点**：
1.  **方案制定**
    - 选择疼痛类型、调理目标、调理周期、单次服务项目、产品组合、复评节点。
    - **规则**：调理方案须关联医师出具的方案建议 ID；调理师不得自行出具医疗方案。
    - 支持 AI 辅助决策模块推荐方案（见 3.6.3）。
2.  **方案执行**
    - 每次服务前确认客户状态（疼痛评分变化、禁忌排查）。
    - 服务中按方案步骤执行，记录产品用量与操作部位。
    - 服务后引导客户完成即时 NPS 与复评。
3.  **方案调整**
    - 基于效果追踪数据，调理师可申请方案调整，需门店管理员或医师审核后生效。

##### **3.5.3 治疗记录录入**

**功能描述**：调理师录入每次服务的治疗记录，作为数据中台核心数据源。

**子功能点**：
1.  **快速录入模板**：按调理方案预填默认值，调理师仅需修改变量字段。
2.  **拍照上传**：操作前后对比图、产品包装图，自动加水印（时间/门店/调理师）。
3.  **提交校验**：必填字段校验、产品用量合理性校验、客户授权状态校验。

##### **3.5.4 依从性管理**

**功能描述**：跟踪客户调理方案执行情况，提升依从性。

**子功能点**：
1.  **依从性评分**：基于到店率、复评完成率、家庭作业完成度等维度自动计算。
2.  **依从性提醒**：低依从性客户自动推送提醒（短信/微信/企业微信外呼）。
3.  **依从性报告**：周期性生成客户依从性报告，作为方案调整依据。

#### **3.6 健康数据中台管理后台 (2G)**

##### **3.6.1 合规大脑**

**功能描述**：依托星耀产业资本合规大脑能力，对数据中台全链路进行合规监控与风险拦截。

**子功能点**：
1.  **数据采集合规审核**：审核新增数据采集字段的合规性（最小必要、用户授权范围）。
2.  **数据使用合规监控**：监控数据使用行为，识别超范围使用、未授权访问。
3.  **数据输出合规审核**：对外数据输出（含融资估值数据输出）须经合规大脑审核，自动脱敏与去标识化。
4.  **合规风险事件库**：沉淀合规风险事件，支撑审计与监管报送。

##### **3.6.2 治疗数据沉淀**

**功能描述**：将门店治疗数据按标准化结构沉淀为数据资产，支撑产品迭代与融资估值。

**子功能点**：
1.  **数据采集标准化**
    - 制定统一数据采集标准（字段定义、取值范围、采集时序）。
    - 数据接入多源异构数据（门店 POS、调理师工作台、客户档案、疼痛评估系统）。
2.  **结构化存储**
    - 按**疼痛类型**（颈肩痛/腰腿痛/关节痛/神经痛等）、**调理方案**（方案模板 ID/产品组合/周期）、**产品组合**（SKU 集合/用量/频次）三维结构化存储。
    - 建立数据血缘关系：客户档案→评估→方案→治疗记录→效果→复购 全链路可追溯。
3.  **数据质量治理**
    - 数据完整性、准确性、一致性、时效性自动化巡检。
    - 异常数据自动隔离与人工复核队列。
4.  **数据资产目录**
    - 数据资产目录化，标注资产所有者、敏感等级、使用范围、质量评分。
    - 支撑数据资产的检索、申请、审批、使用全流程。

**数据需求**：
- 数据资产目录表：资产 ID、资产名称、所有者、敏感等级、使用范围、质量评分、更新频率、血缘关系 JSON。
- 数据血缘表：血缘 ID、上游资产 ID、下游资产 ID、转换逻辑、更新时间。

##### **3.6.3 AI 辅助决策**

**功能描述**：基于数据沉淀构建 AI 模型，为门店运营与调理师提供风险画像、方案推荐、依从性管理能力。

**子功能点**：
1.  **风险画像**
    - 客户健康风险画像：基于疼痛评估、治疗记录、效果数据，识别高风险客户（如疼痛恶化、疑似禁忌症）。
    - 门店经营风险画像：基于经营数据，识别经营异常门店。
    - **规则**：高风险客户自动触发就医建议提醒，调理师不可继续执行调理方案。
2.  **方案推荐**
    - 基于客户画像与同类型客户历史效果数据，推荐**最优调理方案**（Top3 方案及预期效果）。
    - 推荐结果附带可解释性说明（推荐依据、相似客户案例数、历史效果分布）。
3.  **依从性管理**
    - 预测客户依从性下降风险，提前触发干预动作。
    - 推荐个性化干预策略（提醒频次、内容、渠道）。
4.  **模型管理**
    - 模型版本管理：模型训练数据集、版本号、上线时间、效果指标（准确率/召回率/AUC）。
    - 模型效果监控：线上模型预测效果实时监控，效果衰减自动告警。
    - 模型下线与回滚机制。

**数据需求**：
- AI 模型表：模型 ID、模型名称、版本号、训练数据集 ID、算法类型、效果指标 JSON、状态、上线时间、下线时间。
- 模型预测日志表：日志 ID、模型 ID、版本号、输入特征 JSON、预测结果 JSON、预测时间、用户 ID。

##### **3.6.4 数据资产管理**

**功能描述**：将沉淀的治疗数据作为核心数据资产进行全生命周期管理，支撑融资估值对外输出。

**子功能点**：
1.  **数据资产估值**
    - 按数据量、数据质量、数据稀缺性、应用价值等维度定期评估数据资产价值。
    - 输出数据资产估值报告，支撑融资尽调。
2.  **数据资产对外输出**
    - 支撑产品迭代：向产品研发输出治疗数据洞察（脱敏后）。
    - 支撑融资估值：向星耀产业资本、投资机构输出结构化治疗效果数据（合规审核后）。
3.  **数据资产审计**
    - 全量数据资产访问日志，支撑审计与监管。
    - 数据资产使用月报/季报自动生成。

### Part C：平台管理后台

#### **3.7 合规监管与数据资产看板 (2G)**

##### **3.7.1 互联网医院监管（保留扩展）**

**功能描述**：监控平台整体合规运营情况。

**子功能点**：
1.  **业务总览**：实时显示在线医生数、今日问诊数、今日处方数、处方审核通过率。
2.  **异常预警**：
    - **处方预警**：如"同一医生短时内开具大量相同药品"、"单张处方金额异常偏高"。
    - **行为预警**：如"同一地址频繁接收多张处方药"。
    - 预警信息需列表展示，并可下发给人工审核员处理。

##### **3.7.2 产品销售监管**

**功能描述**：监控平台药品与调理产品销售合规性。

**子功能点**：
1.  **销售总览**：药品销售额、调理产品销售额、退货率、投诉率。
2.  **合规预警**：保健品夸大宣传、处方药违规销售、调理产品超范围使用等预警。

##### **3.7.3 数据资产看板**

**功能描述**：星耀产业资本/平台运营层查看全平台数据资产运营情况。

**子功能点**：
1.  **数据资产总览**：数据资产总量、按业务线分布、按敏感等级分布、数据资产估值趋势。
2.  **数据使用监控**：数据访问量、数据输出量、合规审核通过率、合规风险事件数。
3.  **AI 模型监控**：在运行模型数、模型效果分布、模型衰减告警数。
4.  **融资估值支撑**：数据资产估值报告归档、对外输出记录查询。

## **4. 非功能性需求**

1.  **性能需求**：
    - 页面加载时间：普通页面 <2s，图片较多页面 <3s。
    - 核心交易接口（支付、开处方）响应时间 <1s。
    - 数据中台大数据查询：单查询响应时间 <5s，支持千万级数据量下的多维分析。
    - AI 模型预测响应时间：方案推荐 <2s，风险画像 <3s。
2.  **安全性需求**：
    - 通过网络安全等级保护（三级）认证。
    - 防 SQL 注入、XSS 攻击、CSRF 攻击。
    - 用户密码、敏感信息加密存储（采用 bcrypt 等强哈希算法）。
    - 数据资产访问全量审计日志，支持按用户/时间/数据范围检索。
    - AI 模型训练数据与预测结果脱敏存储，模型权重文件加密保护。
3.  **兼容性需求**：
    - 支持主流 iOS/Android 机型上的微信小程序最新版本。
    - 数据中台管理后台支持主流浏览器（Chrome 90+、Edge 90+、Safari 14+）。
4.  **数据治理需求**：
    - 数据采集遵循"最小必要"原则，明确用户授权范围。
    - 数据资产实行全生命周期管理（采集→清洗→存储→分析→输出→归档/销毁），各环节留痕。
    - 数据质量自动化巡检，异常数据隔离率 ≥99%。
    - AI 模型可解释性：方案推荐须附带可解释性说明，风险画像须可追溯至原始数据。

## **5. 附录**

### **5.1 数据字典（部分）**

| 字段名                    | 类型    | 说明                       | 示例                                            |
| ------------------------- | ------- | -------------------------- | ----------------------------------------------- |
| `user_real_name`          | String  | 用户真实姓名（脱敏）       | 张*                                             |
| `doctor_license_no`       | String  | 医生执业证书编号           | 110xxxxx20101010                                |
| `prescription_status`     | Enum    | 处方状态                   | pending_audit, approved, rejected               |
| `drug_is_rx`              | Boolean | 是否为处方药               | true                                            |
| `customer_id`             | String  | 客户健康档案 ID            | C20260724001                                    |
| `store_id`                | String  | 连锁门店 ID                | S001                                            |
| `therapist_id`            | String  | 调理师 ID                  | T001                                            |
| `pain_score`              | Float   | 疼痛评分（0-10）           | 6.5                                             |
| `pain_assessment_type`    | Enum    | 评估量表类型               | VAS, NRS, McGill                                |
| `treatment_record_id`     | String  | 治疗记录 ID                | TR20260724001                                   |
| `care_plan_id`            | String  | 调理方案 ID                | CP001                                           |
| `effect_level`            | Enum    | 效果等级                   | significant, effective, ineffective, worsened   |
| `repurchase_risk`         | Enum    | 复购风险等级               | high, medium, low                               |
| `data_asset_id`           | String  | 数据资产 ID                | DA001                                           |
| `data_sensitivity_level`  | Enum    | 数据敏感等级               | L1, L2, L3, L4                                  |
| `ai_model_id`             | String  | AI 模型 ID                 | M001                                            |
| `ai_model_version`        | String  | 模型版本号                 | v1.2.0                                          |

### **5.2 数据资产生命周期说明**

数据资产实行全生命周期管理，各阶段要求如下：

| 阶段       | 关键动作                             | 责任方               | 留痕要求                           |
| ---------- | ------------------------------------ | -------------------- | ---------------------------------- |
| 采集       | 用户授权、字段标准化、最小必要       | 门店/调理师/中台运营 | 授权书电子签名、采集日志           |
| 清洗       | 数据去重、格式校验、异常隔离         | 数据中台             | 清洗规则版本、隔离记录             |
| 存储       | 分级分类、加密存储、血缘记录         | 数据中台             | 存储位置、加密算法、血缘关系       |
| 分析       | 模型训练、效果评估、洞察输出         | 数据中台/AI 模型     | 模型版本、训练数据集、效果指标     |
| 输出       | 合规审核、脱敏处理、协议管控         | 合规大脑/星耀资本    | 审核记录、脱敏规则、输出协议       |
| 归档/销毁  | 期满归档、合规销毁                   | 数据中台/合规大脑    | 归档时间、销毁证书                 |

### **5.3 待定问题**

1.  医保支付接口的对接方案与时间表需与各地医保局沟通后确定。
2.  具体药品目录和价格需与合作药房最终确认后导入系统。
3.  连锁门店接入方案：直营店、加盟店的数据归属与共享边界需法务明确。
4.  调理师职业资格证书范围与准入标准需与人力资源及合规确认。
5.  AI 模型的训练数据来源、第三方数据合作方案需进一步明确。
6.  数据资产对外输出的合规审批流程与监管报送要求需与法务、星耀产业资本共同确认。
7.  疼痛评估量表的标准化与医学专家评审机制需建立。

## **6. 技术架构建议**

> 本章为技术选型与数据中台实施方案，详细版见独立文档《互联网医疗中心平台技术架构设计方案》。

### **6.1 架构总览**

采用"接入层 → 应用层 → 数据层 → AI 层"四层架构：

```
接入层   微信小程序(2C用户端 / 2B医生端)  +  Web管理后台(门店 / 数据中台 / 平台)
        │
应用层   业务中台(问诊 / 处方 / 商城 / 档案) + 数据中台(采集→治理→资产) + AI决策服务
        │
数据层   业务库(OLTP) · 缓存 · 文档库 · OLAP看板 · 向量库(AI) · 搜索引擎 · 数据湖(资产)
        │
AI层     特征工程 · 模型训练 · MLOps · LLM诊断辅助 · RAG知识库
```

- **接入层**：微信小程序承接 2C/2B 轻量交互；Web 后台承接运营、看板与治理。
- **应用层**：业务逻辑与 AI 推理通过 API 网关串联，AI 仅输出辅助结果。
- **数据层**：多类型存储各司其职，统一通过数据中台对外提供资产服务。
- **AI 层**：离线训练 + 线上推理，模型版本与数据血缘全程可追溯。

### **6.2 前端技术选型**

| 场景 | 推荐方案 | 选型理由 |
| ---- | -------- | -------- |
| 微信小程序（2C/2B） | **原生小程序** 或 `Taro` / `uni-app` | PRD 明确以微信小程序为载体；Taro 复用 React 语法，组件可复用于后台 |
| 三套管理后台 | **Vue3 + Element Plus** 或 **React + Ant Design Pro** | 后台 CRUD/表单密集，ProTable、表单校验开箱即用，开发最快 |
| 中台数据看板 | **ECharts / AntV**，大屏用 **DataV** | 覆盖到店率、转化率、客单价、复购率、NPS、效果趋势、风险画像 |
| 低代码后台骨架 | **若依 RuoYi** 或 **Amis** | RuoYi 自动生成权限 + CRUD 骨架，可省约 30% 后台工作量 |

### **6.3 后端开发语言与框架**

**主推：Python 为主、Node.js 为辅、Go 兜底高并发。**

- **Python (FastAPI)**：AI/ML 生态全栈霸主（PyTorch、scikit-learn、pandas、MLflow）；FastAPI 异步性能优、自动生成接口文档，开发快；复购预测、风险画像、方案推荐、诊断分析统一由 Python 实现。
- **Node.js (NestJS)**：若团队前端为 JS 栈，业务接口统一用 NestJS，适合问诊 IM、订单、支付回调等 IO 密集场景。
- **Go (Gin)**：仅在高并发实时推送（IM 问诊、预警推送）引入，非必需。

> 结论：单一 Python 全栈（FastAPI + 前后端分离）是"快速开发 + AI 贴合"的最优解；若前后端同语言，采用 Node.js 业务 + Python AI 微服务。

### **6.4 数据存储架构**

| 用途 | 选型 | 对应 PRD 需求 |
| ---- | ---- | ------------- |
| 业务主库 OLTP | **PostgreSQL**（首选）或 MySQL | 用户/医生/处方/订单；PG 原生 JSONB 存处方清单、健康标签 |
| 文档/半结构化 | **MongoDB** | 治疗记录 JSON、疼痛评估原始答案 JSON、方案组合 |
| 缓存/会话/队列 | **Redis** | 登录态、处方状态、预警去重、会话列表 |
| OLAP 数据看板 | **ClickHouse**（首选）或 Apache Doris | "千万级多维分析 <5s"——经营宽表、效果分布、复购率滚动窗口 |
| 向量库（AI） | **Milvus** 或 **pgvector** | 方案推荐"相似客户案例"检索、RAG 诊断知识库、风险画像向量化 |
| 搜索引擎 | **Elasticsearch** | 找医生/找药品全文检索、合规风险事件库检索 |
| 数据湖/资产 | 对象存储 + **Apache Iceberg/Hudi** | 5.2 数据资产生命周期：原始数据归档、血缘、对外输出 |

> 起步精简组合：**PostgreSQL + Redis + MongoDB + ClickHouse + pgvector**，覆盖约 95% 需求，全部开源可控。

### **6.5 AI 工具链与数据中台**

贴合"训练数据来源 → 录入 → 分析 → 挖掘 → 诊断分析"全链路：

- **数据录入**：小程序/调理师工作台表单 → FastAPI 写入 Mongo/PG；图片经 OCR（PaddleOCR）提取处方/评估单。
- **数据处理/挖掘**：`pandas` / `PySpark`（千万级用 Spark on 数据湖）。
- **特征工程 & 训练**：`scikit-learn`（复购/风险/依从性用 XGBoost）、`PyTorch`（疼痛效果预测）；`MLflow` 管理模型版本（对应 3.6.3）。
- **诊断/方案辅助**：`LangChain` / `LlamaIndex` + 大模型 API（GLM/通义/混元）做 RAG，输出"可解释方案推荐 + 相似案例数"；仅辅助，不替代医师。
- **MLOps**：`DVC` + `MLflow` + 模型服务化（FastAPI / BentoML）。
- **看板数据源**：核心指标预计算写入 ClickHouse，前端 ECharts 直连查询。

**数据中台流水线**：采集 → 标准化 → 清洗 → 结构化存储（疼痛类型×调理方案×产品组合三维）→ 质量治理 → 资产目录（血缘/敏感等级/质量评分）→ 分析 → 输出（合规脱敏/协议管控）。

### **6.6 推荐技术组合**

- **快速起步 MVP（3–6 个月）**：小程序 `Taro` + 后台 `React + Ant Design Pro` + 后端 `Python FastAPI` + `PostgreSQL + Redis + MongoDB` + 看板 `ClickHouse + ECharts` + 向量 `pgvector` + AI `scikit-learn + MLflow`。
- **生产级（融资估值/等保三级）**：后端 `FastAPI + Python AI 微服务`；DB `PostgreSQL + MongoDB + ClickHouse + Milvus + ES`；数据中台 `Spark/Flink + 数据湖(Iceberg)`；云原生 `K8s + 等保三级安全组 + 加密存储`；实时预警用 Flink，大模型诊断辅助独立 GPU 推理节点。

### **6.7 与 PRD 关键约束的对应**

| PRD 约束 | 技术方案对应 |
| -------- | ------------ |
| 数据留存 ≥15 年、加密 | PG/Mongo TDE 加密 + 冷数据归档数据湖，满足等保三级 |
| 千万级多维分析 <5s | ClickHouse 列式存储 + 预聚合宽表 |
| AI 预测 <2–3s 响应 | 离线训练 + 线上 FastAPI + 向量索引缓存 |
| 合规大脑 / 数据血缘 | 数据湖 Iceberg 自带血缘/版本，MLflow 追模型溯源 |
| AI 可解释性 | RAG 输出推荐依据 + 相似案例数；风险画像追溯原始数据 |
| 微信支付 / 服务通知 | 后端对接微信支付 + 订阅消息 API |

### **6.8 系统部署拓扑**

采用 K8s 容器编排，按安全等级做命名空间隔离（ns-business / ns-ai / ns-data），网络分区满足等保三级（DMZ / 业务区 / 数据区）：

```
                          互联网 / 公网
                              │
                    ┌─────────┴─────────┐
                    │  CDN / WAF / DDoS  │
                    └─────────┬─────────┘
                              │ HTTPS / TLS 1.3
                    ┌─────────┴─────────┐
                    │  Nginx 入口网关     │
                    │ (SSL终止 / 限流 / 灰度)
                    └─────────┬─────────┘
                              │
                    ┌─────────┴─────────┐
                    │  API Gateway        │
                    │ (鉴权 / 路由 / 限流) │
                    └─────────┬─────────┘
                              │ 内网（业务区）
        ┌──────────────┬──────┴───────┬──────────────┐
        │              │              │              │
  业务微服务集群    AI 决策服务     数据中台服务     后台 Web 服务
  (FastAPI × N)  (推理 / GPU 节点) (采集/治理/资产) (React 静态)
        │              │              │
        └──────────────┴──────┬───────┴──────────────┘
                              │ 数据区（加密 / 隔离）
   ┌──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐
   │PostgreSQL│ MongoDB  │  Redis   │ClickHouse│ Milvus   │Elasticsea│
   │ (主从)   │ (副本集) │ (哨兵)   │ (集群)   │ (向量)   │rch(搜索) │
   └──────────┴──────────┴──────────┴──────────┴──────────┴──────────┘
                              │
              数据湖 (Iceberg / 对象存储) —— 资产归档 / 血缘
                              │
   ┌──────────────────────────┴──────────────────────────┐
   │ 安全与合规：等保三级 / 全量审计日志 / 合规大脑审核     │
   └─────────────────────────────────────────────────────┘
      外部对接：微信支付 / 短信 / 微信服务通知 / 星耀产业资本（数据输出）
```

- **接入层**：Nginx 做 SSL 终止与灰度；API Gateway 统一鉴权、路由、限流（令牌桶）、防重放。
- **业务区**：业务微服务、AI 决策服务、数据中台服务、后台静态资源分属不同命名空间，Pod 多副本 + HPA 自动扩缩容。
- **数据区**：数据库独立网段，仅允许网关与服务网段访问；敏感库启用 TDE 加密与访问审计。
- **外部依赖**：微信支付 / 短信 / 服务通知经 HTTPS 对接；向星耀产业资本的数据输出须经合规大脑审核服务。

### **6.9 接口规范**

- **风格与版本**：RESTful + JSON；路径统一前缀 `/api/v1/{module}/{resource}`，模块含 `ih`（互联网医院）、`mt`（数据中台）、`plat`（平台）。
- **鉴权**：微信小程序 `code2session` 换 `openid` 并签发 JWT；后台账号密码 + RBAC 角色权限；接口须带 `Authorization: Bearer <JWT>`。
- **统一响应**：`{ "code": 0, "message": "success", "data": {}, "timestamp": 0, "requestId": "" }`，`code=0` 成功，非 0 为错误码。
- **错误码分段**：`1xxx` 参数、`2xxx` 鉴权、`3xxx` 业务、`4xxx` 合规、`5xxx` 系统。
- **安全规则**：全站 HTTPS/TLS 1.3；敏感字段传输加密、存储脱敏；防重放（`timestamp+nonce+sign`）；限流（令牌桶）；幂等（`Idempotency-Key`）；处方/健康档案/资产输出写操作全量审计留痕。
- **接口文档**：FastAPI 自动生成 OpenAPI（Swagger），导出 `openapi.yaml` 供前端 Mock 联调。
- **关键接口示例**：

| 接口 | Method / Path | 关键字段 | 说明 |
| ---- | ------------- | -------- | ---- |
| 开具处方 | `POST /api/v1/ih/prescriptions` | `patientId, doctorId, items[], diagnosis, signature` | 医生开方，状态置 `pending_audit` |
| 录入治疗记录 | `POST /api/v1/mt/treatment-records` | `customerId, storeId, therapistId, planId, products[], nps` | 调理师录入，不可删仅可更正留痕 |
| 复购预测 | `GET /api/v1/mt/repurchase-prediction?customerId=` | 返回 `nextVisitProb, repurchaseProb, riskLevel, modelVersion` | AI 服务返回，毫秒级 |
| AI 方案推荐 | `POST /api/v1/mt/plan-recommend` | `customerId, painType` → `top3Plans[], rationale, similarCases` | RAG 输出可解释推荐，仅辅助 |

> 所有 AI 类接口返回结果均标注 `isAssist = true`，前端须明示"仅供参考，不替代医师诊断"。

### **6.10 数据库表结构设计**

- **分层与命名**：业务库 PostgreSQL 按 `ih_`（互联网医院）、`mt_`（数据中台）、`plat_`（平台）分域；统一 `id / created_at / updated_at / is_deleted`（逻辑删除）。文档库 MongoDB 存 JSON 密集型数据；ClickHouse 存看板宽表；Milvus 存向量。敏感字段存储脱敏 + 表级 TDE 加密。
- **核心表（节选）**：

| 域 | 表 | 关键字段 |
| -- | -- | -------- |
| ih | `ih_user` | `id, openid(UK), real_name_mask, phone_mask, id_card_mask, user_type` |
| ih | `ih_doctor` | `id, user_id, license_no(UK), title, hospital_id, dept, consult_price` |
| ih | `ih_prescription` | `id, prescription_no(UK), patient_id, doctor_id, pharmacist_id, diagnose, items_json, status(enum), signature_url` |
| mt | `mt_customer` | `id(customer_id), name_mask, gender, phone_mask, source_store_id, health_tags(jsonb), auth_status` |
| mt | `mt_pain_assessment` | `id, customer_id, scale_type(enum VAS/NRS/McGill), answers_json, score, pain_site, pain_nature` |
| mt | `mt_care_plan` | `id(plan_id), customer_id, doctor_advice_id, pain_type, items_json, product_combo_json` |
| mt | `mt_treatment_record` | `id(record_id), customer_id, store_id, therapist_id, plan_id, products_json, nps`（不可删仅留痕） |
| mt | `mt_repurchase_prediction` | `id, customer_id, next_visit_prob, repurchase_prob, risk_level(enum), model_version` |
| plat | `plat_data_asset` | `id(asset_id), name, owner, sensitivity_level(L1-L4), lineage_json` |
| plat | `plat_ai_model` | `id(model_id), name, version, train_dataset_id, metrics_json, status` |
| plat | `plat_audit_log` | `id, actor_id, role, action, before_json, after_json` |

- **多存储协同**：ClickHouse `mt_store_metrics`（经营宽表，预聚合）；MongoDB `treatment_records` / `pain_assessment_raw`（JSON）；Milvus `plan_embeddings` / `case_embeddings`（向量检索/ RAG）。完整字段见附录 5.1 数据字典。

### **6.11 CI/CD 流水线图**

```
开发者 push / MR
   │
   ▼  CI: Lint → 单测 → 安全扫描(SAST/依赖/密钥) → 构建镜像 → 推送私仓
   │
   ├─ dev      : 自动部署 (Helm/K8s)
   ├─ staging  : 自动部署 + 自动化回归 + 等保预检 + 压测
   └─ prod     : 人工审批 → 灰度(Canary 5%→25%→100%) → 全量
                    │
                    ▼  观测: Prometheus + Grafana，异常自动回滚(Argo Rollouts)
```

- **分支**：`main / release/* / develop / feature/*`；镜像 tag = `git-sha`，关联 SBOM 溯源（等保三级）。
- **MLOps 独立流水线**：采集 → 特征工程 → 训练(MLflow) → 模型注册 → 合规评估 → 审批上线 → 推理服务 → 效果监控/回滚；训练数据与模型版本入 `plat_ai_model` 与数据湖，血缘可追溯（对应 3.6.3 / 5.2）。

### **6.12 安全合规检查表（等保三级逐项）**

依据 GB/T 22239-2019 第三级，覆盖 10 个控制类；医疗系统叠加《个保法》《数据安全法》《互联网医院管理办法》《处方管理办法》《生成式 AI 服务管理暂行办法》。

| 控制类 | 关键控制点 | 平台对应措施 | 状态 |
| ------ | ---------- | ------------ | ---- |
| 物理环境 | 机房管控/防灾害 | 依托云厂商等保机房（索取测评证明） | 已规划 |
| 通信网络 | 网段划分/传输加密 | K8s 多可用区 + DMZ/业务区/数据区隔离；全站 TLS 1.3 | 已规划 |
| 区域边界 | 边界防护/入侵防范 | API Gateway + Nginx + WAF + DDoS + 防注入/XSS/CSRF | 已规划 |
| 计算环境 | 身份鉴别/访问控制 | JWT + 微信 `code2session`；RBAC 六角色；库表级最小授权 | 已规划 |
| 计算环境 | 安全审计 | `plat_audit_log` 全量留痕、独立防篡改存储 | 已规划 |
| 计算环境 | 数据保密/备份 | PG/Mongo **TDE 加密** + 敏感字段**存储脱敏**；主从+异地备份 | 已规划 |
| 计算环境 | 个人信息保护 | 最小必要采集 + 授权书电子签名；训练/输出脱敏去标识 | 已规划 |
| 管理中心 | 三权分立/集中管控 | 合规大脑统一管控；审计/告警/运维集中 | 已规划 |
| 管理制度/机构/人员 | 策略/岗位/培训 | 安全合规制度 + 专职岗 + 星耀资本对接 + 年度培训 | 待测评 |
| 建设/运维管理 | 定级测评/漏洞/应急 | 上线前等保测评+渗透+代码审计；季度漏扫；年度应急演练 | 建设中 |

**医疗附加合规**：复诊确认弹窗、医师资质校验、药师审核、电子签名 `signature_url`、AI 输出标注 `isAssist=true`、跨境/对外输出走合规大脑 + 协议（对应 3.1.2 / 3.1.4 / 3.6.1 / 5.2）。**上线前**：定级备案 → 差距测评 → 渗透+代码审计 → 数据分类分级清单 → 应急演练 → 持续 SOC 监测。

### **6.13 第三方服务对接清单（微信支付 / 短信）**

外部依赖统一经「API Gateway → 第三方网关代理」调用，密钥/证书托管 KMS/Vault，禁止硬编码。

| 类别 | 服务 | 用途 | 关键合规/设计 |
| ---- | ---- | ---- | -------------- |
| 交易 | 微信支付（JSAPI） | 处方/OTC 收款、退款 | 商户须持《医疗机构执业许可证》；APIv3 验签 + `out_trade_no` 幂等；处方药支付须关联处方审核通过状态（`ih_prescription.status=approved`）；每日拉账单对账；**医保支付一期不接入** |
| 通知 | 短信 | 验证码/订单/复诊/营销 | 签名+模板备案；验证码限流（60s/5 条·日）+Redis 过期；营销短信带退订、限时段，依据《通信短信息服务管理规定》 |
| 通知 | 微信订阅消息 | 复诊/开方服务通知 | 须用户订阅，退订即停 |
| 身份 | 微信登录 | `code2session` 换 `openid` | 授权最小化 |
| 存储 | 对象存储 OSS/COS | 处方/签名图片 | 私有桶 + 临时签名 URL |

**关联表**：`ih_payment`（`order_no, transaction_id, amount, trade_state, refund_status`）、`ih_sms_log`（`phone_mask, scene, template_code, status, receipt_at`）。详见技术架构设计方案「第 14 章」。

### **6.14 优化补充设计**

| 优化项 | 方案要点 | 对应章节 |
| ------ | -------- | -------- |
| 处方双轨审核 | 内部药师审核 + 外部合理用药引擎（美康/逸曜）预审，HIS 回传支撑复诊连续性 | 技术架构 15.1 |
| 门店行级隔离 | PostgreSQL RLS + `store_id` 会话注入 + API 数据权限中间件，杜绝越权 | 技术架构 15.2 |
| 隐私计算对外输出 | 联邦学习 / 多方安全计算（TEE），原始治疗数据不出域，仅输出模型参数/聚合指标 | 技术架构 15.3 |
| AI 反馈闭环 | 医生采纳/驳回回流 `plat_model_pred_log`，驱动模型再训练与采纳率看板 | 技术架构 15.4 |
| 可观测性补全 | OpenTelemetry 链路追踪（Jaeger）+ Loki/ELK 日志集中，与 `TraceID` 关联 | 技术架构 15.5 |
| 缓存一致性 | Redis Cache-Aside + 延迟双删，敏感字段以 PG 为唯一真相源 | 技术架构 15.6 |
| 搜索 CDC 同步 | ES 经 Debezium/Canal 捕获 PG binlog 准实时同步，避免双写 | 技术架构 15.7 |
| 第三方双通道容灾 | 短信主备双云自动切换；支付预留聚合支付兜底，主链路不降级 | 技术架构 15.8 |
| 上线前置许可 | 互联网医院执业许可 / ICP+EDI / 药品经营许可 / 等保三级 / PIA | 技术架构 15.9 |
