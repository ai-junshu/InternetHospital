# 互联网医疗中心平台 — P0~P3 自检与对抗式审查报告

> 审查日期：2026-07-26
> 对照基线：PRD V2.0、技术架构设计方案 V1.2、工程进度与计划.md、实际代码树（`code/`）
> 审查方法：第一性原理（回到"上线 = 合规资质 + 可运行 + 风险可控"的本质）+ 对抗式（默认每项"已完成"声明可疑，逐一验证或证伪）

---

## 一、审查结论摘要（先给结论）

| 维度 | 结论 |
| --- | --- |
| 计划完成度（P0–P3） | **代码交付基本完成**，机制级验证 21/21 PASS，`tsc --noEmit` 0 错误 |
| PRD V2.0 功能覆盖 | **后端数据域 ≈70%、前台/工作台 ≈10%、平台监管 ≈20%**；存在显著缺口 |
| 技术架构方案覆盖 | RBAC/RLS/plat/可解释/数据分级 **已实现**；第三方对接/等保测评 **部分/未做** |
| 上线就绪度 | **未就绪**：缺等保测评 + 执业许可（硬门槛）+ 真实第三方对接 + 运行时验证 |

**一句话**：平台治理与数据中台后端骨架已具备，但"能上线"所需的合规资质、真实业务流、运行时验证仍大量欠账。

---

## 二、P0–P3 计划完成度核对（计划 vs 证据 vs 结论）

| 阶段 | 计划项 | 实际证据 | 结论 |
| --- | --- | --- | --- |
| P0 主链路 | 后端可启动、核心 CRUD、鉴权可跑通 | `app.main` 导入成功、路由 >50；`verify_p3` 全绿 | ✅ |
| P1 联调 | 前后端联调、login/AuthRoute、统一响应 | admin-web 7 个路由目录、AuthRoute、request 拦截器 | ✅ |
| P2 AI闭环 | RAG 可解释、反馈闭环落库、CH 经营宽表、看板 | `ai-service/rag`、`plat/model_pred_logs`、`store_metrics` + Grafana | ✅ |
| P3 平台治理 | plat 真实 CRUD / RLS / 种子 / 合理用药引擎 / 合规清单 | `plat/ai_models`、`plat/data_assets`、`core/deps.store_scope`、8 个 mt 端点注入、`app/db/seed.py`、`services/rx_engine`、`合规清单...md/docx` | ✅（代码层） |

> 注意：P0–P3 的"完成"指**代码交付与机制级验证**，不等于**运行时与业务级验证**（见第四节风险）。

---

## 三、对照 PRD V2.0 功能覆盖矩阵

### Part A：互联网医院主线
| PRD | 内容 | 实现情况 | 判定 |
| --- | --- | --- | --- |
| 3.1 用户端小程序 | 首页/找医生/找产品/复诊开药/个人中心 | `mp-taro/src/pages` 仅 5 个空壳页（index/login/order/prescription/doctor-consult），无真实业务流、无与后端联调证据 | ⚠️ Partial（脚手架） |
| 3.2 医生端小程序 | 工作台/问诊聊天室/开方 | 同上，仅 doctor-consult 页面壳 | ⚠️ Partial（脚手架） |
| 3.3 医院管理后台 | 科室医生管理/处方审核配置/合作药房 | `ih/` 有 doctors/orders/prescriptions/users/consultations；缺科室结构、药房管理、药师排班审核配置 | ⚠️ Partial |

### Part B：健康数据中台主线
| PRD | 内容 | 实现情况 | 判定 |
| --- | --- | --- | --- |
| 3.4.1 客户健康档案 | 档案/疼痛评估/治疗记录/效果追踪/复购预测 | `mt/` customers、pain_assessment、care_plans、treatment_records、effect、repurchase、risk 后端齐全；admin-web 有 customer/repurchase/risk/data | ✅ 后端 + 部分前端 |
| 3.4.2 门店经营看板 | 到店率/转化/客单价/复购率/NPS/预警 | `store_metrics` 预聚合 + Grafana 看板 | ✅ |
| 3.4.3 调理师调配 | 排班/客户分配/能力标签 | `stores`/`therapists` 管理存在；缺排班、分配、能力标签 | ⚠️ Partial |
| 3.5 调理师工作台 | 我的客户/方案执行/治疗录入/依从性 | 无独立模块；仅后端 treatment_records/care_plans | ❌ Not Done |
| 3.6.1 合规大脑 | 采集审核/使用监控/输出审核/风险事件库 | 仅有审计日志 `plat_audit_logs` + RLS；缺采集审核、使用监控、输出审核、风险事件库 | ⚠️ Partial |
| 3.6.2 治疗数据沉淀 | 标准化/血缘/质量治理/资产目录 | `plat_data_asset`（资产目录+分级+血缘 JSON）已建模 | ✅ 建模 |
| 3.6.3 AI 辅助决策 | 风险画像/方案推荐/依从性/模型管理 | `ai-service`（plan/repurchase/risk）+ `plat_ai_model` + `pred_logs`（反馈闭环+可解释） | ✅ 后端 |
| 3.6.4 数据资产管理 | 估值/对外输出/审计月报 | 仅资产目录；缺估值、对外输出、月报 | ❌ Not Done |
| 3.7 平台监管看板 | 业务总览/异常预警/产品销售监管/数据资产看板 | 仅 `audit_logs` 列表；缺监管总览、异常预警、产品销售监管 | ⚠️ Partial |

### 非功能性需求（第 4 章）
| 项 | 状态 |
| --- | --- |
| 等保三级认证 | 已出测评清单（md+docx），**未测评** |
| 防注入/XSS/CSRF | 参数化查询 + JWT；WAF/CSRF 依赖部署，未验证 |
| 密码/敏感加密存储 | 脱敏展示已做；**落库强加密（bcrypt/KMS）未做** |
| 数据资产全量审计 | ✅ 审计日志 |
| 性能/压测 | 未做 |

---

## 四、对照技术架构设计方案 V1.2

| 架构章节 | 要求 | 实现 | 判定 |
| --- | --- | --- | --- |
| 10.2 RBAC 六角色 | JWT + 角色鉴权 | `core/deps.require_role`、`core/security` | ✅ |
| 11.2 plat 资产/模型目录 | 模型与资产 CRUD | P3 真实化 | ✅ |
| 13 数据分级 L1–L4 / 隐私计算 | 分级建模、联邦学习 | `sensitivity_level` 建模；隐私计算仅规划 | ⚠️ 建模完成，计算未落地 |
| 14 第三方对接（支付/短信/合理用药） | 三方可替换 | 合理用药 = **抽象层 + Mock**；微信支付/短信 **未做** | ⚠️ Partial |
| 15.2 RLS 行级隔离 | 按门店隔离 | `store_scope` + 8 端点注入 + JWT store_id | ✅ 代码级（未运行时验证） |
| 15.4 AI 可解释/反馈闭环 | 依据 + 采纳回写 | `ai-service/rag` + `pred_logs.adopted` | ✅ |
| 15.9 等保三级 | 测评 + 资质 | 清单已出；测评/资质未启动 | ⚠️ Partial |

---

## 五、对抗式审查（找漏洞 / 证伪"已完成"）

以下逐项假设"已完成"是可疑的，给出可被推翻的证据缺口：

1. **RLS 仅代码级，未运行时验证**
   - `store_scope` 逻辑经 `verify_p3` 用模拟 payload 单测通过；但 `customer_ids_for_store` 子查询、各端点的实际 SQL 过滤**未经真实 PG 执行验证**。
   - 风险：隔离可能在某端点因关联键缺失而漏判或 SQL 报错；store/therapist 角色强制绑定 store_id 的前提是 JWT 真的带了 `store_id`，而 dev-token 的 store_id 在 seed 前无法跑通。

2. **种子数据从未执行**
   - `app/db/seed.py` 存在且幂等，但**未运行**；`MtStore` 无数据则 RLS 联调、经营看板、门店管理均无数据可查。
   - 风险：种子未落库 = 门店维度功能在演示/验收时"空转"。

3. **合理用药引擎是玩具 Mock**
   - `MockRxEngine` 仅有 4 条相互作用规则；且 `prescriptions.create` 调用时 `patient` 传空 `{}`，**孕期/过敏/日剂量信息根本没传入**，禁忌与剂量告警几乎不会触发。
   - 风险：上线若仅此 Mock，处方前置校验形同虚设，不满足处方安全合规要求。

4. **前端覆盖极窄**
   - admin-web 仅 7 个目录（customer/repurchase/risk/data/plat/store/login）；PRD 的 3.5 调理师工作台、3.3 医院管理、3.7 平台监管**无前端**。
   - 小程序 `mp-taro` 仅 5 个空壳页，无业务流、无联调证据。

5. **互联网医院主线不完整**
   - `ih/` 缺 3.3 的科室/药房/药师审核配置；3.7 监管看板（业务总览、异常预警、产品销售监管）缺失；支付/短信第三方对接缺失。

6. **等保三级未测评，安全控制未闭环**
   - 双因子认证、落库敏感字段加密（KMS）、审计日志防篡改（WORM）均未做；WAF/CSRF 依赖部署侧，未验证。

7. **性能与容灾未验证**
   - 无压测；ClickHouse 仅在聚合层占位，未验证；AI 推理响应时间未测。

8. **AI 推理真实性未验证**
   - `ai-service` 注册 3 模型，但 repurchase/risk 是否真实训练产出、`pred_log` 是否真实落库，未经运行时验证（本环境无 PG/CH 运行时）。

9. **合规硬门槛（组织流程）全部未启动**
   - 等保三级测评、医疗机构执业许可证、互联网医院准入、药品经营许可证、ICP 证、医师多点执业备案等，均为上线前置硬条件，当前仅出清单。

---

## 六、第一性原理结论

"上线"的本质 = **合规资质（可合法运营）+ 可运行系统（业务流闭环）+ 风险可控（安全/审计/隔离）**。

- **合规资质**：当前为 0（仅有清单），是阻塞上线的硬门槛。
- **可运行系统**：后端数据域骨架可达"内部可用"，但前台业务流（小程序）、医院/门店/调理师工作台、真实第三方对接缺失，离"对外运营"差一个数量级。
- **风险可控**：RBAC + RLS + 审计 + 可解释已就位（代码级），但缺双因子、落库加密、等保测评、运行时验证。

**判定**：P0–P3 完成的是"**平台治理与数据中台后端地基**"，不是"**可上线的互联网医疗产品**"。距上线仍有组织资质 + 业务前台 + 真实对接 + 验证四道大关。

---

## 七、下一步优先级建议（P3-B / P4）

按"阻塞上线 → 业务价值 → 验证"排序：

1. **合规硬门槛（最高优先级，阻塞）**：启动等保三级定级备案与测评；申办医疗机构执业许可证 + 互联网医院准入 + 药品经营许可证 + ICP。
2. **运行时验证（消除第四节省风险）**：`alembic upgrade head` + 跑 `seed.py`；用带 store_id 的 dev-token 真实联调 RLS；验证 pred_log/CH 写入。
3. **真实第三方对接**：合理用药引擎接真实供应商（HttpRxEngine 已实现骨架）；微信支付/短信对接（第 14 章）。
4. **业务前台补位**：小程序 2C/医生端真实业务流；调理师工作台（3.5）、医院管理（3.3）、平台监管看板（3.7）。
5. **安全闭环**：双因子认证、落库敏感字段加密、审计防篡改。
6. **性能与质量**：压测、ClickHouse 验证、AI 推理真实化验证。

---

## 八、验证记录（本次可复现）

- `code/backend/verify_p3.py`：**21/21 PASS**（编译、路由注册、store_scope 逻辑、rx_engine Mock、配置项、seed 结构）。
- `code/admin-web` `npx tsc --noEmit`：**0 错误**。
- 合规清单：`合规清单与等保三级准备工作.md` + `.docx`（pandoc 生成）。

> 本审查报告所有"已完成"声明均以上述代码树与机制级验证为证据；"未做/未验证"项已在第四、五、六节明确，供后续排期与验收追踪。
