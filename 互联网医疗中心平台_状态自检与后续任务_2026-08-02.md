# 互联网医疗中心平台 · 状态自检与后续任务报告

> 生成日期：2026-08-02
> 方法：第一性原理自检 + 对抗式审查 + 文档 vs 工程事实对账
> 事实源：工作区顶层文档（PRD V2.0 / 技术架构 / 整体进度 / 工程进度 / P0-P3 自检报告）+ `code/` 真实代码（含 08-02 已验证的 P3 四模块与 ErrorCode 修复）
> 可信度分级：**已运行时验证**（pytest/tsc/migration 实证）｜**已代码级验证**（源码+测试覆盖）｜**仅文档/未实证**（需联调或联库确认）

---

## A. 第一性原理自检

互联网医疗中心平台的本质是：**在强监管（医疗+药品+数据三法）约束下，运营一个"合规大脑 + 医疗 AI 数据中台"的双主线业务系统**。从"能合法上线并持续运营"这一第一性原理倒推，任何上线实体必须具备四根支柱：

| 支柱 | 第一性原理要求 | 现状对照 |
|---|---|---|
| **① 合法资质** | 等保三级测评、互联网医院执业许可、药品经营许可（合作药房模式）、ICP/EDI 备案、隐私合规（个保法/数安法） | 仅 `等保三级测评与执业许可材料清单.md` 存在，均为"清单/未取证"状态。资质为 P0 硬阻塞。 |
| **② 身份可信** | 医师/药师身份必须真实审核、不可伪造；患者身份可溯源；多租户数据隔离生效 | 医生/药师登录链路已建，但存在**开发态自动建档后门**（无条件建 `DEV-DOC` 档案并签发 doctor JWT）；RLS 注入但**缺生效验证**，`risk_profiles` 甚至完全无隔离。 |
| **③ 交易合规** | 支付必须真实收款、可对账、可退款；处方流转全程审计不可篡改 | 支付仅 dev 沙箱模拟，无真实商户号/证书联调；审计哈希链已落地（强项）。 |
| **④ 风险可控** | 合理用药真实引擎、处方双审核（医师开方+药师审方）、数据安全分级、密钥生产化 | 合理用药仅 4 条 mock 规则；审方后端流程在；密钥仍为本地 KMS 信封（非云 KMS）。 |

**第一性原理结论**：当前系统"工程骨架完成度较高"，但**四根支柱没有一根达到'可合法上线'状态**。最危险的是 ②（身份可信）和 ①（资质）——这两项是监管红线，缺失即违法上线风险，而不是"体验缺陷"。

---

## B. 对抗式审查（尝试证伪每一项"已完成"声明）

审查立场：**假设攻击者/监管核查员试图推翻"我们已就绪"的结论**。对每项声明给出"能否被证伪"的判定。

### B.1 可被证伪的"已完成"声明（真实硬伤）

| # | 声明 | 对抗式反驳（证伪证据） | 严重度 |
|---|---|---|---|
| H1 | RLS 多租户隔离已生效 | `risk_profiles.py:59-68` 两端点**完全未注入** `scope`，任意 store 角色可读取全量 `mt_risk_profile`；`effect_tracking.py:26-94` 注入了 `scope` 但**函数体内从未引用**（死参数，无隔离效果）；`scheduling.py:40` 手工调用 `store_scope(user, db)` 但第二实参 `db` 与形参 `scope_store_id: int|None = Query(...)` 语义不匹配。且 **tests/ 中无任何"A store 访问 B store 应被拒"断言**。→ 隔离"看起来有，实测未必生效"。 | 🔴 高 |
| H2 | 医生/药师身份体系已建立 | `users.py:65-70` 当 `role=doctor` 且无关联档案时，**无条件**自动创建 `IhDoctor(license_no="DEV-DOC-{user.id}", status=pending)` 并签发 doctor JWT；该逻辑**不受 `APP_ENV` 守卫**。生产环境任意用户以 doctor 角色登录即可获得医师 JWT（待审档案）。→ 伪造医师身份路径敞开。 | 🔴 高（合规红线） |
| H3 | 微信支付已完成 | `orders.py:91-149` 的 `pay_order` 调用 `create_prepay` 在 `wxpay_dev_sandbox=True` 时返回**模拟 prepay_id**；`/pay/mock-success:183` 仅沙箱可用但逻辑存在；`/wxpay/notify:160+` 含 `verify_notify` RSA 验签+AES-GCM 解密+幂等，骨架完整，但**无真实商户号/API 证书/联调记录**。→ 仅能 dev 跑通，无法真实收款。 | 🟠 中（上线阻塞） |
| H4 | 合理用药引擎可用 | `rx_engine/mock.py:18-26` 仅 **4 条相互作用规则** + 4 条孕期禁用 + 剂量告警；`HttpRxEngine` 为预留替换点，未接任何真实供应商。→ 仅能拦截最常识冲突，无法承担临床合理用药责任。 | 🟠 中 |
| H5 | 种子已补 | `seed.py` 存在且幂等，但**运行与否未实证**（连库查 `plat_account` 演示账号方可确认）。→ 演示/验收环境可能无数据。 | 🟡 低-中 |
| H6 | IhDoctor.dept 已结构化 | `ih_models.py` 中 `IhDoctor.dept` 仍为 **String 命名**，未外键化到 `IhDepartment`；`hospital.tsx` 的 DeptTab 写科室名称冗余。→ 约束关系无数据库保障。 | 🟡 低 |

### B.2 经对抗仍成立的强项（未被证伪）

| 声明 | 证据 | 判定 |
|---|---|---|
| 双因子 TOTP 已实现 | `security.py` RFC6238 + `auth.py` 真校验 + `PlatAccount.totp_secret` 用 `EncryptedString`(KMS 信封) 落库 + 迁移 0007/0009 | ✅ 真实成立 |
| 审计哈希链不可篡改 | `audit.py` SHA-256 链式 + `verify_audit_chain.py` 可出证 + 迁移 0008 | ✅ 真实成立 |
| 密码强加密落库 | `security.py` PBKDF2-HMAC-SHA256（强于文档预期的 bcrypt） | ✅ 真实成立 |
| 字段级加密 | `encrypted_field.py` KMS 信封，totp_secret/健康标签已加密 | ✅ 真实成立 |
| P3 四模块全链路 | pytest 41 passed / 前端 tsc 0 错 / alembic head=0015 | ✅ 已运行时验证 |
| 错误码统一 | `errors.py` 补 `NOT_FOUND`/`BAD_REQUEST` 别名后 27 处误用消除 | ✅ 已运行时验证 |

---

## C. 文档 vs 工程 事实对账表

> 既有 `P0-P3自检与对抗式审查报告.md`（07-26）严重低估了工程实际，导致"风险被高估、已完成被标为未做"。以下为纠正：

| 能力 | P0-P3自检报告(07-26) 标注 | 工程真实(08-02) | 偏差 |
|---|---|---|---|
| 双因子 TOTP | 未做 / 风险6 | ✅ 已落地（security/auth/迁移0007/0009） | 报告低估 |
| 审计哈希链 | 未做 | ✅ 已落地（audit/verify_audit_chain/迁移0008） | 报告低估 |
| 密码落库加密 | bcrypt/KMS 未做 | ✅ PBKDF2-HMAC-SHA256 | 报告低估 |
| 字段级加密 | 未做 | ✅ KMS 信封（encrypted_field/迁移0009） | 报告低估 |
| 微信支付 | 未做 | ⚠️ 骨架+沙箱模拟，未生产化 | 报告低估（但有真实缺口） |
| 医生身份 | 缺失/最大风险 | ⚠️ 后端已支持，但含 DEV 后门 | 部分低估（风险仍在） |
| RLS | 默认全量 | ⚠️ 部分注入，2 处缺口，无生效测试 | 报告失准 |
| 合理用药 | Mock | ⚠️ 仅 4 规则 Mock | 基本一致 |
| P3 四模块 | 硬编码演示 | ✅ 全链路真实（08-02 验证） | 报告失准（已演进） |

**结论**：文档体系（尤其 P0-P3 自检报告）已**严重过时**，不能作为状态依据。应以本报告 + 实际代码为准。

---

## D. 重新梳理的项目状态报告

### D.1 后端 P0~P3 模块真实完成度

| 模块 | 状态 | 证据级别 |
|---|---|---|
| P0 用户/认证/双因子 | ✅ 认证+JWT+TOTP+落库加密 已完成；⚠️ DEV 建档后门待关 | 已代码级验证 |
| P0 处方/审方/订单/咨询 | ✅ 全流程可跑（pytest 覆盖） | 已运行时验证 |
| P0 支付 | ⚠️ 骨架+沙箱模拟，未生产化 | 已代码级验证 |
| P1 数据中台(mt) | ⚠️ 10 路由 7 隔离，2 处 RLS 缺口，无生效测试 | 已代码级验证 |
| P2 合理用药 | ⚠️ 4 规则 Mock，真引擎未接 | 已代码级验证 |
| P3 药房库存 | ✅ 全链路（08-02 验证） | 已运行时验证 |
| P3 投诉售后 | ✅ 全链路 | 已运行时验证 |
| P3 监管看板 | ✅ 聚合端点替代临时方案 | 已运行时验证 |
| P3 科室结构 | ✅ CRUD 真实（dept 未外键化） | 已运行时验证 |

### D.2 前端真实完成度

- admin-web：7 模块 + P3 四页 + 双因子/审方/投诉/看板 **tsc 0 错误**（08-02 验证）。
- mp-taro：S1 双身份登录**已接入**（token 持久化 + role 路由）；S2 药师审核入口存在但小程序端调用需实证；其余 S3-S8 半闭环/缺口（见后续任务）。

### D.3 合规资质（硬门槛，独立于工程）

| 资质 | 状态 |
|---|---|
| 等保三级测评 | 材料清单已备，未测评取证 |
| 互联网医院执业许可 | 未取证 |
| 药品经营许可（合作药房） | 未取证 |
| ICP 备案 / EDI 许可证 | 未确认 |
| 隐私合规（个保法/数安法） | 设计层有（脱敏/加密/审计），待合规评审 |

### D.4 测试与工程质量

- 后端 pytest：**41 passed**（含 P3 全模块 + XFF + safe_jsonable + 幂等 + RLS 单租户）。
- 前端 tsc：**0 错误**。
- 缺口：无多租户越权集成测试、无支付联调测试、无 seed 运行实证、无 CI/CD。

---

## E. 后续任务分级清单（P0~P3）

### P0 · 合规硬门槛（上线阻塞，不可绕过）
1. **关掉 DEV 建档后门（H2）**：`users.py:65-70` 加 `APP_ENV/DEBUG` 守卫，生产环境禁止自动建 `DEV-DOC`/`DEV-PHA` 档案；doctor/pharmacist 登录必须绑定**已审核通过**的档案，否则拒签。
2. **修复 RLS 缺口（H1）**：`risk_profiles.py` 注入 `store_scope`/`customer_ids_for_store`；`effect_tracking.py` 将 `scope` 用于 WHERE；修正 `scheduling.py:40` 参数语义。补**多租户越权集成测试**（A store 访问 B store 应 2003 FORBIDDEN）。
3. **合规资质推进**：等保三级测评启动、互联网医院执业许可/药品经营许可申请、ICP/EDI 备案确认、隐私合规评审。
4. **支付生产化（H3）**：接入真实微信支付商户号+API 证书，联调统一下单/回调验签/退款/对账；禁用或严格守卫 `/pay/mock-success`。

### P1 · 工程收口（可信上线前必做）
5. **种子实证（H5）**：在演示/验收环境执行 `seed.py` 并验证 `plat_account` 演示账号与双因子已注入；补 seed 运行检查脚本。
6. **密钥生产化**：本地 KMS 信封 → 云 KMS（如腾讯云 KMS），密钥轮转与权限收敛。
7. **合理用药真实化（H4）**：接入真实供应商/知识库，`HttpRxEngine` 落地；保留 Mock 作降级。
8. **IhDoctor.dept 外键化（H6）**：迁移 + 约束关联到 `IhDepartment`，清洗历史 String 数据。

### P2 · 真实化与数据闭环
9. **数据中台(mt) 完善**：CH 聚合验证、数据资产采集器打通（P6）、调理师/健康顾问前端、门店运营看板。
10. **审计脱敏对齐**：审计日志 PII 字段统一脱敏，与展示层一致。

### P3 · 产品补全（小程序闭环）
11. **小程序 S2-S8 闭环**：药师审核真调 `PATCH /prescriptions/{id}/audit` 实证；订单/支付/咨询/处方流转/健康档案/数据中台赋能页补全；开发态后门在小程序端同样关闭。
12. **监管端/医院端/平台运营端前端**：补齐 `operations.tsx` 之外未覆盖的监管看板下钻、医院管理后台。

### 对抗式审查遗留项（必须纳入 P0/P1 跟踪）
- H1 RLS 2 处缺口 + 无生效测试 → 任务 2
- H2 DEV 建档后门 → 任务 1
- H3 支付未生产化 → 任务 4
- H4 合理用药 4 规则 → 任务 7
- H5 种子未实证 → 任务 5
- H6 dept 未外键化 → 任务 8

---

## F. 交付说明
- 本报告为 `.md`，未生成 `.docx`（遵循"文档默认只输出 .md"约定）。
- 后续将同步修订 `互联网医疗中心平台整体进度与下一步计划.md` 与 `互联网医疗中心平台工程进度.md` 以纠正事实偏差。
- `互联网医疗中心平台_项目状态报告.xlsx` 仅在确认后更新。
