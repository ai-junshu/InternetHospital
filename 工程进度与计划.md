# 互联网医疗中心平台 · 工程进度与计划

> 最后更新：2026-07-26
> 依据：PRD V2.0（双主线、7 类用户）、技术架构设计方案 V1.2
> 说明：本文件为"精简整合版"进度跟踪；完整汇总（含 PRD/架构覆盖矩阵、对抗式风险缺口、里程碑排期）见《互联网医疗中心平台整体进度与下一步计划.md》及其 .docx。

---

## 一、总体状态（一句话结论）

**代码交付基本完成（P0–P3 平台治理与数据中台后端地基已就位，机制级验证 21/21 PASS），但"上线就绪度 = 未就绪"：缺等保三级测评、互联网医院/药品经营等执业许可（硬门槛）、真实第三方对接与运行时验证。** 当前是"可内部研发验证的后端骨架"，非"可对外运营的互联网医疗产品"。

| 维度 | 结论 |
| --- | --- |
| 计划完成度（P0–P3） | 代码交付基本完成，机制级验证 21/21 PASS，`tsc --noEmit` 0 错误 |
| PRD V2.0 功能覆盖 | 后端数据域 ≈70%、前台/工作台 ≈10%、平台监管 ≈20% |
| 技术架构方案覆盖 | RBAC / RLS / plat / 可解释 / 数据分级已实现；第三方对接 / 等保测评部分或未做 |
| 上线就绪度 | **未就绪** |

---

## 二、各模块进度明细

### 文档（✅ 已完成）
| 文档 | 状态 |
|------|------|
| PRD V1.0（纯 2C 在线问诊） | 完成 |
| PRD V2.0（双主线、7 类用户） | 完成 |
| 技术架构设计方案 V1.2（15 章） | 完成 |
| 合规清单与等保三级准备工作 | 完成（md+docx） |
| P0–P3 自检与对抗式审查报告 | 完成（md+docx） |
| 整体进度与下一步计划（本汇总配套） | 完成（md+docx） |

### ai-service（✅ 最成熟，已端到端验证）
- `model_loader.py`：MLflow 加载可用（已修 `torch` DLL 崩溃、`version` int→str 两处 bug）。
- 三个接口 **真实推理通过**：`plan-recommend` v8 / `repurchase-prediction` v7 / `risk-profile` v5，均带 `isAssist:true`。
- `train_sample.py`：训练并注册 3 模型到 Production 正常。
- ✅ **RAG 可解释（2026-07-26）**：`plan-recommend` 接入 `app/rag/retriever.py`，`rationale` 引用真实知识条目。
- ✅ **AI 反馈闭环（2026-07-26）**：业务回写落 `plat_model_pred_log(adopted=pending)` + `plat/model-pred-logs` 采纳/驳回接口（RBAC + 审计）；`model_id` 可空。

### backend（🟢 P0–P3 业务链路已落地并验证）
- **基础设施**：`core`（config / response / errors / security JWT+防重放 / deps）、`middleware`（request_id / idempotency / audit 骨架）、`db`、`models`（完整 `ih_/mt_/plat_` ORM）、`schemas`、`alembic`（0001/0003/0004/0005）、Dockerfile。
- **业务路由（真实 CRUD）**：`ih`（users 登录签发 JWT / doctors / orders+支付 / prescriptions 开方+药师审核 / consultations 在线复诊 会话+图文消息）、`mt`（customers+授权 / pain_assessment / care_plans 对接 ai-service / treatment_records / stores/therapists / repurchase_predictions 回写 / risk_profiles 回写）、`plat`（audit-logs 真实查询 + RBAC；ai_models / data_assets 真实 CRUD）。
- **🔴 鉴权强制 + 审计真实化（2026-07-26）**：全部业务路由注入 `current_user`/`require_role`；`write_audit` 取 JWT 真实主体；新增 `POST /auth/dev-token`（仅 debug 开启）。机制级验证 9/9 PASS。
- **P1 复购/风险回写**：调用 ai-service v7/v5 真实推理并落库，AI 不可用优雅降级。
- **P2 ClickHouse 经营宽表（2026-07-26）**：`services/store_metrics.py`（PG 预聚合写 CH，幂等；CH 不可用降级）+ `mt/store-metrics` + Grafana 看板（金额字段暂置 0，待订单对接补齐）。
- **P3 平台治理（2026-07-26 ✅）**：① plat ai_models/data_assets 真实 CRUD（RBAC+审计）；② RLS 行级隔离（`store_scope` + JWT `store_id` + dev-token 带门店令牌，8 个 mt 端点注入）；③ `app/db/seed.py` 幂等种子（3 门店+调理师+预置 3 模型/4 资产）；④ 合理用药引擎 `services/rx_engine`（抽象层+Mock，开方前置校验降级，预留 HttpRxEngine）；⑤ 等保/执业许可合规清单。机制级验证 21/21 PASS。
- **🔐 jwt_secret 加固（2026-07-26）**：32 字节强随机串，`.env`/`.env.example` 同步，警告已消除。
- **admin-web 登录入口（2026-07-26）**：登录页 + `AuthRoute` 守卫 + 401 清理。

### admin-web（🟢 已接真实接口）
- `services/mt.ts` + `services/request.ts`（axios 封装、JWT 注入、401 清理）= 真实对接；`BasicLayout` + `AuthRoute`。
- 真实页面：customer（列表+授权）、repurchase（发起+历史）、risk（发起+历史）、data（经营看板）、plat（审计日志 + AI 模型目录 + 数据资产目录）、login。
- `npx tsc --noEmit` 0 错误。

### mp-taro（🔴 仅页面骨架）
- 5 个占位页（index / login / doctor-consult / order / prescription），`wx.login` 仅 `console.log`，无真实业务流。

### infra（🟡 编排较完整）
- ✅ `docker-compose.yml`：PG(+pgvector) / Redis / Mongo / ClickHouse / MinIO / Jaeger / Prometheus / Grafana 全编排。
- ✅ `k8s/helm`：namespace + deployment 基础模板；`pre-commit`、README 在。
- ⚠️ `nginx` 配置、pg/CH 的 init SQL 多为占位。

---

## 三、关键风险 / 待决事项（对抗式审查缺口）

1. **RLS 仅代码级，未运行时验证**（子查询/各端点 SQL 未经真实 PG 执行）。
2. **种子数据从未执行**（`seed.py` 幂等但未跑，门店维度功能演示"空转"）。
3. **合理用药引擎是玩具 Mock**（`MockRxEngine` 仅 4 条规则，开方 `patient` 传空，禁忌/剂量告警几乎不触发）。
4. **前端覆盖极窄**（admin-web 7 目录；PRD 3.5 调理师工作台 / 3.3 医院管理 / 3.7 平台监管无前端；小程序空壳）。
5. **互联网医院主线不完整**（`ih/` 缺科室/药房/药师审核配置、监管看板；支付/短信未对接）。
6. **等保未测评 + 安全未闭环**（双因子、落库加密 KMS、审计防篡改 WORM 均未做）。
7. **性能与容灾未验证**（无压测、CH 未验证、AI 推理时延未测）。
8. **AI 推理真实性未运行时验证**（本环境无 PG/CH 运行时）。
9. **合规硬门槛全未启动**（等保测评、执业许可、药品经营许可、ICP、多点执业备案仅出清单）。

> 详细证据与 PRD/架构覆盖矩阵见《互联网医疗中心平台整体进度与下一步计划.md》。

---

## 四、下一步计划（P3-B / P4，优先级排序）

| 优先级 | 计划项 | 关键交付物 | 阻塞/价值 |
| --- | --- | --- | --- |
| **P0（最高，阻塞）** | 合规硬门槛 | 等保三级定级备案与测评排期；申办执业许可证 + 互联网医院准入 + 药品经营许可证 + ICP + 医师多点执业备案 | 阻塞上线 |
| **P1** | 运行时验证 | `alembic upgrade head` + 跑 `seed.py`；带 store_id 的 dev-token 真实联调 RLS；验证 pred_log / CH 写入 | 消除风险 1/2/8 |
| **P2** | 真实第三方对接 | HttpRxEngine 接真实合理用药供应商；微信支付 / 短信对接；开方 patient 信息真实传入 | 消除风险 3/5 |
| **P3** | 业务前台补位 | 小程序 2C/医生端真实业务流；调理师工作台（3.5）、医院管理（3.3）、平台监管看板（3.7） | 消除风险 4/5 |
| **P4** | 安全闭环 | 双因子认证、落库敏感字段加密（KMS）、审计防篡改（WORM） | 消除风险 6 |
| **P5** | 性能与质量 | 压测、ClickHouse 验证、AI 推理真实化验证、WAF/CSRF 部署验证 | 消除风险 7 |

**排期建议**：① 立即并行启动合规资质申办 + 本地运行时验证环境；② 迭代 1 完成运行时验证闭环；③ 迭代 2 真实第三方对接；④ 迭代 3–4 业务前台补位；⑤ 安全闭环与性能质量贯穿并行。

---

## 五、验证记录（可复现）

- `code/backend/verify_p3.py`：**21/21 PASS**。
- `code/admin-web` `npx tsc --noEmit`：**0 错误**。
- 合规清单 / 审查报告 / 整体进度文档：均含 md + docx（pandoc 生成）。
